<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>周言志</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://www.tenxiaodao.top/</id>
  <link href="https://www.tenxiaodao.top/" rel="alternate"/>
  <link href="https://www.tenxiaodao.top/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, 周言志</rights>
  <subtitle>欢迎来到我的世界</subtitle>
  <title>言志</title>
  <updated>2026-09-19T15:28:56.360Z</updated>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="安全" scheme="https://www.tenxiaodao.top/categories/%E5%AE%89%E5%85%A8/"/>
    <category term="信息安全" scheme="https://www.tenxiaodao.top/tags/%E4%BF%A1%E6%81%AF%E5%AE%89%E5%85%A8/"/>
    <category term="技能大赛" scheme="https://www.tenxiaodao.top/tags/%E6%8A%80%E8%83%BD%E5%A4%A7%E8%B5%9B/"/>
    <category term="备考" scheme="https://www.tenxiaodao.top/tags/%E5%A4%87%E8%80%83/"/>
    <content>
      <![CDATA[<p>本文整理山西省职业院校技能大赛(高职组)”信息安全管理与评估”赛项的规程要点、考核内容与备考方向,供参赛备赛参考。资料来源为赛项官方规程文件(第十八届,2024年12月发布)。</p><span id="more"></span><h2 id="赛项概览"><a href="#赛项概览" class="headerlink" title="赛项概览"></a>赛项概览</h2><ul><li><strong>赛项名称</strong>:信息安全管理与评估(Information Security Management and Evaluation)</li><li><strong>赛项组别</strong>:高职组(含教师组与学生组)</li><li><strong>赛项归属</strong>:电子信息大类</li><li><strong>竞赛形式</strong>:个人赛,以院校为单位组队参赛,不得跨校组队</li><li><strong>竞赛总时长</strong>:3 小时(不含阶段切换时间)</li></ul><blockquote><p>说明:本赛项对接全国职业院校技能大赛网络安全项目的技术标准,覆盖信息安全技术应用高职专业教学标准。</p></blockquote><h2 id="竞赛模块与分值"><a href="#竞赛模块与分值" class="headerlink" title="竞赛模块与分值"></a>竞赛模块与分值</h2><p>竞赛分三个阶段,各模块内容、时长与权重如下:</p><table><thead><tr><th>阶段</th><th>模块</th><th>主要内容</th><th>时长</th><th>分值&#x2F;权重</th></tr></thead><tbody><tr><td>第一阶段</td><td>网络平台搭建与设备安全防护</td><td>网络规划、基础网络、安全设备配置与防护</td><td>60 分钟</td><td>300 分(权重 30%)</td></tr><tr><td>第二阶段</td><td>网络安全事件响应、数字取证、应用程序安全</td><td>日志分析、内存取证、流量分析、逆向分析</td><td>60 分钟</td><td>300 分(权重 30%)</td></tr><tr><td>第三阶段</td><td>夺旗挑战 CTF + 理论技能与职业素养</td><td>渗透测试、理论答题</td><td>60 分钟</td><td>400 分(权重 40%)</td></tr></tbody></table><p>第三阶段权重最高(40%),其中 CTF 渗透占 25%、理论与职业素养占 15%。</p><h2 id="第一阶段-网络平台搭建"><a href="#第一阶段-网络平台搭建" class="headerlink" title="第一阶段:网络平台搭建"></a>第一阶段:网络平台搭建</h2><h3 id="网络规划"><a href="#网络规划" class="headerlink" title="网络规划"></a>网络规划</h3><ul><li>VLSM(可变长子网掩码)、CIDR(无类别域间路由)</li></ul><h3 id="基础网络"><a href="#基础网络" class="headerlink" title="基础网络"></a>基础网络</h3><p>VLAN、WLAN、STP、SVI、RIPv2、OSPF、BGP、IPv6、组播等</p><h3 id="网络安全设备配置与防护"><a href="#网络安全设备配置与防护" class="headerlink" title="网络安全设备配置与防护"></a>网络安全设备配置与防护</h3><ul><li><strong>访问控制</strong>:防 DoS&#x2F;DDoS 攻击、包过滤、应用层代理、状态化包过滤、URL 过滤、基于 IP&#x2F;协议&#x2F;应用&#x2F;用户角色&#x2F;时间等的带宽控制、QoS 策略</li><li><strong>密码学和 VPN</strong>:密码学基本理论、L2L IPSec VPN、GRE Over IPSec、L2TP Over IPSec、IKE(PSK&#x2F;PKI)、SSL VPN</li><li><strong>数据分析</strong>:利用日志系统进行数据分析,把控网络安全</li></ul><h2 id="第二阶段-安全事件响应与数字取证"><a href="#第二阶段-安全事件响应与数字取证" class="headerlink" title="第二阶段:安全事件响应与数字取证"></a>第二阶段:安全事件响应与数字取证</h2><h3 id="网络安全事件响应"><a href="#网络安全事件响应" class="headerlink" title="网络安全事件响应"></a>网络安全事件响应</h3><ul><li>操作系统日志分析</li><li>应用系统&#x2F;中间件日志分析</li><li>系统进程分析</li><li>系统安全漏洞及加固</li></ul><h3 id="数字取证调查"><a href="#数字取证调查" class="headerlink" title="数字取证调查"></a>数字取证调查</h3><ul><li>内存镜像分析</li><li>编码转换、加解密、数据隐写</li><li>文件分析取证</li><li>网络流量包分析</li></ul><h3 id="应用程序安全"><a href="#应用程序安全" class="headerlink" title="应用程序安全"></a>应用程序安全</h3><ul><li>程序逆向分析</li><li>移动应用程序代码分析</li><li>恶意脚本代码分析</li></ul><h2 id="第三阶段-夺旗挑战-CTF-网络安全渗透"><a href="#第三阶段-夺旗挑战-CTF-网络安全渗透" class="headerlink" title="第三阶段:夺旗挑战 CTF(网络安全渗透)"></a>第三阶段:夺旗挑战 CTF(网络安全渗透)</h2><p>针对预设靶机环境进行渗透测试,考点包括:</p><ul><li>SQL 注入</li><li>文件上传</li><li>命令执行</li><li>缓冲区溢出&#x2F;栈溢出</li><li>信息收集</li><li>逆向文件分析</li><li>二进制漏洞利用</li><li>应用服务漏洞利用</li><li>操作系统漏洞利用</li><li>密码学分析</li></ul><h3 id="理论技能与职业素养"><a href="#理论技能与职业素养" class="headerlink" title="理论技能与职业素养"></a>理论技能与职业素养</h3><p>信息安全与网络基础、操作系统安全、网络协议安全、网络设备安全、网络数据安全、程序代码安全、网络安全渗透、安全运维与应急服务、密码技术、网络安全法律法规和职业素养。</p><h2 id="竞赛评分-成绩产生办法"><a href="#竞赛评分-成绩产生办法" class="headerlink" title="竞赛评分(成绩产生办法)"></a>竞赛评分(成绩产生办法)</h2><p>各模块由分组裁判背对背评分,统计组核对每小题得分并汇总。评分注重:</p><table><thead><tr><th>阶段</th><th>任务</th><th>考核内容</th><th>权重</th><th>评分方式</th></tr></thead><tbody><tr><td>第一阶段 30%</td><td>网络平台搭建(5%)</td><td>防火墙、网络日志系统、WAF、无线控制器、三层交换机物理连接&#x2F;命名&#x2F;IP 等</td><td>5%</td><td>结果评分-客观</td></tr><tr><td></td><td>网络安全设备配置与防护(25%)</td><td>防火墙路由&#x2F;安全策略&#x2F;NAT&#x2F;VPN;网络日志检测统计告警;WAF 防护过滤策略;无线网络与安全策略;三层交换机路由与二层安全</td><td>25%</td><td>结果评分-客观</td></tr><tr><td>第二阶段 30%</td><td>事件响应、取证、应用安全(30%)</td><td>日志分析、漏洞分析、进程分析、内存分析、加固、逆向、编码加解密、隐写、文件取证、流量分析、移动应用分析、代码审计</td><td>30%</td><td>结果评分-客观</td></tr><tr><td>第三阶段 40%</td><td>CTF 渗透(25%)</td><td>SQL 注入、文件上传、命令执行、栈&#x2F;缓冲区溢出等漏洞利用靶机;信息收集、逆向、二进制&#x2F;应用&#x2F;系统漏洞利用、密码学分析</td><td>25%</td><td>结果评分-客观</td></tr><tr><td></td><td>理论与职业素养(15%)</td><td>网络与信息安全理论知识、职业素养、团队协作、文档规范性</td><td>15%</td><td>结果评分-客观</td></tr></tbody></table><blockquote><p>比赛中禁止攻击裁判服务器和网络连接设备,经 WAF&#x2F;网络设备告警发现攻击行为将按扰乱赛场秩序处理,取消成绩。</p></blockquote><h2 id="奖项设定"><a href="#奖项设定" class="headerlink" title="奖项设定"></a>奖项设定</h2><p>以实际参赛队总数为基础:一等奖 10%、二等奖 20%、三等奖 30%(四舍五入)。</p><h2 id="技术平台与软件清单"><a href="#技术平台与软件清单" class="headerlink" title="技术平台与软件清单"></a>技术平台与软件清单</h2><p>赛项执委会统一提供操作环境(Windows),并安装以下关键软件:</p><h3 id="系统与虚拟化"><a href="#系统与虚拟化" class="headerlink" title="系统与虚拟化"></a>系统与虚拟化</h3><ul><li>Windows 10 及以上、Windows Server DataCenter 2016+</li><li>VMware Workstation 12+、VirtualBox 6.1.28</li><li>Linux CentOS 7.6.1810、Ubuntu 20.04</li></ul><h3 id="渗透与取证工具"><a href="#渗透与取证工具" class="headerlink" title="渗透与取证工具"></a>渗透与取证工具</h3><ul><li><strong>Kali</strong> 2021.3、Parrot-security 4.11.2</li><li>Wireshark 3.4.9</li><li>IDA Free 7.0、OllyDbg 1.10+、WinDbg、gdb-pwndbg</li><li>Volatility 2.6+、Autopsy 4.0+</li><li>StegSolve 1.4、audacity 3.1.0、HxD Hex Editor、EmEditor</li><li>apktool 2.6.1、jadx-gui 1.2.0、Android Studio、frida 15.1.10</li><li>sagemath 9.2、pwntools 4.5.0、pycryptodome 3.14.1、one_gadget 1.7.4</li><li>CaptfEncoder 2.1.0、BeautifulSoup4、Neo-reGeorg、Frp</li></ul><h3 id="竞赛设备清单-参考型号"><a href="#竞赛设备清单-参考型号" class="headerlink" title="竞赛设备清单(参考型号)"></a>竞赛设备清单(参考型号)</h3><table><thead><tr><th>设备</th><th>参考型号</th></tr></thead><tbody><tr><td>三层虚拟化交换机</td><td>神州数码 CS6200</td></tr><tr><td>防火墙</td><td>神州数码 DCFW-1800E-N3002-Pro</td></tr><tr><td>服务器</td><td>神州数码 DCST-6000B-Pro</td></tr><tr><td>WEB 应用防火墙</td><td>神州数码 DCFW-1800-WAF-P</td></tr><tr><td>网络日志系统</td><td>神州数码 DCBC-NetLog</td></tr><tr><td>无线交换机</td><td>神州数码 DCWS-6028-Pro</td></tr><tr><td>无线接入点</td><td>神州数码 WL8200-I2</td></tr></tbody></table><h2 id="涉及国家标准"><a href="#涉及国家标准" class="headerlink" title="涉及国家标准"></a>涉及国家标准</h2><ol><li>WSC2022_WSO554_Cyber_Security《世界技能大赛网络安全项目职业标准》</li><li>4-04-04-02《网络与信息安全管理员》</li><li>4-04-04-04《信息安全测试员》</li><li>GB&#x2F;T 22239-2019《信息安全技术 网络安全等级保护基本要求》</li><li>GB&#x2F;T 28448-2019《信息安全技术 网络安全等级保护测评要求》</li><li>GB&#x2F;T 36627-2018《信息安全技术 网络安全等级保护测试评估技术指南》</li><li>GB&#x2F;T 31509-2015《信息安全技术 信息安全风险评估实施指南》</li><li>ISO17799《信息安全管理实施细则》</li><li>ISO&#x2F;IEC 27001《信息安全管理体系》</li></ol><h2 id="竞赛流程时间表-参考"><a href="#竞赛流程时间表-参考" class="headerlink" title="竞赛流程时间表(参考)"></a>竞赛流程时间表(参考)</h2><table><thead><tr><th>日期</th><th>时间</th><th>事项</th></tr></thead><tbody><tr><td>第一天(报到)</td><td>15:00-15:30</td><td>参赛队报到,领取资料</td></tr><tr><td></td><td>15:30-15:50</td><td>领队会</td></tr><tr><td></td><td>15:50-16:20</td><td>参观赛场</td></tr><tr><td></td><td>16:20</td><td>检查封闭赛场</td></tr><tr><td>第二天(比赛)</td><td>08:00</td><td>裁判进入裁判室</td></tr><tr><td></td><td>08:40-09:00</td><td>选手就位,宣读纪律,赛题发放</td></tr><tr><td></td><td>09:00-12:00</td><td>第一、二、三阶段比赛</td></tr><tr><td></td><td>12:00-12:30</td><td>结果提交</td></tr><tr><td></td><td>12:35</td><td>比赛结束</td></tr></tbody></table><h2 id="备考建议"><a href="#备考建议" class="headerlink" title="备考建议"></a>备考建议</h2><ol><li><strong>第一阶段(网络平台搭建)</strong>:重点练神州数码系列设备(交换机&#x2F;防火墙&#x2F;WAF&#x2F;无线)的配置。掌握 VLAN&#x2F;STP&#x2F;OSPF&#x2F;BGP、防火墙安全策略&#x2F;NAT&#x2F;VPN(IPSec、SSL VPN)、WAF 防护策略、无线控制器配置。</li><li><strong>第二阶段(取证与应急响应)</strong>:熟练使用 Volatility 做内存取证、Wireshark 做流量分析、Autopsy 做文件取证;掌握日志分析、进程分析、漏洞加固、编码转换与隐写、程序逆向(IDA&#x2F;OllyDbg)。</li><li><strong>第三阶段(CTF 渗透)</strong>:这是分值最高(40%)的部分,务必重点突破。刷 SQL 注入、文件上传、命令执行、栈溢出等 Web&#x2F;Pwn 题型;可参考本博客 CTF 学习资料一文。</li><li><strong>理论部分</strong>:系统复习网络安全等级保护(等保 2.0)、密码技术、网络安全法律法规,不要丢这 15% 的分。</li><li><strong>设备熟悉</strong>:提前了解神州数码设备的命令体系,避免赛场不熟悉命令行。</li><li><strong>文档规范</strong>:提交的竞赛文档不得标识任何地名&#x2F;校名&#x2F;姓名&#x2F;参赛编号,否则取消成绩。</li></ol><h2 id="资料来源"><a href="#资料来源" class="headerlink" title="资料来源"></a>资料来源</h2><ul><li><a href="https://blog.csdn.net/qq_22406677/article/details/144461297">第十八届赛项规程(CSDN 转载)</a></li><li>山西省职业院校技能大赛官方通知(以当年省教育厅发布为准)<ul><li>各届赛项规程内容结构基本一致,以当年正式发布的规程文件为最终依据。</li></ul></li></ul>]]>
    </content>
    <id>https://www.tenxiaodao.top/2026/09/19/26-9-19infosec-competition/</id>
    <link href="https://www.tenxiaodao.top/2026/09/19/26-9-19infosec-competition/"/>
    <published>2026-09-19T15:30:00.000Z</published>
    <summary>
      <![CDATA[<p>本文整理山西省职业院校技能大赛(高职组)”信息安全管理与评估”赛项的规程要点、考核内容与备考方向,供参赛备赛参考。资料来源为赛项官方规程文件(第十八届,2024年12月发布)。</p>]]>
    </summary>
    <title>信息安全管理与评估 - 省赛备考资料</title>
    <updated>2026-09-19T15:28:56.360Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="安全" scheme="https://www.tenxiaodao.top/categories/%E5%AE%89%E5%85%A8/"/>
    <category term="CTF" scheme="https://www.tenxiaodao.top/tags/CTF/"/>
    <category term="安全" scheme="https://www.tenxiaodao.top/tags/%E5%AE%89%E5%85%A8/"/>
    <category term="学习笔记" scheme="https://www.tenxiaodao.top/tags/%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    <content>
      <![CDATA[<p>CTF(Capture The Flag,夺旗赛)是一种网络安全竞赛形式,参赛者通过分析、破解各类安全题目获取 flag(一串特定格式的字符串)来得分。本文整理 CTF 各方向的入门资料、练习平台、常用工具与 Writeup 来源,方便随时查阅。</p><span id="more"></span><h2 id="主要方向"><a href="#主要方向" class="headerlink" title="主要方向"></a>主要方向</h2><p>CTF 题目通常按技术方向分类,常见的有以下几类:</p><ul><li><strong>Web</strong>:Web 安全。涉及 SQL 注入、XSS、SSRF、文件上传、文件包含、反序列化、SSTI 模板注入、命令注入等。</li><li><strong>Pwn</strong>:二进制漏洞利用。涉及栈溢出、堆利用(UAF、Double Free、Tcache 攻击等)、格式化字符串漏洞、ROP 等。</li><li><strong>Reverse</strong>:逆向工程。分析二进制程序逻辑还原算法,涉及反汇编、反编译、脱壳、反混淆等。</li><li><strong>Crypto</strong>:密码学。涉及 RSA、AES、DES、古典密码、椭圆曲线、哈希碰撞等。</li><li><strong>Misc</strong>:杂项。涉及隐写术、流量分析、数字取证、内存取证、编码转换、脑洞题等。</li><li><strong>其他方向</strong>:Mobile(安卓逆向)、IoT(固件分析)、区块链智能合约安全等。</li></ul><h2 id="知识库与教程"><a href="#知识库与教程" class="headerlink" title="知识库与教程"></a>知识库与教程</h2><ul><li><a href="https://ctf-wiki.org/">CTF Wiki</a>:中文最系统的 CTF 知识库,涵盖各方向基础到进阶,入门必看。</li><li><a href="https://github.com/firmianalive/ctf-wiki">CTF All In One</a>:GitHub 上的综合资料。</li><li>书籍推荐:<ul><li>《从0到1:CTFer成长之路》—— 适合入门,覆盖各方向基础。</li><li>《CTF特训营》—— 较为系统的实战训练。</li><li>《密码学原理与实践》—— Crypto 方向理论补充。</li><li>《加密与解密》(段钢)—— Reverse 方向经典。</li></ul></li></ul><h2 id="练习平台"><a href="#练习平台" class="headerlink" title="练习平台"></a>练习平台</h2><table><thead><tr><th>平台</th><th>地址</th><th>特点</th></tr></thead><tbody><tr><td>BUUCTF</td><td><a href="https://buuoj.cn/">https://buuoj.cn</a></td><td>国内最常用,题目丰富,收录大量历年赛题</td></tr><tr><td>攻防世界</td><td><a href="https://adworld.xctf.org.cn/">https://adworld.xctf.org.cn</a></td><td>XCTF 旗下,按难度分级,适合刷题</td></tr><tr><td>CTFHub</td><td><a href="https://www.ctfhub.com/">https://www.ctfhub.com</a></td><td>技能树式引导学习,适合入门</td></tr><tr><td>NSSCTF</td><td><a href="https://www.nssctf.cn/">https://www.nssctf.cn</a></td><td>国内平台,题目持续更新</td></tr><tr><td>BugKu</td><td><a href="https://ctf.bugku.com/">https://ctf.bugku.com</a></td><td>老牌平台,Misc 题较多</td></tr><tr><td>PicoCTF</td><td><a href="https://picoctf.org/">https://picoctf.org</a></td><td>卡内基梅隆出品,英文,非常适合零基础入门</td></tr><tr><td>HackTheBox</td><td><a href="https://www.hackthebox.com/">https://www.hackthebox.com</a></td><td>偏渗透测试,实战性强</td></tr><tr><td>TryHackMe</td><td><a href="https://tryhackme.com/">https://tryhackme.com</a></td><td>引导式学习路径,适合新手</td></tr><tr><td>RootMe</td><td><a href="https://www.root-me.org/">https://www.root-me.org</a></td><td>法语&#x2F;英语,题目质量高</td></tr><tr><td>CTFtime</td><td><a href="https://ctftime.org/">https://ctftime.org</a></td><td>全球赛事日历与 Writeup 聚合</td></tr></tbody></table><h2 id="常用工具"><a href="#常用工具" class="headerlink" title="常用工具"></a>常用工具</h2><h3 id="Web"><a href="#Web" class="headerlink" title="Web"></a>Web</h3><ul><li><strong>Burp Suite</strong>:抓包、重放、爆破,Web 题必备。</li><li><strong>sqlmap</strong>:自动化 SQL 注入检测与利用。</li><li><strong>dirsearch &#x2F; gobuster</strong>:目录与文件爆破。</li><li><strong>AntSword(蚁剑)&#x2F; Godzilla(哥斯拉)</strong>:WebShell 管理。</li><li><strong>wfuzz</strong>:模糊测试参数与路径。</li></ul><h3 id="Pwn"><a href="#Pwn" class="headerlink" title="Pwn"></a>Pwn</h3><ul><li><strong>pwntools</strong>:Python 库,编写 exp 脚本的核心工具。</li><li><strong>gdb + pwndbg &#x2F; gef &#x2F; peda</strong>:动态调试插件,增强 gdb。</li><li><strong>ROPgadget &#x2F; ropper</strong>:查找 ROP 链 gadget。</li><li><strong>one_gadget</strong>:查找 libc 中的单 gadget execve。</li><li><strong>LibcSearcher &#x2F; libc-database</strong>:根据泄漏地址匹配 libc 版本。</li><li><strong>checksec</strong>:查看二进制保护机制(NX、Canary、PIE 等)。</li></ul><h3 id="Reverse"><a href="#Reverse" class="headerlink" title="Reverse"></a>Reverse</h3><ul><li><strong>IDA Pro</strong>:反汇编与反编译标杆(有免费版)。</li><li><strong>Ghidra</strong>:NSA 开源的逆向套件,免费功能强。</li><li><strong>x64dbg &#x2F; x32dbg</strong>:Windows 动态调试。</li><li><strong>dnSpy &#x2F; ILSpy</strong>:.NET 程序反编译。</li><li><strong>jd-gui</strong>:Java 反编译。</li><li><strong>APKTool &#x2F; jadx &#x2F; frida</strong>:安卓逆向与 Hook。</li></ul><h3 id="Crypto"><a href="#Crypto" class="headerlink" title="Crypto"></a>Crypto</h3><ul><li><strong>SageMath</strong>:代数与数论计算利器,RSA 题常客。</li><li><strong>CyberChef</strong>:编码&#x2F;解码&#x2F;加密一站式网页工具。</li><li><strong>yafu</strong>:大数分解。</li><li><strong>RSAtool &#x2F; RsaCtfTool</strong>:RSA 攻击工具集。</li><li><strong>openssl</strong>:证书与密钥分析命令行工具。</li></ul><h3 id="Misc"><a href="#Misc" class="headerlink" title="Misc"></a>Misc</h3><ul><li><strong>010 Editor</strong>:十六进制编辑,带模板解析。</li><li><strong>StegSolve</strong>:图片隐写分析。</li><li><strong>binwalk &#x2F; foremost</strong>:文件提取与分离。</li><li><strong>Audacity</strong>:音频隐写查看频谱图。</li><li><strong>Wireshark &#x2F; tshark</strong>:流量包分析。</li><li><strong>Volatility</strong>:内存取证分析框架。</li><li><strong>Zsteg</strong>:PNG&#x2F;BMP LSB 隐写检测。</li><li><strong>ExifTool</strong>:查看图片元数据。</li></ul><h2 id="Writeup-与资讯来源"><a href="#Writeup-与资讯来源" class="headerlink" title="Writeup 与资讯来源"></a>Writeup 与资讯来源</h2><ul><li><a href="https://ctftime.org/writeups">CTFtime Writeups</a>:全球赛事题解聚合。</li><li><a href="https://xz.aliyun.com/">先知社区</a>:阿里安全的攻防技术文章。</li><li><a href="https://www.anquanke.com/">安全客</a>:安全资讯与技术文章。</li><li>GitHub:搜索各高校与战队仓库(如 <code>xxx-ctf-team/writeup</code>)。</li><li>个人博客:多关注常年打比赛的师傅们的博客。</li></ul><h2 id="学习建议"><a href="#学习建议" class="headerlink" title="学习建议"></a>学习建议</h2><ol><li>先确定一到两个感兴趣的方向深入,而非全面铺开。</li><li>从 PicoCTF &#x2F; CTFHub 技能树入门,建立基础概念。</li><li>刷 BUUCTF &#x2F; 攻防世界积累题型,遇到不会的题看 Writeup 复现。</li><li>组队参加线上比赛(如强网杯、网鼎杯、DASCTF),实战中成长最快。</li><li>养成记笔记习惯,整理自己的 exp 模板与工具清单。</li></ol>]]>
    </content>
    <id>https://www.tenxiaodao.top/2026/09/19/ctf/</id>
    <link href="https://www.tenxiaodao.top/2026/09/19/ctf/"/>
    <published>2026-09-19T15:14:35.000Z</published>
    <summary>
      <![CDATA[<p>CTF(Capture The Flag,夺旗赛)是一种网络安全竞赛形式,参赛者通过分析、破解各类安全题目获取 flag(一串特定格式的字符串)来得分。本文整理 CTF 各方向的入门资料、练习平台、常用工具与 Writeup 来源,方便随时查阅。</p>]]>
    </summary>
    <title>ctf</title>
    <updated>2026-09-19T15:18:59.122Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="AI 探索" scheme="https://www.tenxiaodao.top/categories/AI-%E6%8E%A2%E7%B4%A2/"/>
    <category term="部署" scheme="https://www.tenxiaodao.top/tags/%E9%83%A8%E7%BD%B2/"/>
    <category term="CowAgent" scheme="https://www.tenxiaodao.top/tags/CowAgent/"/>
    <category term="飞牛NAS" scheme="https://www.tenxiaodao.top/tags/%E9%A3%9E%E7%89%9BNAS/"/>
    <category term="chatgpt-on-wechat" scheme="https://www.tenxiaodao.top/tags/chatgpt-on-wechat/"/>
    <category term="局域网" scheme="https://www.tenxiaodao.top/tags/%E5%B1%80%E5%9F%9F%E7%BD%91/"/>
    <content>
      <![CDATA[<h1 id="飞牛-NAS-部署-CowAgent-并开放局域网访问"><a href="#飞牛-NAS-部署-CowAgent-并开放局域网访问" class="headerlink" title="飞牛 NAS 部署 CowAgent 并开放局域网访问"></a>飞牛 NAS 部署 CowAgent 并开放局域网访问</h1><blockquote><p>环境：飞牛 NAS（主机名 <code>fnnas</code>，普通用户 <code>FNNAS</code>，Python 3.11）<br>CowAgent 源码位于 <code>/root/CowAgent/</code>，Web 控制台端口 <code>9899</code><br>所有命令在 NAS 上以 <strong>root</strong> 运行（<code>sudo -i</code>）</p></blockquote><p>CowAgent 即 <code>chatgpt-on-wechat</code>（命令名 <code>cow</code>，包名 <code>cowagent</code>），是一个可以把 AI 接入微信&#x2F;飞书的聊天机器人框架，支持多模型后端（智谱 GLM、OpenAI 等）、技能扩展、Web 控制台等功能。飞牛 NAS 没有自带浏览器，Web 控制台默认只监听 <code>127.0.0.1</code>，本机访问不了，所以需要把监听地址改成 <code>0.0.0.0</code>，让局域网内的电脑&#x2F;手机能通过浏览器访问。</p><p>本文从零开始记录完整部署流程：获取源码、安装依赖、初始配置、用 systemd 托管服务、开放局域网访问，最后附常见坑和速查命令。</p><hr><h2 id="为什么选源码部署"><a href="#为什么选源码部署" class="headerlink" title="为什么选源码部署"></a>为什么选源码部署</h2><p>项目提供了一个 <code>docker-compose.yml</code>，但引用的预构建镜像只有 <code>amd64</code> 版本。飞牛 NAS 如果是 ARM 架构（<code>aarch64</code>），Docker 方式会触发平台不匹配警告——虽然可以通过 <code>platform: linux/amd64</code> 模拟运行，或本地 <code>docker build</code> 自建 arm64 镜像，但都比较折腾，性能也打折。</p><p>源码部署更直接：克隆代码、<code>pip install -e .</code> 装好就能用，改配置方便，后续装技能、改源码都即时生效。所以本次走的是<strong>源码 + editable 安装</strong>方式。</p><hr><h2 id="1-获取-CowAgent"><a href="#1-获取-CowAgent" class="headerlink" title="1. 获取 CowAgent"></a>1. 获取 CowAgent</h2><h3 id="1-1-前置条件检查"><a href="#1-1-前置条件检查" class="headerlink" title="1.1 前置条件检查"></a>1.1 前置条件检查</h3><p>飞牛 NAS（fnOS，Debian 系）自带 Python 3.11，先确认版本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">python3 --version</span><br><span class="line"><span class="comment"># 期望输出 Python 3.11.x</span></span><br></pre></td></tr></table></figure><p>确认 git 可用（fnOS 默认带）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git --version</span><br><span class="line"><span class="comment"># 期望输出 git version 2.x.x</span></span><br></pre></td></tr></table></figure><p>确认 pip 可用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">python3 -m pip --version</span><br></pre></td></tr></table></figure><p>如果 pip 不存在，用系统包管理器补上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">apt update &amp;&amp; apt install -y python3-pip</span><br></pre></td></tr></table></figure><p>如果 <code>python3 -m venv</code> 报错缺模块，装一下 <code>venv</code> 包：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">apt install -y python3-venv</span><br></pre></td></tr></table></figure><h3 id="1-2-克隆源码"><a href="#1-2-克隆源码" class="headerlink" title="1.2 克隆源码"></a>1.2 克隆源码</h3><p>切到 root，克隆到 <code>/root/CowAgent/</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> -i</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/zhayujie/chatgpt-on-wechat.git /root/CowAgent</span><br><span class="line"><span class="built_in">cd</span> /root/CowAgent</span><br></pre></td></tr></table></figure><blockquote><p>如果你的 CowAgent 是定制 fork，换成对应的仓库地址即可。关键是源码最终在 <code>/root/CowAgent/</code> 这个路径下。</p></blockquote><h3 id="1-3-创建虚拟环境并安装"><a href="#1-3-创建虚拟环境并安装" class="headerlink" title="1.3 创建虚拟环境并安装"></a>1.3 创建虚拟环境并安装</h3><p>建议建一个独立 venv，避免污染系统 Python：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">python3 -m venv /root/CowAgent/venv</span><br><span class="line"><span class="built_in">source</span> /root/CowAgent/venv/bin/activate</span><br><span class="line">pip install -U pip</span><br></pre></td></tr></table></figure><p>editable 安装（<code>-e</code> 参数），这样改源码即时生效，<code>cow</code> 命令也能直接用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pip install -e .</span><br></pre></td></tr></table></figure><p>这一步会安装依赖（包括 <code>itchat</code>、<code>requests</code>、<code>flask</code> 等），耗时取决于网络，一般几分钟。</p><h3 id="1-4-验证安装"><a href="#1-4-验证安装" class="headerlink" title="1.4 验证安装"></a>1.4 验证安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cow --version</span><br></pre></td></tr></table></figure><p>看到版本号输出即安装成功。也可以用 Python 方式验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">python3 -c <span class="string">&quot;import cowagent; print(&#x27;ok&#x27;)&quot;</span></span><br></pre></td></tr></table></figure><hr><h2 id="2-初始配置"><a href="#2-初始配置" class="headerlink" title="2. 初始配置"></a>2. 初始配置</h2><h3 id="2-1-创建配置文件"><a href="#2-1-创建配置文件" class="headerlink" title="2.1 创建配置文件"></a>2.1 创建配置文件</h3><p>CowAgent 的配置文件是 <code>config.json</code>，首次使用需要从模板复制一份：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> /root/CowAgent</span><br><span class="line"><span class="built_in">cp</span> config-template.json config.json</span><br></pre></td></tr></table></figure><blockquote><p>如果仓库里没有 <code>config-template.json</code>，直接 <code>vi config.json</code> 手动创建，参考下面的完整示例。</p></blockquote><h3 id="2-2-关键字段说明"><a href="#2-2-关键字段说明" class="headerlink" title="2.2 关键字段说明"></a>2.2 关键字段说明</h3><table><thead><tr><th>字段</th><th>作用</th><th>示例值</th></tr></thead><tbody><tr><td><code>channel_type</code></td><td>渠道类型，<code>web</code> 表示 Web 控制台</td><td><code>&quot;web&quot;</code></td></tr><tr><td><code>cow_lang</code></td><td>语言</td><td><code>&quot;zh&quot;</code></td></tr><tr><td><code>web_port</code></td><td>Web 控制台端口</td><td><code>9899</code></td></tr><tr><td><code>web_host</code></td><td>Web 监听地址，默认无（即 <code>127.0.0.1</code>）</td><td><code>&quot;0.0.0.0&quot;</code></td></tr><tr><td><code>web_password</code></td><td>Web 控制台登录密码</td><td>自定义</td></tr><tr><td><code>bot_type</code></td><td>AI 后端类型（<code>zhipu</code> &#x2F; <code>openai</code>）</td><td><code>&quot;zhipu&quot;</code></td></tr><tr><td><code>model</code></td><td>模型名</td><td><code>&quot;glm-5-turbo&quot;</code></td></tr><tr><td><code>zhipu_ai_api_key</code></td><td>智谱 API key</td><td>从 <a href="https://open.bigmodel.cn/">open.bigmodel.cn</a> 获取</td></tr><tr><td><code>zhipu_ai_api_base</code></td><td>智谱 API 地址</td><td><code>&quot;https://open.bigmodel.cn/api/paas/v4&quot;</code></td></tr><tr><td><code>agent_max_context_tokens</code></td><td>上下文 token 上限</td><td><code>50000</code></td></tr></tbody></table><blockquote><p><code>web_host</code> 和 <code>web_password</code> 在模板里默认没有，需要手动加（第 3 节专门讲）。</p></blockquote><h3 id="2-3-最小可用配置示例"><a href="#2-3-最小可用配置示例" class="headerlink" title="2.3 最小可用配置示例"></a>2.3 最小可用配置示例</h3><p>以下是接通智谱 GLM 并开放 Web 控制台的最小配置（省略了不相关字段）：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;channel_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;web&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;cow_lang&quot;</span><span class="punctuation">:</span> <span class="string">&quot;zh&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;web_port&quot;</span><span class="punctuation">:</span> <span class="number">9899</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;web_host&quot;</span><span class="punctuation">:</span> <span class="string">&quot;0.0.0.0&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;web_password&quot;</span><span class="punctuation">:</span> <span class="string">&quot;你的登录密码&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;bot_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;zhipu&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;model&quot;</span><span class="punctuation">:</span> <span class="string">&quot;glm-5-turbo&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;zhipu_ai_api_key&quot;</span><span class="punctuation">:</span> <span class="string">&quot;你的智谱key&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;zhipu_ai_api_base&quot;</span><span class="punctuation">:</span> <span class="string">&quot;https://open.bigmodel.cn/api/paas/v4&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;agent_max_context_tokens&quot;</span><span class="punctuation">:</span> <span class="number">50000</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><blockquote><p>如果用 OpenAI 兼容接口，把 <code>bot_type</code> 改成 <code>&quot;openai&quot;</code>，用 <code>open_ai_api_key</code> &#x2F; <code>open_ai_api_base</code> 替代智谱相关字段，<code>model</code> 填对应模型名。</p></blockquote><h3 id="2-4-填入-API-key"><a href="#2-4-填入-API-key" class="headerlink" title="2.4 填入 API key"></a>2.4 填入 API key</h3><p>拿到智谱 key 后，用 sed 写入（注意替换 <code>你的智谱key</code>）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sed -i <span class="string">&#x27;s/&quot;zhipu_ai_api_key&quot;: &quot;YOUR_KEY&quot;/&quot;zhipu_ai_api_key&quot;: &quot;你的智谱key&quot;/&#x27;</span> /root/CowAgent/config.json</span><br></pre></td></tr></table></figure><p>或者直接 <code>vi</code> 编辑，找到 <code>zhipu_ai_api_key</code> 那行改掉。验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep zhipu_ai_api_key /root/CowAgent/config.json</span><br></pre></td></tr></table></figure><hr><h2 id="3-开放局域网访问"><a href="#3-开放局域网访问" class="headerlink" title="3. 开放局域网访问"></a>3. 开放局域网访问</h2><p>默认配置<strong>没有</strong> <code>web_host</code> 字段，因此监听 <code>127.0.0.1</code>，只能本机访问。需要在 <code>config.json</code> 里加两个字段：</p><ul><li><code>web_host</code>: <code>0.0.0.0</code> —— 监听所有网卡</li><li><code>web_password</code>: 自己设一个登录密码</li></ul><h3 id="推荐做法：用-sed-一行插入"><a href="#推荐做法：用-sed-一行插入" class="headerlink" title="推荐做法：用 sed 一行插入"></a>推荐做法：用 sed 一行插入</h3><p>在 <code>&quot;web_port&quot;: 9899</code> 前面插入 <code>web_host</code> 和 <code>web_password</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> sed -i <span class="string">&#x27;s/&quot;web_port&quot;: 9899/&quot;web_host&quot;: &quot;0.0.0.0&quot;,&quot;web_password&quot;: &quot;你的密码&quot;,&quot;web_port&quot;: 9899/&#x27;</span> /root/CowAgent/config.json</span><br></pre></td></tr></table></figure><blockquote><p>终端自动换行会把长命令断成两行导致报错。如果复制粘贴失败，改用反斜杠续行，或直接用 <code>vi</code> 手动编辑。</p></blockquote><p>验证是否改成功：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep web_host /root/CowAgent/config.json</span><br></pre></td></tr></table></figure><p>看到 <code>&quot;web_host&quot;: &quot;0.0.0.0&quot;</code> 即成功。</p><h3 id="替代做法：vi-手动编辑"><a href="#替代做法：vi-手动编辑" class="headerlink" title="替代做法：vi 手动编辑"></a>替代做法：vi 手动编辑</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vi /root/CowAgent/config.json</span><br></pre></td></tr></table></figure><ol><li>找到 <code>&quot;web_port&quot;: 9899</code> 那一行</li><li>按 <code>i</code> 进入编辑模式</li><li>在 <code>&quot;web_port&quot;</code> 前面加上 <code>&quot;web_host&quot;: &quot;0.0.0.0&quot;, &quot;web_password&quot;: &quot;你的密码&quot;,</code></li><li>按 <code>Esc</code>，输入 <code>:wq</code> 回车保存</li></ol><h3 id="飞牛防火墙放行"><a href="#飞牛防火墙放行" class="headerlink" title="飞牛防火墙放行"></a>飞牛防火墙放行</h3><p>如果上一步做完仍然从局域网访问不了，检查 fnOS 防火墙是否放行了 <code>9899</code> 端口：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看当前规则</span></span><br><span class="line">iptables -L -n | grep 9899</span><br><span class="line"></span><br><span class="line"><span class="comment"># 没有的话手动放行</span></span><br><span class="line">iptables -I INPUT -p tcp --dport 9899 -j ACCEPT</span><br></pre></td></tr></table></figure><blockquote><p>fnOS 大部分版本默认不拦局域网端口，这一步通常不需要。但如果你在飞牛 Docker 网络或虚拟接口环境下部署，可能需要手动放行。</p></blockquote><hr><h2 id="4-配置-systemd-托管"><a href="#4-配置-systemd-托管" class="headerlink" title="4. 配置 systemd 托管"></a>4. 配置 systemd 托管</h2><p>CowAgent 需要长期运行，最好交给 systemd 管理——开机自启、崩溃自动重启、日志统一收集。</p><h3 id="4-1-创建服务文件"><a href="#4-1-创建服务文件" class="headerlink" title="4.1 创建服务文件"></a>4.1 创建服务文件</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> &gt; /etc/systemd/system/cowagent.service &lt;&lt; <span class="string">&#x27;EOF&#x27;</span></span><br><span class="line">[Unit]</span><br><span class="line">Description=CowAgent (chatgpt-on-wechat)</span><br><span class="line">After=network-online.target</span><br><span class="line">Wants=network-online.target</span><br><span class="line"></span><br><span class="line">[Service]</span><br><span class="line">Type=simple</span><br><span class="line">WorkingDirectory=/root/CowAgent</span><br><span class="line">ExecStart=/root/CowAgent/venv/bin/python3 app.py</span><br><span class="line">Restart=always</span><br><span class="line">RestartSec=10</span><br><span class="line">StandardOutput=append:/var/log/cowagent.log</span><br><span class="line">StandardError=append:/var/log/cowagent.log</span><br><span class="line"></span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure><p>几个关键点：</p><ul><li><code>WorkingDirectory</code> 设为 <code>/root/CowAgent</code>，确保 <code>config.json</code> 和源码能被找到</li><li><code>ExecStart</code> 用 venv 里的 Python，而不是系统 Python，确保依赖正确加载</li><li><code>Restart=always</code> + <code>RestartSec=10</code>：进程崩溃后 10 秒自动拉起，避免重启风暴</li><li>日志统一写到 <code>/var/log/cowagent.log</code></li></ul><h3 id="4-2-启动并设置开机自启"><a href="#4-2-启动并设置开机自启" class="headerlink" title="4.2 启动并设置开机自启"></a>4.2 启动并设置开机自启</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">systemctl daemon-reload</span><br><span class="line">systemctl <span class="built_in">enable</span> --now cowagent</span><br><span class="line">systemctl status cowagent --no-pager</span><br></pre></td></tr></table></figure><p>看到 <code>active (running)</code> 即成功。确认日志正常输出：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">tail</span> -f /var/log/cowagent.log</span><br></pre></td></tr></table></figure><p>看到 Web 控制台启动、监听 <code>0.0.0.0:9899</code> 的日志行就对了。</p><h3 id="4-3-日常管理命令"><a href="#4-3-日常管理命令" class="headerlink" title="4.3 日常管理命令"></a>4.3 日常管理命令</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看状态</span></span><br><span class="line">systemctl status cowagent</span><br><span class="line"></span><br><span class="line"><span class="comment"># 重启（改完 config.json 后必须执行）</span></span><br><span class="line">systemctl restart cowagent</span><br><span class="line"></span><br><span class="line"><span class="comment"># 停止</span></span><br><span class="line">systemctl stop cowagent</span><br><span class="line"></span><br><span class="line"><span class="comment"># 看实时日志</span></span><br><span class="line"><span class="built_in">tail</span> -f /var/log/cowagent.log</span><br></pre></td></tr></table></figure><blockquote><p>如果暂时不想用 systemd，也可以用 <code>screen</code> 或 <code>nohup</code> 后台运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># screen 方式</span></span><br><span class="line">screen -S cowagent</span><br><span class="line"><span class="built_in">cd</span> /root/CowAgent &amp;&amp; <span class="built_in">source</span> venv/bin/activate &amp;&amp; python3 app.py</span><br><span class="line"><span class="comment"># 按 Ctrl+A 再按 D 脱离会话</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># nohup 方式</span></span><br><span class="line"><span class="built_in">nohup</span> /root/CowAgent/venv/bin/python3 app.py &gt; /var/log/cowagent.log 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure><p>这两种方式都不支持开机自启和崩溃重启，长期运行还是推荐 systemd。</p></blockquote><hr><h2 id="5-从电脑访问"><a href="#5-从电脑访问" class="headerlink" title="5. 从电脑访问"></a>5. 从电脑访问</h2><p>先查 NAS 的局域网 IP：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hostname -I</span><br></pre></td></tr></table></figure><p>然后在电脑&#x2F;手机浏览器打开：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http://NAS的IP:9899</span><br></pre></td></tr></table></figure><p>例如 NAS IP 是 <code>192.168.1.100</code>，就访问 <code>http://192.168.1.100:9899</code>，输入第 3 步设的 <code>web_password</code> 登录。</p><p>登录后就能在 Web 控制台里直接跟 AI 对话，也可以从这里安装&#x2F;管理技能（<code>cow skill install ...</code>）。</p><blockquote><p>如果页面打不开，依次排查：</p><ol><li><code>systemctl status cowagent</code> 确认服务在跑</li><li><code>grep web_host /root/CowAgent/config.json</code> 确认是 <code>0.0.0.0</code></li><li><code>hostname -I</code> 确认 IP 没错</li><li>浏览器和 NAS 在同一局域网（同网段）</li></ol></blockquote><hr><h2 id="常见坑"><a href="#常见坑" class="headerlink" title="常见坑"></a>常见坑</h2><table><thead><tr><th>现象</th><th>原因</th><th>解决</th></tr></thead><tbody><tr><td><code>python3 -c &quot;...&quot;</code> 报 <code>IndentationError</code></td><td>终端自动换行把命令截断、加了缩进</td><td>改用 <code>sed</code> 或 <code>vi</code></td></tr><tr><td>heredoc <code>cat &gt; /tmp/fix.py</code> 卡住</td><td>进了输入模式</td><td><code>Ctrl+C</code> 退出，改用 <code>sed</code></td></tr><tr><td><code>sed: no input files</code> &#x2F; <code>Permission denied</code></td><td>命令被换行断成两行</td><td>用 <code>sudo</code> + 反斜杠续行，或 <code>cd /root/CowAgent</code> 后对 <code>config.json</code> 操作</td></tr><tr><td><code>cow skill install ...</code> 报 <code>Permission denied</code> 访问 <code>/root/CowAgent/cli/__init__.py</code></td><td><code>cow</code> 是 editable 安装，去 <code>/root/CowAgent/</code> 找源码；普通用户 <code>FNNAS</code> 没权限读 <code>/root</code></td><td>用 <code>sudo -i</code> 切到 root 再运行 <code>cow</code></td></tr><tr><td>Web 控制台打不开</td><td><code>web_host</code> 没改成 <code>0.0.0.0</code>，或 systemd 服务没启动</td><td><code>grep web_host config.json</code> 确认配置，<code>systemctl status cowagent</code> 确认服务在跑</td></tr><tr><td><code>pip install -e .</code> 报权限错误</td><td>没用 venv，直接装到了系统 Python</td><td>确保先 <code>source venv/bin/activate</code></td></tr><tr><td>浏览器能打开但提示密码错误</td><td><code>web_password</code> 没设，或设了但没重启服务</td><td><code>systemctl restart cowagent</code></td></tr><tr><td>飞牛 NAS 重启后服务没自动起来</td><td><code>systemctl enable</code> 没执行</td><td><code>systemctl enable cowagent</code></td></tr><tr><td>页面能打开但 AI 不回复</td><td>API key 没填对，或 <code>bot_type</code> &#x2F; <code>model</code> 配错</td><td><code>grep -E &#39;bot_type|model|api_key&#39; config.json</code> 检查，对照第 2 节示例</td></tr><tr><td>日志报 <code>Address already in use</code></td><td>9899 端口被占</td><td><code>lsof -i:9899</code> 找到占用进程，<code>kill</code> 掉或改 <code>web_port</code></td></tr></tbody></table><hr><h2 id="常用命令速查"><a href="#常用命令速查" class="headerlink" title="常用命令速查"></a>常用命令速查</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 切到 root（CowAgent 在 /root 下，需要 root 权限）</span></span><br><span class="line"><span class="built_in">sudo</span> -i</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看配置</span></span><br><span class="line"><span class="built_in">cat</span> /root/CowAgent/config.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 改监听地址</span></span><br><span class="line"><span class="built_in">sudo</span> sed -i <span class="string">&#x27;s/&quot;web_port&quot;: 9899/&quot;web_host&quot;: &quot;0.0.0.0&quot;,&quot;web_password&quot;: &quot;你的密码&quot;,&quot;web_port&quot;: 9899/&#x27;</span> /root/CowAgent/config.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证</span></span><br><span class="line">grep web_host /root/CowAgent/config.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查 NAS 局域网 IP</span></span><br><span class="line">hostname -I</span><br><span class="line"></span><br><span class="line"><span class="comment"># 重启服务（改完配置后执行）</span></span><br><span class="line">systemctl restart cowagent</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看服务状态</span></span><br><span class="line">systemctl status cowagent</span><br><span class="line"></span><br><span class="line"><span class="comment"># 看实时日志</span></span><br><span class="line"><span class="built_in">tail</span> -f /var/log/cowagent.log</span><br></pre></td></tr></table></figure><hr><h2 id="文件清单"><a href="#文件清单" class="headerlink" title="文件清单"></a>文件清单</h2><table><thead><tr><th>路径</th><th>作用</th></tr></thead><tbody><tr><td><code>/root/CowAgent/</code></td><td>CowAgent 源码目录</td></tr><tr><td><code>/root/CowAgent/venv/</code></td><td>Python 虚拟环境（隔离依赖）</td></tr><tr><td><code>/root/CowAgent/config.json</code></td><td>主配置文件（端口、监听地址、API key 等）</td></tr><tr><td><code>/root/CowAgent/config-template.json</code></td><td>配置模板（初始复制用）</td></tr><tr><td><code>/etc/systemd/system/cowagent.service</code></td><td>systemd 服务文件</td></tr><tr><td><code>/var/log/cowagent.log</code></td><td>运行日志</td></tr></tbody></table>]]>
    </content>
    <id>https://www.tenxiaodao.top/cowagent-fnnas-deploy/</id>
    <link href="https://www.tenxiaodao.top/cowagent-fnnas-deploy/"/>
    <published>2026-07-12T07:00:00.000Z</published>
    <summary>本文记录在飞牛 NAS 上通过源码部署 CowAgent（chatgpt-on-wechat）并开放 Web 控制台局域网访问的完整流程，含源码安装、初始配置、systemd 托管、常见坑与速查命令。</summary>
    <title>飞牛 NAS 部署 CowAgent 并开放局域网访问</title>
    <updated>2026-08-25T17:51:20.254Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="AI 探索" scheme="https://www.tenxiaodao.top/categories/AI-%E6%8E%A2%E7%B4%A2/"/>
    <category term="CowAgent" scheme="https://www.tenxiaodao.top/tags/CowAgent/"/>
    <category term="飞牛NAS" scheme="https://www.tenxiaodao.top/tags/%E9%A3%9E%E7%89%9BNAS/"/>
    <category term="Headroom" scheme="https://www.tenxiaodao.top/tags/Headroom/"/>
    <category term="GLM" scheme="https://www.tenxiaodao.top/tags/GLM/"/>
    <category term="智谱" scheme="https://www.tenxiaodao.top/tags/%E6%99%BA%E8%B0%B1/"/>
    <category term="压缩代理" scheme="https://www.tenxiaodao.top/tags/%E5%8E%8B%E7%BC%A9%E4%BB%A3%E7%90%86/"/>
    <category term="systemd" scheme="https://www.tenxiaodao.top/tags/systemd/"/>
    <content>
      <![CDATA[<h1 id="飞牛-NAS-部署-Headroom-压缩代理：给-CowAgent-省-Token-的完整指南"><a href="#飞牛-NAS-部署-Headroom-压缩代理：给-CowAgent-省-Token-的完整指南" class="headerlink" title="飞牛 NAS 部署 Headroom 压缩代理：给 CowAgent 省 Token 的完整指南"></a>飞牛 NAS 部署 Headroom 压缩代理：给 CowAgent 省 Token 的完整指南</h1><blockquote><p>环境：飞牛 NAS（主机名 <code>fnnas</code>，<code>aarch64</code>，Python 3.11.2，无 uv）<br>CowAgent 源码位于 <code>/root/CowAgent/</code>，当前 <code>bot_type: zhipu</code>，模型 <code>glm-5-turbo</code>，智谱 key 已配置<br>所有命令在 NAS 上以 <strong>root</strong> 运行（<code>sudo -i</code>）</p></blockquote><h2 id="链路"><a href="#链路" class="headerlink" title="链路"></a>链路</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CowAgent (zhipu/OpenAI 格式) → headroom (:8787/v1, 压缩上下文) → https://open.bigmodel.cn/api/paas/v4 → GLM</span><br></pre></td></tr></table></figure><p>Headroom 是一个透明代理，CowAgent 发的智谱 key 会原样透传到上游，<strong>不用装 cc-switch</strong>。CowAgent 的 <code>bot_type</code>、<code>model</code>、<code>zhipu_ai_api_key</code> 都不动，只改 <code>zhipu_ai_api_base</code> 指向 headroom。</p><blockquote><p>如果你还没部署 CowAgent，先看 <a href="/cowagent-fnnas-deploy/">飞牛 NAS 部署 CowAgent 并开放局域网访问</a>，本文假设 CowAgent 已经跑起来并且能用智谱 GLM 正常回复。</p></blockquote><h2 id="先知道这些"><a href="#先知道这些" class="headerlink" title="先知道这些"></a>先知道这些</h2><ol><li><strong>安装包很大</strong>：<code>headroom-ai</code> 会拖 torch &#x2F; transformers &#x2F; opencv &#x2F; scipy &#x2F; sentence-transformers &#x2F; pandas 等一堆 ML 依赖，aarch64 上约 <strong>3–5 GB</strong>，装十几分钟正常。若某个包没有 aarch64 wheel 会尝试源码编译（可能失败，届时单独处理）。</li><li>**压缩可能让机器人”忘事”**：headroom <code>token</code> 模式会改写&#x2F;压缩历史轮次以省 token。CowAgent 自己已截断到 <code>agent_max_context_tokens: 50000</code> &#x2F; 20 轮，再压一层，长对话里可能丢细节。微信&#x2F;飞书聊天场景要留意体验。</li><li><strong>会改 CowAgent 配置</strong>：先备份 <code>config.json</code>，出问题能秒回滚。</li></ol><hr><h2 id="1-装-headroom"><a href="#1-装-headroom" class="headerlink" title="1. 装 headroom"></a>1. 装 headroom</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 建 venv</span></span><br><span class="line">python3 -m venv ~/.headroom-venv</span><br><span class="line">~/.headroom-venv/bin/pip install -U pip</span><br><span class="line"></span><br><span class="line"><span class="comment"># 装 headroom-ai（这一步很慢，耐心等）</span></span><br><span class="line">~/.headroom-venv/bin/pip install headroom-ai</span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证</span></span><br><span class="line">~/.headroom-venv/bin/headroom --version</span><br></pre></td></tr></table></figure><p>看到 <code>headroom, version 0.28.0</code>（或更高）即成功。</p><blockquote><p>如果 pip 报 Python 版本不满足（headroom 可能要求 3.10+，3.11 一般够），改用 uv 装 Python 3.12：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">curl -LsSf https://astral.sh/uv/install.sh | sh</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="string">&quot;<span class="variable">$HOME</span>/.local/bin:<span class="variable">$PATH</span>&quot;</span></span><br><span class="line">uv venv ~/.headroom-venv --python 3.12</span><br><span class="line">~/.headroom-venv/bin/pip install headroom-ai</span><br></pre></td></tr></table></figure></blockquote><h2 id="2-写启动脚本"><a href="#2-写启动脚本" class="headerlink" title="2. 写启动脚本"></a>2. 写启动脚本</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> &gt; ~/.headroom-start.sh &lt;&lt; <span class="string">&#x27;EOF&#x27;</span></span><br><span class="line"><span class="comment">#!/bin/bash</span></span><br><span class="line"><span class="comment"># 上游：智谱 paas/v4（OpenAI 格式）</span></span><br><span class="line"><span class="built_in">export</span> OPENAI_TARGET_API_URL=https://open.bigmodel.cn/api/paas/v4</span><br><span class="line"><span class="comment"># 跳过上游健康检查（避免误判启动失败）</span></span><br><span class="line"><span class="built_in">export</span> HEADROOM_SKIP_UPSTREAM_CHECK=1</span><br><span class="line"><span class="comment"># CowAgent 是普通 OpenAI 客户端，不注入 headroom_retrieve 工具，跑纯压缩</span></span><br><span class="line"><span class="built_in">export</span> HEADROOM_NO_CCR_INJECT_TOOL=1</span><br><span class="line"><span class="comment"># 不注入 memory 工具/上下文（进一步省 token，避免干扰聊天）</span></span><br><span class="line"><span class="built_in">export</span> HEADROOM_NO_MEMORY_TOOLS=1</span><br><span class="line"><span class="built_in">export</span> HEADROOM_NO_MEMORY_CONTEXT=1</span><br><span class="line"><span class="built_in">exec</span> ~/.headroom-venv/bin/headroom proxy --port 8787 --host 127.0.0.1</span><br><span class="line">EOF</span><br><span class="line"><span class="built_in">chmod</span> +x ~/.headroom-start.sh</span><br></pre></td></tr></table></figure><p>参数说明：</p><ul><li><code>--host 127.0.0.1</code>：只本机监听，不暴露到局域网（CowAgent 在同一台 NAS，够用）</li><li><code>OPENAI_TARGET_API_URL</code>：headroom 把 OpenAI 格式请求转发到智谱</li><li><code>HEADROOM_NO_CCR_INJECT_TOOL</code>：<strong>关键</strong>，否则 CowAgent 收到陌生的 <code>headroom_retrieve</code> 工具会报错</li><li>默认 <code>--mode token</code>（压缩优先），想更激进可加 <code>--target-ratio 0.5</code>（保留约 50% token）</li></ul><h2 id="3-先手跑一次，验证能通"><a href="#3-先手跑一次，验证能通" class="headerlink" title="3. 先手跑一次，验证能通"></a>3. 先手跑一次，验证能通</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">~/.headroom-start.sh</span><br></pre></td></tr></table></figure><p>另开一个终端（或先 Ctrl+C，用下面命令测），跑 curl 验证 OpenAI 入口 + 智谱转发：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">curl -s http://127.0.0.1:8787/v1/chat/completions \</span><br><span class="line">  -H <span class="string">&quot;Authorization: Bearer <span class="subst">$(python3 -c &#x27;import json;print(json.load(open(<span class="string">&quot;/root/CowAgent/config.json&quot;</span>)</span>)[&quot;</span>zhipu_ai_api_key<span class="string">&quot;])&#x27;)&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;content-type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&quot;model&quot;:&quot;glm-5-turbo&quot;,&quot;max_tokens&quot;:16,&quot;messages&quot;:[&#123;&quot;role&quot;:&quot;user&quot;,&quot;content&quot;:&quot;说声ok&quot;&#125;]&#125;&#x27;</span></span><br></pre></td></tr></table></figure><p>看到正常的 GLM 回复（<code>&quot;content&quot;:&quot;ok&quot;...</code> 之类）就说明链路通了。<code>Ctrl+C</code> 停掉手跑的进程，下一步交给 systemd 托管。</p><blockquote><p>如果 curl 报错，看 <code>/tmp/headroom-proxy.log</code>（手跑时是终端输出）排查。常见是 key 没透传或模型名不对。</p></blockquote><h2 id="4-设开机自启（systemd）"><a href="#4-设开机自启（systemd）" class="headerlink" title="4. 设开机自启（systemd）"></a>4. 设开机自启（systemd）</h2><p>fnOS 是 Debian 系，systemd 可用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> &gt; /etc/systemd/system/headroom.service &lt;&lt; <span class="string">&#x27;EOF&#x27;</span></span><br><span class="line">[Unit]</span><br><span class="line">Description=Headroom compression proxy</span><br><span class="line">After=network-online.target</span><br><span class="line">Wants=network-online.target</span><br><span class="line"></span><br><span class="line">[Service]</span><br><span class="line">Type=simple</span><br><span class="line">ExecStart=/root/.headroom-start.sh</span><br><span class="line">Restart=always</span><br><span class="line">RestartSec=10</span><br><span class="line">StandardOutput=append:/var/log/headroom.log</span><br><span class="line">StandardError=append:/var/log/headroom.log</span><br><span class="line"></span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line">systemctl daemon-reload</span><br><span class="line">systemctl <span class="built_in">enable</span> --now headroom</span><br><span class="line">systemctl status headroom --no-pager</span><br></pre></td></tr></table></figure><p>看到 <code>active (running)</code> 即成功。日志在 <code>/var/log/headroom.log</code>。</p><h2 id="5-改-CowAgent-配置指向-headroom"><a href="#5-改-CowAgent-配置指向-headroom" class="headerlink" title="5. 改 CowAgent 配置指向 headroom"></a>5. 改 CowAgent 配置指向 headroom</h2><p>先备份：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cp</span> /root/CowAgent/config.json /root/CowAgent/config.json.bak.$(<span class="built_in">date</span> +%s)</span><br></pre></td></tr></table></figure><p>只改 <code>zhipu_ai_api_base</code>，其它不动：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sed -i <span class="string">&#x27;s#&quot;zhipu_ai_api_base&quot;: &quot;https://open.bigmodel.cn/api/paas/v4&quot;#&quot;zhipu_ai_api_base&quot;: &quot;http://127.0.0.1:8787/v1&quot;#&#x27;</span> /root/CowAgent/config.json</span><br></pre></td></tr></table></figure><p>验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep zhipu_ai_api_base /root/CowAgent/config.json</span><br></pre></td></tr></table></figure><p>应为 <code>&quot;zhipu_ai_api_base&quot;: &quot;http://127.0.0.1:8787/v1&quot;</code>。</p><h2 id="6-重启-CowAgent"><a href="#6-重启-CowAgent" class="headerlink" title="6. 重启 CowAgent"></a>6. 重启 CowAgent</h2><p>改完 <code>zhipu_ai_api_base</code> 后必须重启 CowAgent 才能生效。CowAgent 的重启方式取决于它是怎么跑起来的：</p><h3 id="如果用-systemd-托管（推荐）"><a href="#如果用-systemd-托管（推荐）" class="headerlink" title="如果用 systemd 托管（推荐）"></a>如果用 systemd 托管（推荐）</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">systemctl restart cowagent</span><br><span class="line">systemctl status cowagent --no-pager</span><br></pre></td></tr></table></figure><p>看到 <code>active (running)</code> 即已重启。日志在 <code>/var/log/cowagent.log</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">tail</span> -f /var/log/cowagent.log</span><br></pre></td></tr></table></figure><blockquote><p>如果还没给 CowAgent 配 systemd，参考 <a href="/cowagent-fnnas-deploy/">CowAgent 部署指南</a> 第 4 节。</p></blockquote><h3 id="如果用-screen-nohup-跑的"><a href="#如果用-screen-nohup-跑的" class="headerlink" title="如果用 screen &#x2F; nohup 跑的"></a>如果用 screen &#x2F; nohup 跑的</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># screen 方式</span></span><br><span class="line">screen -r cowagent       <span class="comment"># 进入会话</span></span><br><span class="line"><span class="comment"># Ctrl+C 停掉旧进程</span></span><br><span class="line"><span class="built_in">cd</span> /root/CowAgent &amp;&amp; <span class="built_in">source</span> venv/bin/activate &amp;&amp; python3 app.py</span><br><span class="line"><span class="comment"># Ctrl+A 再按 D 脱离会话</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># nohup 方式</span></span><br><span class="line"><span class="built_in">kill</span> $(pgrep -f cowagent)</span><br><span class="line"><span class="built_in">nohup</span> /root/CowAgent/venv/bin/python3 app.py &gt; /var/log/cowagent.log 2&gt;&amp;1 &amp;</span><br></pre></td></tr></table></figure><h3 id="验证链路"><a href="#验证链路" class="headerlink" title="验证链路"></a>验证链路</h3><p>重启后在 Web 控制台或微信&#x2F;飞书发一条消息，能正常回复就说明整条链路通了——headroom 压缩 + GLM 转发没有断。如果报错，先看两头日志定位：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># headroom 日志</span></span><br><span class="line"><span class="built_in">tail</span> -f /var/log/headroom.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># CowAgent 日志</span></span><br><span class="line"><span class="built_in">tail</span> -f /var/log/cowagent.log</span><br></pre></td></tr></table></figure><h2 id="7-确认压缩在生效"><a href="#7-确认压缩在生效" class="headerlink" title="7. 确认压缩在生效"></a>7. 确认压缩在生效</h2><p>看 headroom 日志：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">tail</span> -f /var/log/headroom.log</span><br></pre></td></tr></table></figure><p>请求经过时会有 <code>tokens_before / tokens_after / latency_ms</code> 之类的记录，<code>tokens_after &lt; tokens_before</code> 就说明在省 token。</p><hr><h2 id="出问题怎么回滚"><a href="#出问题怎么回滚" class="headerlink" title="出问题怎么回滚"></a>出问题怎么回滚</h2><p>CowAgent 不正常就立刻切回直连智谱：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cp</span> /root/CowAgent/config.json.bak.* /root/CowAgent/config.json</span><br><span class="line"><span class="comment"># 重启 CowAgent</span></span><br></pre></td></tr></table></figure><p>Headroom 本身停掉不影响 CowAgent（只是没了压缩，CowAgent 直连 headroom 会连不上，所以回滚 config 必须做）。要彻底卸载 headroom：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">systemctl <span class="built_in">disable</span> --now headroom</span><br><span class="line"><span class="built_in">rm</span> /etc/systemd/system/headroom.service</span><br><span class="line">systemctl daemon-reload</span><br><span class="line"><span class="built_in">rm</span> -rf ~/.headroom-venv ~/.headroom-start.sh</span><br></pre></td></tr></table></figure><h2 id="备选：如果-zhipu-SDK-不兼容代理"><a href="#备选：如果-zhipu-SDK-不兼容代理" class="headerlink" title="备选：如果 zhipu SDK 不兼容代理"></a>备选：如果 zhipu SDK 不兼容代理</h2><p>CowAgent 的 zhipu bot 用的是智谱 SDK，极少数情况下可能跟代理相处不好。若第 6 步报错，改用标准 OpenAI 通道（更可预测）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">python3 - &lt;&lt; <span class="string">&#x27;EOF&#x27;</span></span><br><span class="line">import json</span><br><span class="line">p = <span class="string">&quot;/root/CowAgent/config.json&quot;</span></span><br><span class="line">c = json.load(open(p))</span><br><span class="line">c[<span class="string">&quot;bot_type&quot;</span>] = <span class="string">&quot;openai&quot;</span></span><br><span class="line">c[<span class="string">&quot;open_ai_api_base&quot;</span>] = <span class="string">&quot;http://127.0.0.1:8787/v1&quot;</span></span><br><span class="line">c[<span class="string">&quot;open_ai_api_key&quot;</span>] = c[<span class="string">&quot;zhipu_ai_api_key&quot;</span>]</span><br><span class="line">c[<span class="string">&quot;model&quot;</span>] = <span class="string">&quot;glm-5-turbo&quot;</span></span><br><span class="line">json.dump(c, open(p, <span class="string">&quot;w&quot;</span>), indent=4, ensure_ascii=False)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;done&quot;</span>)</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure><h2 id="然后重启-CowAgent。"><a href="#然后重启-CowAgent。" class="headerlink" title="然后重启 CowAgent。"></a>然后重启 CowAgent。</h2><h2 id="文件清单"><a href="#文件清单" class="headerlink" title="文件清单"></a>文件清单</h2><table><thead><tr><th>路径</th><th>作用</th></tr></thead><tbody><tr><td><code>~/.headroom-venv/</code></td><td>Headroom 专用 Python 虚拟环境</td></tr><tr><td><code>~/.headroom-start.sh</code></td><td>Headroom 启动脚本（含环境变量配置）</td></tr><tr><td><code>/etc/systemd/system/headroom.service</code></td><td>systemd 服务文件</td></tr><tr><td><code>/var/log/headroom.log</code></td><td>Headroom 运行日志</td></tr><tr><td><code>/root/CowAgent/config.json</code></td><td>CowAgent 主配置（<code>zhipu_ai_api_base</code> 改成了 headroom）</td></tr><tr><td><code>/root/CowAgent/config.json.bak.*</code></td><td>改配置前的备份（回滚用）</td></tr></tbody></table>]]>
    </content>
    <id>https://www.tenxiaodao.top/headroom-fnnas-deploy/</id>
    <link href="https://www.tenxiaodao.top/headroom-fnnas-deploy/"/>
    <published>2026-07-12T06:00:00.000Z</published>
    <summary>本文记录在飞牛 NAS（aarch64）上部署 Headroom 压缩代理的完整过程，通过透明代理压缩上下文、节省 token，为 CowAgent 的 GLM 调用降本增效。</summary>
    <title>飞牛 NAS 部署 Headroom 压缩代理：给 CowAgent 省 Token 的完整指南</title>
    <updated>2026-08-25T17:52:18.623Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="技术笔记" scheme="https://www.tenxiaodao.top/categories/%E6%8A%80%E6%9C%AF%E7%AC%94%E8%AE%B0/"/>
    <category term="Headroom" scheme="https://www.tenxiaodao.top/tags/Headroom/"/>
    <category term="GLM" scheme="https://www.tenxiaodao.top/tags/GLM/"/>
    <category term="智谱" scheme="https://www.tenxiaodao.top/tags/%E6%99%BA%E8%B0%B1/"/>
    <category term="Claude Code" scheme="https://www.tenxiaodao.top/tags/Claude-Code/"/>
    <category term="cc-switch" scheme="https://www.tenxiaodao.top/tags/cc-switch/"/>
    <category term="省钱" scheme="https://www.tenxiaodao.top/tags/%E7%9C%81%E9%92%B1/"/>
    <category term="代理" scheme="https://www.tenxiaodao.top/tags/%E4%BB%A3%E7%90%86/"/>
    <content>
      <![CDATA[<blockquote><p>把 Claude Code 的 API 调用链路改成 <code>Claude Code → Headroom → cc-switch → GLM</code>，用国产模型替代 Claude 的同时，靠 Headroom 压缩上下文进一步降低 token 消耗。本文记录完整的搭建过程。</p></blockquote><hr><h2 id="为什么要这么折腾"><a href="#为什么要这么折腾" class="headerlink" title="为什么要这么折腾"></a>为什么要这么折腾</h2><p>Claude Code 用起来很爽，但 Claude 的 API 价格摆在那里——尤其是长上下文对话，token 消耗很快。国内智谱 GLM 提供了 Anthropic 兼容接口，可以完全替代 Claude 的模型层，价格低很多。当然claude也可以用，同理。</p><p>但光替换模型还不够。Claude Code 默认会把完整对话历史每次都发上去，如果能在中间加一层压缩代理，剪掉冗余的上下文，token 消耗还能再省一笔。</p><p>所以最终链路设计成这样：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Claude Code → Headroom (:8787，压缩上下文)</span><br><span class="line">            → cc-switch (:15721，注入 GLM key)</span><br><span class="line">            → https://open.bigmodel.cn/api/anthropic (glm-5.2)</span><br></pre></td></tr></table></figure><p>三个组件各司其职：</p><ul><li><strong>Headroom</strong>：HTTP 代理，负责压缩发往上游的上下文，省 token。</li><li><strong>cc-switch</strong>：GUI 应用，本地代理 <code>:15721</code>，保管真实 GLM API key，并把 Claude 的各模型名映射成 <code>glm-5.2</code>。</li><li><strong>GLM</strong>：智谱开放平台提供的 Anthropic 兼容接口。</li></ul><hr><h2 id="前置条件"><a href="#前置条件" class="headerlink" title="前置条件"></a>前置条件</h2><ul><li>macOS（Apple Silicon 或 Intel 均可）。</li><li>已安装 <a href="https://github.com/farion1231/cc-switch">cc-switch</a>（<code>/Applications/CC Switch.app</code>）并配置好一个 GLM provider（本文用 Anthropic 兼容模式）。</li><li>已有智谱 API key（在 <a href="https://open.bigmodel.cn/">open.bigmodel.cn</a> 获取）。</li><li>系统自带 Python 3.9 不够用，Headroom 需要 Python ≥ 3.10，所以需要用 <code>uv</code> 单独建一个 Python 3.12 的虚拟环境。</li></ul><hr><h2 id="第一步：安装-uv"><a href="#第一步：安装-uv" class="headerlink" title="第一步：安装 uv"></a>第一步：安装 uv</h2><p>Headroom 通过 <code>uv</code> 安装。如果还没装 uv：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl -LsSf https://astral.sh/uv/install.sh | sh</span><br></pre></td></tr></table></figure><p>安装后 uv 在 <code>~/.local/bin/uv</code>，确认一下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">~/.local/bin/uv --version</span><br></pre></td></tr></table></figure><p>如果提示找不到命令，把 <code>~/.local/bin</code> 加进 <code>PATH</code>，写进 <code>~/.zshrc</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> PATH=<span class="string">&quot;<span class="variable">$HOME</span>/.local/bin:<span class="variable">$PATH</span>&quot;</span></span><br></pre></td></tr></table></figure><hr><h2 id="第二步：用-uv-创建-Python-3-12-环境并安装-Headroom"><a href="#第二步：用-uv-创建-Python-3-12-环境并安装-Headroom" class="headerlink" title="第二步：用 uv 创建 Python 3.12 环境并安装 Headroom"></a>第二步：用 uv 创建 Python 3.12 环境并安装 Headroom</h2><p>系统 Python 3.9 太旧了，用 uv 自带的 Python 管理功能建一个新环境：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">uv venv ~/.headroom-venv --python 3.12</span><br><span class="line"><span class="built_in">source</span> ~/.headroom-venv/bin/activate</span><br><span class="line">uv pip install headroom</span><br></pre></td></tr></table></figure><p>验证安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">~/.headroom-venv/bin/headroom --version</span><br><span class="line"><span class="comment"># 期望输出类似：headroom, version 0.28.0</span></span><br></pre></td></tr></table></figure><hr><h2 id="第三步：配置-cc-switch"><a href="#第三步：配置-cc-switch" class="headerlink" title="第三步：配置 cc-switch"></a>第三步：配置 cc-switch</h2><p>打开 CC Switch.app，添加一个 provider，类型选 <strong>Anthropic 兼容</strong>：</p><ul><li>Base URL：<code>https://open.bigmodel.cn/api/anthropic</code></li><li>API Key：你的智谱 key</li><li>模型映射：把 Opus &#x2F; Sonnet &#x2F; Haiku &#x2F; Fable 全部映射到 <code>glm-5.2</code></li></ul><p>然后开启<strong>本地代理</strong>（<code>enableLocalProxy: true</code>），cc-switch 会在 <code>127.0.0.1:15721</code> 启动一个代理。点应用后，cc-switch 会写入 <code>~/.claude/settings.json</code>，内容大致长这样：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;env&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_AUTH_TOKEN&quot;</span><span class="punctuation">:</span> <span class="string">&quot;PROXY_MANAGED&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_BASE_URL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://127.0.0.1:15721&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_DEFAULT_OPUS_MODEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;claude-opus-4-8&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_DEFAULT_OPUS_MODEL_NAME&quot;</span><span class="punctuation">:</span> <span class="string">&quot;glm-5.2&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_DEFAULT_SONNET_MODEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;claude-sonnet-4-6&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_DEFAULT_SONNET_MODEL_NAME&quot;</span><span class="punctuation">:</span> <span class="string">&quot;glm-5.2&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_DEFAULT_HAIKU_MODEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;claude-haiku-4-5&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_DEFAULT_HAIKU_MODEL_NAME&quot;</span><span class="punctuation">:</span> <span class="string">&quot;glm-5.2&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_DEFAULT_FABLE_MODEL&quot;</span><span class="punctuation">:</span> <span class="string">&quot;glm-5.2&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;ANTHROPIC_DEFAULT_FABLE_MODEL_NAME&quot;</span><span class="punctuation">:</span> <span class="string">&quot;glm-5.2&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>注意 <code>ANTHROPIC_AUTH_TOKEN: PROXY_MANAGED</code> 是 cc-switch 的占位符——真实的 key 存在 cc-switch 内部的 SQLite 数据库里（<code>~/.cc-switch/cc-switch.db</code>），<strong>不会</strong>以明文出现在 settings.json 中。</p><p>此时链路已经是 <code>Claude Code → cc-switch → GLM</code> 了，但还没有压缩。下一步把 Headroom 插到中间。</p><hr><h2 id="第四步：编写-Headroom-启动脚本"><a href="#第四步：编写-Headroom-启动脚本" class="headerlink" title="第四步：编写 Headroom 启动脚本"></a>第四步：编写 Headroom 启动脚本</h2><p>新建 <code>~/.headroom-start.sh</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> &gt; ~/.headroom-start.sh &lt;&lt;<span class="string">&#x27;EOF&#x27;</span></span><br><span class="line"><span class="comment">#!/usr/bin/env zsh</span></span><br><span class="line"><span class="built_in">source</span> <span class="string">&quot;<span class="variable">$HOME</span>/.headroom-venv/bin/activate&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Upstream = cc-switch 的本地代理（负责注入 GLM key）</span></span><br><span class="line"><span class="built_in">export</span> ANTHROPIC_TARGET_API_URL=<span class="string">&quot;http://127.0.0.1:15721&quot;</span></span><br><span class="line"><span class="built_in">export</span> HEADROOM_HOST=127.0.0.1</span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出压缩：默认关掉，设为 1 启用</span></span><br><span class="line"><span class="built_in">export</span> HEADROOM_OUTPUT_SHAPER=<span class="string">&quot;<span class="variable">$&#123;HEADROOM_OUTPUT_SHAPER:-0&#125;</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 跳过上游可达性检查——cc-switch 是 loopback 代理，</span></span><br><span class="line"><span class="comment"># Headroom 的 /readyz 探活可能误报</span></span><br><span class="line"><span class="built_in">export</span> HEADROOM_SKIP_UPSTREAM_CHECK=1</span><br><span class="line"></span><br><span class="line"><span class="built_in">exec</span> headroom proxy --port 8787 --host 127.0.0.1</span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line"><span class="built_in">chmod</span> +x ~/.headroom-start.sh</span><br></pre></td></tr></table></figure><p>几个关键环境变量：</p><ul><li><code>ANTHROPIC_TARGET_API_URL</code> 指向 cc-switch（<code>:15721</code>），而不是直连 GLM——key 注入仍由 cc-switch 负责，Headroom 只管压缩和转发。</li><li><code>HEADROOM_SKIP_UPSTREAM_CHECK=1</code>：cc-switch 是 loopback 代理，Headroom 对它做 <code>/readyz</code> 探活会误报，关掉换手动验证。</li><li><code>HEADROOM_OUTPUT_SHAPER</code>：默认 <code>0</code>（不压缩输出），想连模型回包一起压缩就设 <code>1</code>。</li></ul><p>先手动跑一次，确认能起来：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">~/.headroom-start.sh</span><br><span class="line"><span class="comment"># 另开终端验证：</span></span><br><span class="line">curl -s http://127.0.0.1:8787/livez</span><br></pre></td></tr></table></figure><p>确认无误后 <code>Ctrl+C</code> 停掉，下一步交给 launchd 托管。</p><hr><h2 id="第五步：用-launchd-设置开机自启"><a href="#第五步：用-launchd-设置开机自启" class="headerlink" title="第五步：用 launchd 设置开机自启"></a>第五步：用 launchd 设置开机自启</h2><p>创建 <code>~/Library/LaunchAgents/com.headroom.proxy.plist</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> &gt; ~/Library/LaunchAgents/com.headroom.proxy.plist &lt;&lt;<span class="string">&#x27;EOF&#x27;</span></span><br><span class="line">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span>?&gt;</span><br><span class="line">&lt;!DOCTYPE plist PUBLIC <span class="string">&quot;-//Apple//DTD PLIST 1.0//EN&quot;</span> <span class="string">&quot;http://www.apple.com/DTDs/PropertyList-1.0.dtd&quot;</span>&gt;</span><br><span class="line">&lt;plist version=<span class="string">&quot;1.0&quot;</span>&gt;</span><br><span class="line">&lt;dict&gt;</span><br><span class="line">    &lt;key&gt;Label&lt;/key&gt;</span><br><span class="line">    &lt;string&gt;com.headroom.proxy&lt;/string&gt;</span><br><span class="line"></span><br><span class="line">    &lt;key&gt;ProgramArguments&lt;/key&gt;</span><br><span class="line">    &lt;array&gt;</span><br><span class="line">        &lt;string&gt;/bin/zsh&lt;/string&gt;</span><br><span class="line">        &lt;string&gt;__HOME__/.headroom-start.sh&lt;/string&gt;</span><br><span class="line">    &lt;/array&gt;</span><br><span class="line"></span><br><span class="line">    &lt;key&gt;RunAtLoad&lt;/key&gt;</span><br><span class="line">    &lt;<span class="literal">true</span>/&gt;</span><br><span class="line"></span><br><span class="line">    &lt;key&gt;KeepAlive&lt;/key&gt;</span><br><span class="line">    &lt;<span class="literal">true</span>/&gt;</span><br><span class="line"></span><br><span class="line">    &lt;key&gt;ThrottleInterval&lt;/key&gt;</span><br><span class="line">    &lt;<span class="built_in">integer</span>&gt;10&lt;/integer&gt;</span><br><span class="line"></span><br><span class="line">    &lt;key&gt;StandardOutPath&lt;/key&gt;</span><br><span class="line">    &lt;string&gt;/tmp/headroom-proxy.log&lt;/string&gt;</span><br><span class="line"></span><br><span class="line">    &lt;key&gt;StandardErrorPath&lt;/key&gt;</span><br><span class="line">    &lt;string&gt;/tmp/headroom-proxy.log&lt;/string&gt;</span><br><span class="line"></span><br><span class="line">    &lt;key&gt;ProcessType&lt;/key&gt;</span><br><span class="line">    &lt;string&gt;Background&lt;/string&gt;</span><br><span class="line">&lt;/dict&gt;</span><br><span class="line">&lt;/plist&gt;</span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line">sed -i <span class="string">&#x27;&#x27;</span> <span class="string">&quot;s|__HOME__|<span class="variable">$HOME</span>|&quot;</span> ~/Library/LaunchAgents/com.headroom.proxy.plist</span><br></pre></td></tr></table></figure><p>加载并启动：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">launchctl load ~/Library/LaunchAgents/com.headroom.proxy.plist</span><br><span class="line">launchctl list | grep headroom</span><br></pre></td></tr></table></figure><p>看到 <code>com.headroom.proxy</code> 出现在列表里就对了。plist 的几个关键配置：</p><ul><li><code>RunAtLoad=true</code>：开机或加载时自动启动。</li><li><code>KeepAlive=true</code>：进程意外退出会自动拉起。</li><li><code>ThrottleInterval=10</code>：崩溃重启的最小间隔 10 秒，防止重启风暴。</li><li>日志统一写到 <code>/tmp/headroom-proxy.log</code>。</li></ul><p>日常管理命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 停止</span></span><br><span class="line">launchctl unload ~/Library/LaunchAgents/com.headroom.proxy.plist</span><br><span class="line"></span><br><span class="line"><span class="comment"># 重启</span></span><br><span class="line">launchctl unload ... &amp;&amp; launchctl load ...</span><br><span class="line"></span><br><span class="line"><span class="comment"># 看日志</span></span><br><span class="line"><span class="built_in">tail</span> -f /tmp/headroom-proxy.log</span><br></pre></td></tr></table></figure><hr><h2 id="第六步：把-Claude-Code-的-base-URL-指向-Headroom"><a href="#第六步：把-Claude-Code-的-base-URL-指向-Headroom" class="headerlink" title="第六步：把 Claude Code 的 base URL 指向 Headroom"></a>第六步：把 Claude Code 的 base URL 指向 Headroom</h2><p>cc-switch 默认会把 <code>ANTHROPIC_BASE_URL</code> 写成 <code>http://127.0.0.1:15721</code>（直连 cc-switch）。要插入 Headroom，需要改成 <code>:8787</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">python3 - &lt;&lt;<span class="string">&#x27;EOF&#x27;</span></span><br><span class="line">import json, pathlib</span><br><span class="line">p = pathlib.Path.home() / <span class="string">&quot;.claude&quot;</span> / <span class="string">&quot;settings.json&quot;</span></span><br><span class="line">cfg = json.loads(p.read_text())</span><br><span class="line">cfg.setdefault(<span class="string">&quot;env&quot;</span>, &#123;&#125;)[<span class="string">&quot;ANTHROPIC_BASE_URL&quot;</span>] = <span class="string">&quot;http://127.0.0.1:8787&quot;</span></span><br><span class="line">p.write_text(json.dumps(cfg, indent=2, ensure_ascii=False))</span><br><span class="line"><span class="built_in">print</span>(p.read_text())</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure><p>改完后<strong>必须重启 Claude Code</strong>——当前会话用的还是改之前的 base URL，不重启不生效。</p><hr><h2 id="第七步：验证整条链路"><a href="#第七步：验证整条链路" class="headerlink" title="第七步：验证整条链路"></a>第七步：验证整条链路</h2><h3 id="直接请求-Headroom，确认能拿到-GLM-的回复"><a href="#直接请求-Headroom，确认能拿到-GLM-的回复" class="headerlink" title="直接请求 Headroom，确认能拿到 GLM 的回复"></a>直接请求 Headroom，确认能拿到 GLM 的回复</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">curl -s -X POST http://127.0.0.1:8787/v1/messages \</span><br><span class="line">  -H <span class="string">&quot;x-api-key: PROXY_MANAGED&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;anthropic-version: 2023-06-01&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;content-type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&quot;model&quot;:&quot;glm-5.2&quot;,&quot;max_tokens&quot;:16,&quot;messages&quot;:[&#123;&quot;role&quot;:&quot;user&quot;,&quot;content&quot;:&quot;say ok&quot;&#125;]&#125;&#x27;</span></span><br></pre></td></tr></table></figure><p>正常应该返回带 <code>msg_...</code> id 的 JSON，content 是 GLM 的回复内容。</p><h3 id="Headroom-自检"><a href="#Headroom-自检" class="headerlink" title="Headroom 自检"></a>Headroom 自检</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">~/.headroom-venv/bin/headroom doctor --port 8787</span><br></pre></td></tr></table></figure><p>注意 <code>doctor</code> 的 <code>/livez</code> 探针对 Headroom 自己的端口有效，不要让它去探 cc-switch 的 <code>15721</code>。</p><h3 id="检查-launchd-状态"><a href="#检查-launchd-状态" class="headerlink" title="检查 launchd 状态"></a>检查 launchd 状态</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">launchctl list | grep headroom</span><br></pre></td></tr></table></figure><p>第二列是 <code>0</code> 表示运行正常；非零说明崩了，看 <code>/tmp/headroom-proxy.log</code> 排查。</p><hr><h2 id="一个重要的大坑"><a href="#一个重要的大坑" class="headerlink" title="一个重要的大坑"></a>一个重要的大坑</h2><p>cc-switch 有一个很坑的行为：如果你在 GUI 里<strong>切换 provider</strong> 或重新点「应用」，它会重写 <code>~/.claude/settings.json</code>，把 <code>ANTHROPIC_BASE_URL</code> 改回 <code>http://127.0.0.1:15721</code>，<strong>直接绕过了 Headroom</strong>。</p><p>所以：</p><ul><li>每次在 cc-switch 里动过之后，重新执行第六步把 base URL 改回 <code>:8787</code>，然后重启 Claude Code。</li><li>或者干脆配好之后就不要再动 cc-switch，只在 GLM key 过期需要更新时才进 GUI。</li></ul><hr><h2 id="文件清单"><a href="#文件清单" class="headerlink" title="文件清单"></a>文件清单</h2><table><thead><tr><th>路径</th><th>作用</th></tr></thead><tbody><tr><td><code>~/.local/bin/uv</code></td><td>uv 安装器</td></tr><tr><td><code>~/.headroom-venv/</code></td><td>Headroom 专用 Python 3.12 venv</td></tr><tr><td><code>~/.headroom-start.sh</code></td><td>Headroom 启动脚本</td></tr><tr><td><code>~/Library/LaunchAgents/com.headroom.proxy.plist</code></td><td>launchd 自启配置</td></tr><tr><td><code>/tmp/headroom-proxy.log</code></td><td>Headroom 运行日志</td></tr><tr><td><code>~/.claude/settings.json</code></td><td>Claude Code 配置（base URL 指向 <code>:8787</code>）</td></tr><tr><td><code>~/.cc-switch/</code></td><td>cc-switch 数据库与配置（含真实 GLM key）</td></tr></tbody></table><hr><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><p><strong>Q: Claude Code 启动报连接 <code>:8787</code> 失败？</strong><br>A: Headroom 没起来。<code>launchctl list | grep headroom</code> 看进程在不在，不在就 <code>launchctl load ~/Library/LaunchAgents/com.headroom.proxy.plist</code>；在但崩了就看 <code>/tmp/headroom-proxy.log</code>。</p><p><strong>Q: 改了 cc-switch 后 Claude Code 不压缩了？</strong><br>A: 检查 <code>~/.claude/settings.json</code> 里 <code>ANTHROPIC_BASE_URL</code> 是不是被改回 <code>:15721</code> 了，改回 <code>:8787</code> 并重启 Claude Code。</p><p><strong>Q: 想连模型输出也压缩？</strong><br>A: 编辑 <code>~/.headroom-start.sh</code>，把 <code>HEADROOM_OUTPUT_SHAPER</code> 改成 <code>1</code>，然后 <code>launchctl unload &amp;&amp; load</code> 重启 Headroom。</p><p><strong>Q: 怎么临时停掉压缩直连 GLM？</strong><br>A: 把 <code>ANTHROPIC_BASE_URL</code> 临时改回 <code>http://127.0.0.1:15721</code> 并重启 Claude Code，用完再改回来。Headroom 进程本身不用停。</p><hr><h2 id="最后的效果"><a href="#最后的效果" class="headerlink" title="最后的效果"></a>最后的效果</h2><p>全部配好之后，每次 Claude Code 发请求都会经过 Headroom 压缩再转发到 cc-switch，最后由 cc-switch 注入 GLM key 送到智谱。实际使用下来，上下文压缩能省掉相当可观的 token 量，再加上 GLM 本身比 Claude 便宜很多，这套链路长期用下来确实能省不少。</p><p>唯一需要留意的就是那个 cc-switch 覆写 base URL 的坑，记住每次动过 cc-switch 后改回来就行。如果你只是稳定用同一个 GLM provider，配好之后几乎不用再管它。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/headroom-cc-switch-glm/</id>
    <link href="https://www.tenxiaodao.top/headroom-cc-switch-glm/"/>
    <published>2026-07-02T10:00:00.000Z</published>
    <summary>
      <![CDATA[<blockquote>
<p>把 Claude Code 的 API 调用链路改成 <code>Claude Code → Headroom → cc-switch → GLM</code>，用国产模型替代 Claude 的同时，靠 Headroom 压缩上下文进一步降低 to]]>
    </summary>
    <title>Headroom + cc-switch + GLM：给 Claude Code 省钱的代理链路搭建指南</title>
    <updated>2026-07-02T12:55:47.613Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="工具分享" scheme="https://www.tenxiaodao.top/categories/%E5%B7%A5%E5%85%B7%E5%88%86%E4%BA%AB/"/>
    <category term="跨平台" scheme="https://www.tenxiaodao.top/tags/%E8%B7%A8%E5%B9%B3%E5%8F%B0/"/>
    <category term="效率工具" scheme="https://www.tenxiaodao.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <category term="局域网" scheme="https://www.tenxiaodao.top/tags/%E5%B1%80%E5%9F%9F%E7%BD%91/"/>
    <category term="Synergy" scheme="https://www.tenxiaodao.top/tags/Synergy/"/>
    <category term="键鼠共享" scheme="https://www.tenxiaodao.top/tags/%E9%94%AE%E9%BC%A0%E5%85%B1%E4%BA%AB/"/>
    <content>
      <![CDATA[<blockquote><p>Synergy 是一款纯软件的 KVM 方案：不用切换器、不用额外硬件，装好软件就能用一套键盘鼠标控制多台电脑。Windows、macOS、Linux、树莓派都能连在一起。</p></blockquote><hr><h2 id="为什么需要-Synergy"><a href="#为什么需要-Synergy" class="headerlink" title="为什么需要 Synergy"></a>为什么需要 Synergy</h2><p>桌面上摆两台电脑的情况其实挺常见的——一台 MacBook 办公，一台 Windows 台式机打游戏或跑实验；或者 Linux 服务器 + 日常用的笔记本。每台机器各接一套键鼠，桌面很快就不够用了。</p><p>硬件 KVM 切换器可以解决，但有两个问题：</p><ol><li><strong>贵</strong>——支持高分辨率 + 多显示器的 KVM 价格不便宜</li><li><strong>不灵活</strong>——同一时刻只能操作一台机器，没法把鼠标从 A 屏滑到 B 屏</li></ol><p>Synergy 的做法更符合直觉：光标移动到屏幕边缘，自动跳到另一台电脑的屏幕上。跟用多显示器一样，只是另一块屏幕是物理上独立的计算机。</p><hr><h2 id="功能介绍"><a href="#功能介绍" class="headerlink" title="功能介绍"></a>功能介绍</h2><h3 id="跨平台操控"><a href="#跨平台操控" class="headerlink" title="跨平台操控"></a>跨平台操控</h3><p>Synergy 支持 Windows &#x2F; macOS &#x2F; Linux &#x2F; 树莓派，不限操作系统组合。光标移向屏幕边缘自动切换，拖拽丝滑。</p><h3 id="剪贴板共享"><a href="#剪贴板共享" class="headerlink" title="剪贴板共享"></a>剪贴板共享</h3><p>跨设备复制粘贴文本，从 Mac 上复制一段代码，直接 Ctrl+V 粘贴到 Windows 上。不用再发微信、传网盘、跑 scp。</p><h3 id="键位自动适配"><a href="#键位自动适配" class="headerlink" title="键位自动适配"></a>键位自动适配</h3><p>Windows 的 <code>Win</code> 键在 macOS 上自动映射为 <code>Cmd</code>，组合键的使用习惯不被打断。反过来也一样，Mac 键盘的 <code>Cmd+C</code> 到了 Windows 机器上等于 <code>Ctrl+C</code>。</p><h3 id="拖拽传输文件（专业版）"><a href="#拖拽传输文件（专业版）" class="headerlink" title="拖拽传输文件（专业版）"></a>拖拽传输文件（专业版）</h3><p>专业版支持跨设备拖拽文件，相当于在局域网里搭了一个临时的点对点文件通道，传小文件非常方便。</p><h3 id="TLS-SSL-加密"><a href="#TLS-SSL-加密" class="headerlink" title="TLS&#x2F;SSL 加密"></a>TLS&#x2F;SSL 加密</h3><p>局域网通信默认走 TLS 加密，不用担心同网络下有人抓包劫持输入。如果只是在家里的信任网络用，也可以关掉加密降低一点点延迟。</p><hr><h2 id="工作模式"><a href="#工作模式" class="headerlink" title="工作模式"></a>工作模式</h2><p>Synergy 是典型的主从（Server &#x2F; Client）架构：</p><ul><li><strong>Server（服务端）</strong>：键盘鼠标实际连接的那台机器，负责接收输入并转发给其他设备</li><li><strong>Client（客户端）</strong>：被控制的机器，只需要运行客户端软件</li></ul><p>谁当 Server 取决于键盘鼠标接在哪台机器上。比如你的主力机是 MacBook，键鼠都连在 MacBook 上，那 MacBook 就跑 Server，其他电脑跑 Client。</p><p>配置完之后的拓扑大概长这样：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">┌──────────┐      ┌──────────┐      ┌──────────┐</span><br><span class="line">│  MacBook  │─────│ Windows  │─────│  Linux   │</span><br><span class="line">│  (Server) │     │ (Client) │     │ (Client) │</span><br><span class="line">└──────────┘      └──────────┘      └──────────┘</span><br><span class="line">    ↓ 鼠标向右                      ↓ 鼠标向右</span><br></pre></td></tr></table></figure><p>屏幕的位置关系在配置界面里用拖拽排列，就跟 macOS 的多显示器设置一样。</p><hr><h2 id="安装与配置教程"><a href="#安装与配置教程" class="headerlink" title="安装与配置教程"></a>安装与配置教程</h2><h3 id="1-下载安装"><a href="#1-下载安装" class="headerlink" title="1. 下载安装"></a>1. 下载安装</h3><p>下载对应平台的安装包，Server 和 Client 都需要装。</p><p>各平台安装包：</p><table><thead><tr><th>平台</th><th>文件</th><th>安装方式</th></tr></thead><tbody><tr><td>Windows</td><td><code>synergy-1.14.6-windows-x64.msi</code></td><td>双击安装，一路 Next</td></tr><tr><td>macOS Intel</td><td><code>synergy-1.14.6-macos-x64.dmg</code></td><td>拖到 Applications</td></tr><tr><td>macOS Apple Silicon</td><td><code>synergy-1.14.6-macos-arm64.dmg</code></td><td>拖到 Applications</td></tr><tr><td>Linux (Debian&#x2F;Ubuntu)</td><td><code>synergy-1.14.6-linux-amd64.deb</code></td><td><code>sudo dpkg -i</code></td></tr><tr><td>Linux (通用)</td><td><code>synergy-1.14.6-linux-x64.tar.gz</code></td><td>解压后运行 <code>./synergy</code></td></tr></tbody></table><h3 id="2-配置-Server"><a href="#2-配置-Server" class="headerlink" title="2. 配置 Server"></a>2. 配置 Server</h3><ol><li>在连接键盘鼠标的那台机器上启动 Synergy，选择 <strong>Server</strong> 模式</li><li>点击 <strong>Configure Server</strong>，拖动右上角的电脑图标到网格上</li><li>配置每台电脑的<strong>屏幕名称</strong>（Screen Name），这个名称必须跟 Client 端设置的一致</li><li>按照物理布局排列屏幕位置——比如 MacBook 在左边，Windows 在右边，就把 MacBook 拖到格子左边、Windows 拖到右边</li><li>点击 <strong>OK</strong> 保存</li></ol><h3 id="3-配置-Client"><a href="#3-配置-Client" class="headerlink" title="3. 配置 Client"></a>3. 配置 Client</h3><ol><li>在被控制的机器上启动 Synergy，选择 <strong>Client</strong> 模式</li><li>在 <strong>Server IP</strong> 栏输入 Server 机器的局域网 IP 地址</li><li>确保 <strong>Screen Name</strong> 跟 Server 上配置的名称一致</li><li>点击 <strong>Start</strong>，几秒后应该就连上了</li></ol><h3 id="4-启动"><a href="#4-启动" class="headerlink" title="4. 启动"></a>4. 启动</h3><p>配置好后，Server 端先点 <strong>Start</strong>，然后各 Client 依次点 <strong>Start</strong>。鼠标移动到屏幕边缘就能看到光标跳到另一台机器上了。</p><blockquote><p><strong>小技巧</strong>：如果不想一开机就手动启动，可以把 Synergy 设为开机自启。Windows 上加个启动项，macOS 上加到登录项里，Linux 上写个 systemd service。</p></blockquote><hr><h2 id="实用技巧"><a href="#实用技巧" class="headerlink" title="实用技巧"></a>实用技巧</h2><h3 id="锁定光标"><a href="#锁定光标" class="headerlink" title="锁定光标"></a>锁定光标</h3><p>玩游戏的时候最怕鼠标滑到另一台屏幕上。Synergy 提供了 <strong>锁定模式</strong>（Scroll Lock 键切换），开启后光标不会离开当前屏幕，再按一次 Scroll Lock 解除。</p><p>也有更直接的办法——不用快捷键，直接通过托盘图标菜单切换锁定。</p><h3 id="隐藏系统鼠标"><a href="#隐藏系统鼠标" class="headerlink" title="隐藏系统鼠标"></a>隐藏系统鼠标</h3><p>鼠标离开当前机器时，可以把这台机器的鼠标指针隐藏起来，视觉上更干净。这个选项在 Server 的 Settings 里。</p><h3 id="反向滚动修正"><a href="#反向滚动修正" class="headerlink" title="反向滚动修正"></a>反向滚动修正</h3><p>macOS 的自然滚动方向和 Windows &#x2F; Linux 相反。Synergy 可以在客户端设置里单独反转滚动方向，不让两块屏幕的滚动手感打架。</p><h3 id="多显示器客户端"><a href="#多显示器客户端" class="headerlink" title="多显示器客户端"></a>多显示器客户端</h3><p>如果 Client 机器接了多个显示器，Synergy 也能正常处理——光标从 Server 过去之后，在 Client 的多屏之间移动是 Client 自己的事情，Synergy 只关心跨机器的边界。</p><hr><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><h3 id="连接不上-一直显示-Connecting"><a href="#连接不上-一直显示-Connecting" class="headerlink" title="连接不上 &#x2F; 一直显示 Connecting"></a>连接不上 &#x2F; 一直显示 Connecting</h3><p>排查步骤：</p><ol><li><code>ping</code> 检查两台机器是否在同一个局域网</li><li>检查防火墙是否放行了 Synergy（默认端口 24800）</li><li>确认 Server 和 Client 的 Screen Name 大小写完全一致</li><li>Windows 上用 <code>ipconfig</code>，macOS&#x2F;Linux 上用 <code>ifconfig</code> 或 <code>ip addr</code> 确认 IP 地址正确</li></ol><h3 id="延迟高"><a href="#延迟高" class="headerlink" title="延迟高"></a>延迟高</h3><p>Synergy 走的是局域网，延迟一般很小。如果感觉卡顿：</p><ul><li>检查是否有人在大流量下载 &#x2F; 上传</li><li>关掉 TLS 加密试试</li><li>两台机器尽量用有线网络，避免 Wi-Fi 信号干扰</li></ul><h3 id="剪贴板没有同步"><a href="#剪贴板没有同步" class="headerlink" title="剪贴板没有同步"></a>剪贴板没有同步</h3><ul><li>确认两台机器都至少连接成功过一次</li><li>某些 Linux 桌面环境需要额外安装 <code>xclip</code> 或 <code>xsel</code></li><li>只支持纯文本同步，图片和文件同步需要专业版</li></ul><hr><h2 id="更新日志"><a href="#更新日志" class="headerlink" title="更新日志"></a>更新日志</h2><p><strong>2023年5月4日 — Synergy 1.14.6</strong></p><p>这个版本是目前最新的稳定版，主要更新内容：</p><ul><li>修复了 macOS Ventura 上的权限问题</li><li>改进了 Windows 上的后台驻留稳定性</li><li>更新了 TLS 加密库</li><li>优化了高 DPI 屏幕下的光标平滑度</li></ul><p>1.14.5 和 1.14.6 在使用体验上没有本质区别，5 修了极少数的 bug，如果已经在用 1.14.5 且一切正常，可以不升。</p><hr><h2 id="资源与下载"><a href="#资源与下载" class="headerlink" title="资源与下载"></a>资源与下载</h2><p> <strong>必下载</strong>：1.14 基础包里包含激活码，无论下哪个版本都需要它的密钥文件。</p><p> <strong>下载地址</strong>：<a href="https://wwazn.lanzoul.com/i9iPb3tw417c">https://wwazn.lanzoul.com/i9iPb3tw417c</a></p><h3 id="各版本下载"><a href="#各版本下载" class="headerlink" title="各版本下载"></a>各版本下载</h3><table><thead><tr><th>版本</th><th>说明</th></tr></thead><tbody><tr><td>Synergy 1.14.6</td><td>最新稳定版，推荐</td></tr><tr><td>Synergy 1.14.5</td><td>上一版本，稳定可用</td></tr><tr><td>Synergy 1.14.1</td><td>早期版本，激活码通用</td></tr></tbody></table><p>之前的激活码在新版本上依然有效，亲测可用。</p><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>Synergy 是我桌面上每天都在用的工具。两台电脑、一套键鼠，光标划过去就是另一台机器，不需要切换器也不需要第二根鼠标。它解决的不是什么惊天动地的问题，但一旦用惯了，就再也回不去了。</p><p>配置一次大概十分钟，之后开机自动连接，几乎没有存在感——一个好的工具就应该是这样的。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/synergy-kvm-share/</id>
    <link href="https://www.tenxiaodao.top/synergy-kvm-share/"/>
    <published>2026-07-01T10:00:00.000Z</published>
    <summary>
      <![CDATA[<blockquote>
<p>Synergy 是一款纯软件的 KVM 方案：不用切换器、不用额外硬件，装好软件就能用一套键盘鼠标控制多台电脑。Windows、macOS、Linux、树莓派都能连在一起。</p>
</blockquote>
<hr>
<h2 id="为什么需要-]]>
    </summary>
    <title>Synergy 1.14.6 — 多设备键鼠共享，一套键鼠控制多台电脑</title>
    <updated>2026-07-01T09:17:09.673Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="技术笔记" scheme="https://www.tenxiaodao.top/categories/%E6%8A%80%E6%9C%AF%E7%AC%94%E8%AE%B0/"/>
    <category term="macOS" scheme="https://www.tenxiaodao.top/tags/macOS/"/>
    <category term="Electron" scheme="https://www.tenxiaodao.top/tags/Electron/"/>
    <category term="全屏" scheme="https://www.tenxiaodao.top/tags/%E5%85%A8%E5%B1%8F/"/>
    <category term="跨平台" scheme="https://www.tenxiaodao.top/tags/%E8%B7%A8%E5%B9%B3%E5%8F%B0/"/>
    <category term="打包" scheme="https://www.tenxiaodao.top/tags/%E6%89%93%E5%8C%85/"/>
    <content>
      <![CDATA[<blockquote><p>Mineradio 是一款基于 Electron 的沉浸式音乐播放器，视觉和交互体验都做得很棒，但官方只提供了 Windows 版。前段时间抽空把它适配到了 macOS，并打包成了 .dmg 安装包。这篇文章记录一下踩过的坑和最终方案。</p></blockquote><hr><h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>Mineradio 用 Electron 构建——也就是说底层是 Chromium + Node.js——按理说天生跨平台。项目里也确实有一段 macOS 的判断逻辑：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 第 1288 行</span></span><br><span class="line"><span class="keyword">if</span> (process.<span class="property">platform</span> !== <span class="string">&#x27;darwin&#x27;</span>) app.<span class="title function_">quit</span>();</span><br></pre></td></tr></table></figure><p>这行代码的意思是”非 Windows 就退出”，给 macOS 留了个明确的入口开关。抱着试试看的心态关掉这个判断，跑了一下 <code>npm start</code>，窗口真的弹出来了。但全屏按钮点了没反应。</p><p>于是开始了适配之旅。</p><hr><h2 id="全屏修复：最折腾的一环"><a href="#全屏修复：最折腾的一环" class="headerlink" title="全屏修复：最折腾的一环"></a>全屏修复：最折腾的一环</h2><h3 id="根因"><a href="#根因" class="headerlink" title="根因"></a>根因</h3><p>Electron 窗口如果同时设置了 <code>transparent: true</code> 和 <code>frame: false</code>（透明 + 无边框），在 macOS 上调用 <code>setFullScreen(true)</code> 会<strong>静默失败</strong>。这是 Electron 在 macOS 上的一个已知限制，因为原生全屏动画依赖窗口管理器的某些特性，透明无边框窗口不满足条件。</p><p>Mineradio 恰好就是这种窗口：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">mainWindow = <span class="keyword">new</span> <span class="title class_">BrowserWindow</span>(&#123;</span><br><span class="line">  <span class="attr">transparent</span>: <span class="literal">true</span>,  <span class="comment">// ← 透明背景</span></span><br><span class="line">  <span class="attr">frame</span>: <span class="literal">false</span>,       <span class="comment">// ← 无边框</span></span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><p>macOS 提供了一个替代方法叫 <code>setSimpleFullScreen()</code>。它不依赖原生全屏动画，透明无边框窗口也能正常使用。唯一的区别是它不会触发系统级的全屏转场动画（就是 macOS 那个带 Mission Control 飞入效果的切换），但实际效果完全一致——窗口占满整个屏幕。</p><p>改动一共涉及四处地方：</p><p><strong>1. 进入全屏时判断平台</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (process.<span class="property">platform</span> === <span class="string">&#x27;darwin&#x27;</span>) &#123;</span><br><span class="line">  win.<span class="title function_">setSimpleFullScreen</span>(<span class="literal">true</span>);</span><br><span class="line">  <span class="comment">// 手动铺满屏幕</span></span><br><span class="line">  <span class="keyword">const</span> display = screen.<span class="title function_">getDisplayMatching</span>(win.<span class="title function_">getBounds</span>());</span><br><span class="line">  win.<span class="title function_">setBounds</span>(display.<span class="property">bounds</span>, <span class="literal">false</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  win.<span class="title function_">setFullScreen</span>(<span class="literal">true</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>2. 退出全屏时同样走 macOS 分支</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (process.<span class="property">platform</span> === <span class="string">&#x27;darwin&#x27;</span> &amp;&amp; win.<span class="title function_">isSimpleFullScreen</span>()) &#123;</span><br><span class="line">  win.<span class="title function_">setSimpleFullScreen</span>(<span class="literal">false</span>);</span><br><span class="line">  <span class="title function_">applyWindowedBounds</span>(win);</span><br><span class="line">  <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>3. Escape 键退出的判断条件</strong></p><p>原来的 <code>isFullScreen()</code> 在 simple fullscreen 模式下返回 <code>false</code>，所以要多加一个 <code>isSimpleFullScreen()</code> 的判断。</p><p><strong>4. HTML 全屏退出时避免与简单全屏冲突</strong></p><p>当视频等 HTML 元素退出全屏时，如果窗口本身也处于 simple fullscreen 状态，不要重置窗口尺寸。</p><p>改完之后全屏功能完全正常，包括窗口大小恢复和 Escape 键退出都跟原生体验一致。</p><hr><h2 id="打包成-dmg"><a href="#打包成-dmg" class="headerlink" title="打包成 .dmg"></a>打包成 .dmg</h2><h3 id="生成-macOS-图标"><a href="#生成-macOS-图标" class="headerlink" title="生成 macOS 图标"></a>生成 macOS 图标</h3><p>项目里只有 Windows 的 <code>.ico</code> 和一张 <code>build/icon.png</code>。macOS 需要 <code>.icns</code> 格式，可以用系统自带的 <code>sips</code> 和 <code>iconutil</code> 从 PNG 生成：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 创建 iconset 目录</span></span><br><span class="line"><span class="built_in">mkdir</span> -p build/icon.iconset</span><br><span class="line"></span><br><span class="line"><span class="comment"># 用 sips 生成各种尺寸</span></span><br><span class="line"><span class="keyword">for</span> size <span class="keyword">in</span> 16 32 64 128 256 512 1024; <span class="keyword">do</span></span><br><span class="line">  sips -z <span class="variable">$size</span> <span class="variable">$size</span> build/icon.png \</span><br><span class="line">    --out build/icon.iconset/icon_<span class="variable">$&#123;size&#125;</span>x<span class="variable">$&#123;size&#125;</span>.png</span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 转换为 .icns</span></span><br><span class="line">iconutil -c icns build/icon.iconset -o build/icon.icns</span><br></pre></td></tr></table></figure><h3 id="electron-builder-配置"><a href="#electron-builder-配置" class="headerlink" title="electron-builder 配置"></a>electron-builder 配置</h3><p>在 <code>package.json</code> 的 <code>build</code> 字段里添加 macOS 和 DMG 配置：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">&quot;mac&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;executableName&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Mineradio&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;icon&quot;</span><span class="punctuation">:</span> <span class="string">&quot;build/icon.icns&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;category&quot;</span><span class="punctuation">:</span> <span class="string">&quot;public.app-category.music&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;hardenedRuntime&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;gatekeeperAssess&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;target&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span> <span class="attr">&quot;target&quot;</span><span class="punctuation">:</span> <span class="string">&quot;dmg&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;arch&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;arm64&quot;</span><span class="punctuation">,</span> <span class="string">&quot;x64&quot;</span><span class="punctuation">]</span> <span class="punctuation">&#125;</span><span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line"><span class="attr">&quot;dmg&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;title&quot;</span><span class="punctuation">:</span> <span class="string">&quot;$&#123;productName&#125; $&#123;version&#125;&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;icon&quot;</span><span class="punctuation">:</span> <span class="string">&quot;build/icon.icns&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;contents&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span> <span class="attr">&quot;x&quot;</span><span class="punctuation">:</span> <span class="number">130</span><span class="punctuation">,</span> <span class="attr">&quot;y&quot;</span><span class="punctuation">:</span> <span class="number">220</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span> <span class="attr">&quot;x&quot;</span><span class="punctuation">:</span> <span class="number">410</span><span class="punctuation">,</span> <span class="attr">&quot;y&quot;</span><span class="punctuation">:</span> <span class="number">220</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;link&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;path&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/Applications&quot;</span> <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h3 id="绕过-dmg-builder-的坑"><a href="#绕过-dmg-builder-的坑" class="headerlink" title="绕过 dmg-builder 的坑"></a>绕过 dmg-builder 的坑</h3><p>在国内网络环境下，electron-builder 的 <code>dmg-builder</code> 工具经常下载失败（404 错误）。折腾了几次之后发现不如分两步走：</p><ol><li>先用 <code>electron-builder --mac dir</code> 生成 <code>.app</code> 目录包</li><li>再用系统自带的 <code>hdiutil</code> 手动制作 DMG</li></ol><p>手动生成 DMG 的命令很简单：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">hdiutil create -volname <span class="string">&quot;Mineradio 1.0.10&quot;</span> \</span><br><span class="line">  -srcfolder dist/mac-arm64/Mineradio.app \</span><br><span class="line">  -ov -format UDZO \</span><br><span class="line">  Mineradio-1.0.10-arm64.dmg</span><br></pre></td></tr></table></figure><p>最终产物约 141MB，拖到 Applications 文件夹就能用。</p><h3 id="首次启动"><a href="#首次启动" class="headerlink" title="首次启动"></a>首次启动</h3><p>因为没有 Apple 开发者签名，macOS 会弹”无法验证开发者”的提示。解决办法跟其他未签名应用一样——右键点”打开”即可，只需要确认一次，后续正常双击运行。</p><hr><h2 id="macOS-上哪些功能不可用"><a href="#macOS-上哪些功能不可用" class="headerlink" title="macOS 上哪些功能不可用"></a>macOS 上哪些功能不可用</h2><p>以下是几个依赖 Windows 专属 API 的功能，在 macOS 上无法工作：</p><table><thead><tr><th>功能</th><th>原因</th><th>影响</th></tr></thead><tbody><tr><td>桌面壁纸模式</td><td>使用了 Windows <code>WorkerW</code> API 将播放器嵌入桌面壁纸层</td><td>无法将播放器设为壁纸</td></tr><tr><td>桌面歌词鼠标穿透</td><td>使用 Windows <code>GetAsyncKeyState</code> 轮询中键</td><td>桌面歌词的锁定切换不可用</td></tr><tr><td>自动创建桌面快捷方式</td><td>使用 Windows <code>.lnk</code> 文件 API</td><td>无影响，拖到 Applications 即可</td></tr></tbody></table><p>其他核心功能全部正常：</p><ul><li>音乐播放、搜索、歌单管理</li><li>网易云音乐登录与播放</li><li>QQ 音乐音源补充</li><li>歌词显示与桌面歌词（仅穿透不可用）</li><li>粒子视觉、Emily 播放态</li><li>3D 歌单架</li><li>天气电台</li><li>更新检测</li></ul><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这次适配比预想的简单，大部分时间花在全屏问题的排查上。Electron 应用在 macOS 上跑起来的门槛其实很低，真正的坑往往藏在那些听起来人畜无害的 API 组合里——比如 <code>transparent</code> + <code>frame: false</code> 不能全屏这种事情，文档里不会特别提醒你。</p><p>最终适配的代码和构建配置我都放在了 <a href="https://github.com/tenxiaodao/Mineradio-1.0.10">Mineradio-macOS</a> 这个 repo 里，有需要可以直接拿去用。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/mineradio-macos-port/</id>
    <link href="https://www.tenxiaodao.top/mineradio-macos-port/"/>
    <published>2026-06-25T10:00:00.000Z</published>
    <summary>
      <![CDATA[<blockquote>
<p>Mineradio 是一款基于 Electron 的沉浸式音乐播放器，视觉和交互体验都做得很棒，但官方只提供了 Windows 版。前段时间抽空把它适配到了 macOS，并打包成了 .dmg 安装包。这篇文章记录一下踩过的坑和最终方案。</p>
<]]>
    </summary>
    <title>把 Mineradio 移植到 macOS：全屏修复与 DMG 打包记录</title>
    <updated>2026-06-25T11:17:22.607Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="游戏开发" scheme="https://www.tenxiaodao.top/categories/%E6%B8%B8%E6%88%8F%E5%BC%80%E5%8F%91/"/>
    <category term="JavaScript" scheme="https://www.tenxiaodao.top/tags/JavaScript/"/>
    <category term="游戏开发" scheme="https://www.tenxiaodao.top/tags/%E6%B8%B8%E6%88%8F%E5%BC%80%E5%8F%91/"/>
    <category term="HTML5" scheme="https://www.tenxiaodao.top/tags/HTML5/"/>
    <category term="Canvas" scheme="https://www.tenxiaodao.top/tags/Canvas/"/>
    <category term="ECS" scheme="https://www.tenxiaodao.top/tags/ECS/"/>
    <content>
      <![CDATA[<blockquote><p>一款基于 Entity-Component-System (ECS) 架构的 HTML5 竖屏打飞机游戏——<strong>Strike Force</strong>。Canvas 原生渲染、纯 JS 实现，直接在浏览器里玩。</p></blockquote><div style="display:flex;justify-content:center;margin:32px 0;">  <iframe src="/games/strike-force/" width="400" height="640" style="border:2px solid #1a1a3a;border-radius:8px;box-shadow:0 0 30px rgba(79,195,247,0.2);max-width:100%;" allowfullscreen></iframe></div><hr><h2 id="试玩"><a href="#试玩" class="headerlink" title="试玩"></a>试玩</h2><p>按下 <strong>ENTER</strong> 开始游戏。<strong>WASD &#x2F; 方向键</strong> 控制飞机移动，<strong>空格</strong> 开火。摧毁敌机获得分数，活着就是胜利。</p><hr><h2 id="为什么用-ECS"><a href="#为什么用-ECS" class="headerlink" title="为什么用 ECS"></a>为什么用 ECS</h2><p>游戏逻辑拆成了十几个独立的 System，每个 System 只做一件事：</p><table><thead><tr><th>System</th><th>职责</th></tr></thead><tbody><tr><td><code>BackgroundSystem</code></td><td>星空滚动背景</td></tr><tr><td><code>InputSystem</code></td><td>键盘输入</td></tr><tr><td><code>PlayerMoveSystem</code></td><td>玩家移动</td></tr><tr><td><code>EnemyAISystem</code></td><td>敌机 AI</td></tr><tr><td><code>WeaponSystem</code></td><td>子弹生成</td></tr><tr><td><code>MoveSystem</code></td><td>位置更新</td></tr><tr><td><code>CollisionSystem</code></td><td>碰撞检测</td></tr><tr><td><code>DamageSystem</code></td><td>伤害处理</td></tr><tr><td><code>SpawnSystem</code></td><td>波次生成</td></tr><tr><td><code>LifespanSystem</code></td><td>生命周期</td></tr><tr><td><code>CleanupSystem</code></td><td>垃圾回收</td></tr><tr><td><code>RenderSystem</code></td><td>Canvas 渲染</td></tr><tr><td><code>HUDSystem</code></td><td>计分&#x2F;血量 UI</td></tr></tbody></table><p>好处很明显：</p><ul><li><strong>关注点分离</strong>——改碰撞不会炸掉渲染</li><li><strong>数据驱动</strong>——Component 就是纯数据对象，System 遍历处理</li><li><strong>易扩展</strong>——加一个新 System 就能加一个新特性，不改现有代码</li></ul><hr><h2 id="技术要点"><a href="#技术要点" class="headerlink" title="技术要点"></a>技术要点</h2><h3 id="1-自包含-HTML"><a href="#1-自包含-HTML" class="headerlink" title="1. 自包含 HTML"></a>1. 自包含 HTML</h3><p>整个游戏打包在一个 HTML 文件里（658 行），无任何外部依赖。<code>Canvas 2D</code> 原生渲染，没有框架，没有 CDN 脚本，打开即玩。</p><h3 id="2-性能"><a href="#2-性能" class="headerlink" title="2. 性能"></a>2. 性能</h3><p>用 <code>requestAnimationFrame</code> 驱动游戏循环，帧间隔上界 50ms 防止切标签页后的时间跳跃。碰撞检测用简单的 AABB 包围盒，每帧对子弹-敌人做配对遍历——在这种小型弹幕游戏中完全够用。</p><h3 id="3-敌机波次"><a href="#3-敌机波次" class="headerlink" title="3. 敌机波次"></a>3. 敌机波次</h3><p>敌机从屏幕顶部以递增速度下落，随分数增加生成频率和数量都会上升。每架敌机都有独立的生命值，打够次数才会爆炸。</p><hr><h2 id="源码结构"><a href="#源码结构" class="headerlink" title="源码结构"></a>源码结构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">StrikeForce.html          # 自包含游戏（658 行）</span><br><span class="line">src/</span><br><span class="line">├── main.js               # 入口 &amp; 游戏循环</span><br><span class="line">├── ecs/                  # ECS 核心</span><br><span class="line">│   ├── World.js</span><br><span class="line">│   ├── Entity.js</span><br><span class="line">│   └── Component.js</span><br><span class="line">├── components/           # 数据组件</span><br><span class="line">├── systems/              # 逻辑系统</span><br><span class="line">├── factory/              # 实体工厂</span><br><span class="line">└── data/                 # 配置 &amp; 常量</span><br></pre></td></tr></table></figure><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>从 ECS 核心到十几个 System 搭起来的游戏循环，不到 700 行代码就做出了一款可玩的打飞机游戏。如果你对 ECS 游戏架构感兴趣，或者想看看纯 Canvas 的 2D 游戏怎么写，欢迎留言讨论。</p><hr><p><em>试玩窗口在文章开头，如果你看不到，说明你的浏览器不支持 iframe 嵌入。直接访问 <a href="/games/strike-force/">&#x2F;games&#x2F;strike-force&#x2F;</a> 也一样。</em></p>]]>
    </content>
    <id>https://www.tenxiaodao.top/strike-force-shoot-em-up/</id>
    <link href="https://www.tenxiaodao.top/strike-force-shoot-em-up/"/>
    <published>2026-06-24T15:59:00.000Z</published>
    <summary>
      <![CDATA[<blockquote>
<p>一款基于 Entity-Component-System (ECS) 架构的 HTML5 竖屏打飞机游戏——<strong>Strike Force</strong>。Canvas 原生渲染、纯 JS 实现，直接在浏览器里玩。</p>
</bloc]]>
    </summary>
    <title>Strike Force - 我用 ECS 架构做了一款打飞机小游戏</title>
    <updated>2026-08-25T20:38:20.455Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="AI" scheme="https://www.tenxiaodao.top/categories/AI/"/>
    <category term="AI" scheme="https://www.tenxiaodao.top/tags/AI/"/>
    <category term="Codex" scheme="https://www.tenxiaodao.top/tags/Codex/"/>
    <category term="OpenAI" scheme="https://www.tenxiaodao.top/tags/OpenAI/"/>
    <category term="CLI" scheme="https://www.tenxiaodao.top/tags/CLI/"/>
    <category term="故障排查" scheme="https://www.tenxiaodao.top/tags/%E6%95%85%E9%9A%9C%E6%8E%92%E6%9F%A5/"/>
    <content>
      <![CDATA[<h1 id="Codex-CLI-疯狂写盘自救指南"><a href="#Codex-CLI-疯狂写盘自救指南" class="headerlink" title="Codex CLI 疯狂写盘自救指南"></a>Codex CLI 疯狂写盘自救指南</h1><p>最近 OpenAI Codex CLI 被曝出一个相当严重的磁盘写入 bug：SQLite 日志在 TRACE 级别全量落盘，一年能写穿一块消费级 NVMe SSD。本文带你理解问题原理、检查自己是否中招，并提供从升级到触发器的完整自救方案。</p><h2 id="这个-bug-到底在干什么"><a href="#这个-bug-到底在干什么" class="headerlink" title="这个 bug 到底在干什么"></a>这个 bug 到底在干什么</h2><p>Codex CLI 会把运行日志写进一个本地 SQLite 数据库 <code>~/.codex/logs_2.sqlite</code>。问题出在它在 <strong>TRACE 级别全量落盘</strong>——每一个 WebSocket 事件、每一条依赖库的噪声日志都往里写，而且不停地 insert + prune，造成严重的写放大。</p><p>社区实测（<a href="https://github.com/openai/codex/issues/28224">issue #28224</a>）：</p><ul><li>一台机器连续跑 21 天，向磁盘写了约 <strong>37 TB</strong>，折算下来一年约 <strong>640 TB</strong>。</li><li>消费级 NVMe SSD 的标称写入寿命（TBW）通常只有 600 TB 左右——<strong>理论上不到一年就能把一块盘的寿命写穿</strong>。</li><li>还带两个连锁故障：<code>logs_2.sqlite</code> 超过约 200MB 时 CLI 会以 SIGTRAP 崩溃（<a href="https://github.com/openai/codex/issues/29237">#29237</a>）；<code>logs_2.sqlite-wal</code> 能无上限涨到几十 GB（<a href="https://github.com/openai/codex/issues/28997">#28997</a>）。</li></ul><p><strong>0.142.0（2026-06-22 发布）带来了部分修复</strong>：两个 PR——<a href="https://github.com/openai/codex/pull/29432">#29432</a>（停掉逐条 WebSocket 事件日志）和 <a href="https://github.com/openai/codex/pull/29457">#29457</a>（过滤噪声 target）——按 issue 估算能砍掉约 85% 的日志写入，纯命令行场景改善明显。</p><blockquote><p><strong>但它没有完全修好。</strong> 实测在 <strong>macOS + Codex 桌面 App（app-server）</strong> 路径下，即使内嵌 CLI 已经是 0.142.0，<code>TRACE target=log</code> 的高频写入依然存在——60 秒采样里 <code>max(id)</code> 还在前进 1400~1700 行，多人复现（<a href="https://github.com/openai/codex/issues/29532">#29532</a>，OPEN）。所以本篇不只教你升级，还给你<strong>可靠检测 + 清理 + 在线止血</strong>的整套兜底。</p></blockquote><p>还有一点：升级只能<strong>止住未来的写入</strong>，已经被写爆的旧日志不会自动回收。完整处置流程是 <strong>升级 → 真正确认停没停 → 没停就清理 &#x2F; trigger 止血</strong>。</p><h2 id="一、确认自己中没中招"><a href="#一、确认自己中没中招" class="headerlink" title="一、确认自己中没中招"></a>一、确认自己中没中招</h2><p>先看日志文件有多大，重点盯 <code>-wal</code> 那个：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> -lah ~/.codex/logs_2.sqlite*</span><br><span class="line"><span class="built_in">du</span> -sh ~/.codex/</span><br></pre></td></tr></table></figure><p>如果 <code>logs_2.sqlite-wal</code> 已经是几百 MB 甚至 GB 级，基本可以确定你正被这个 bug 持续写盘。</p><blockquote><p><strong>光看文件大小会骗你。</strong> insert + prune 的 churn 会让 <code>-wal</code> 稳定在几 MB、<code>logs_2.sqlite</code> 体积也不怎么涨，看着没事，底层却一直在写（<a href="https://github.com/openai/codex/issues/29532">#29532</a> 实测）。更靠谱的判定是隔 60 秒采两次行号，看它还涨不涨：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sqlite3 ~/.codex/logs_2.sqlite <span class="string">&quot;SELECT MAX(id) FROM logs;&quot;</span></span><br><span class="line"><span class="comment"># 等 60 秒再跑一次；数值仍在大幅前进（几百上千）= 正在持续写盘</span></span><br></pre></td></tr></table></figure></blockquote><p>Windows 路径是 <code>%USERPROFILE%\.codex\</code>，直接在资源管理器里看这三个 <code>logs_2.sqlite*</code> 文件的大小即可。</p><h2 id="二、升级到-0-142-0"><a href="#二、升级到-0-142-0" class="headerlink" title="二、升级到 0.142.0"></a>二、升级到 0.142.0</h2><p>两种方式任选其一。</p><p><strong>方式一：npm 手动升级</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install -g @openai/codex@latest</span><br></pre></td></tr></table></figure><p><code>@latest</code> 当前解析到的就是 <code>0.142.0</code>，已经包含 <a href="https://github.com/openai/codex/pull/29432">#29432</a> &#x2F; <a href="https://github.com/openai/codex/pull/29457">#29457</a> 这两个修复。</p><p>升级完确认版本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">codex --version</span><br></pre></td></tr></table></figure><p><strong>方式二：让 AI 代办</strong></p><p>如果你装了 Claude Code，把这句话丢给它，让它联网搜索并完成升级：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">search web &amp; update codex cli</span><br></pre></td></tr></table></figure><blockquote><p>必须是 <code>0.142.0</code> 或更高才带修复。<code>0.141.x</code> 及更早版本仍会疯狂写盘——重装同一个旧版本没用，认准版本号。升级后<strong>务必回第一步用 <code>max(id)</code> 采样复测</strong>，因为 macOS App 路径上它可能还没停（见下文）。</p></blockquote><h2 id="三、清理已经被写爆的旧日志"><a href="#三、清理已经被写爆的旧日志" class="headerlink" title="三、清理已经被写爆的旧日志"></a>三、清理已经被写爆的旧日志</h2><blockquote><p>动手前<strong>先完全退出 Codex</strong>（命令行会话和桌面 App 都关掉）。文件还被占用时删不干净，WAL 也可能立刻重建。</p></blockquote><p>这个日志库<strong>不含任何对话内容或配置</strong>，纯粹是诊断日志，删掉是安全的——Codex 下次启动会自动重建一个干净的小文件。</p><p><strong>macOS &#x2F; Linux：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">rm</span> ~/.codex/logs_2.sqlite ~/.codex/logs_2.sqlite-wal ~/.codex/logs_2.sqlite-shm</span><br></pre></td></tr></table></figure><p>或者一行通配搞定：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">rm</span> ~/.codex/logs_2.sqlite*</span><br></pre></td></tr></table></figure><p><strong>Windows（PowerShell）：</strong></p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Remove-Item</span> <span class="string">&quot;<span class="variable">$env:USERPROFILE</span>\.codex\logs_2.sqlite*&quot;</span></span><br></pre></td></tr></table></figure><h2 id="四、验证修复生效"><a href="#四、验证修复生效" class="headerlink" title="四、验证修复生效"></a>四、验证修复生效</h2><ol><li>版本号到位：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">codex --version</span><br><span class="line"><span class="comment"># 期望输出 0.142.0 或更高</span></span><br></pre></td></tr></table></figure><ol start="2"><li><strong>别只看文件大小</strong>，按第一步那招采样 <code>max(id)</code>，确认写入真的停了：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sqlite3 ~/.codex/logs_2.sqlite <span class="string">&quot;SELECT MAX(id) FROM logs;&quot;</span></span><br><span class="line"><span class="comment"># 隔 60 秒两次，数值基本不动 = 写入真停了</span></span><br></pre></td></tr></table></figure><p>3.（进阶，Linux）直接看 SSD 实际写入量。用 <code>smartctl</code> 对比挂机前后的 <code>Data Units Written</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> smartctl -a /dev/nvme0 | grep <span class="string">&quot;Data Units Written&quot;</span></span><br></pre></td></tr></table></figure><p>挂机一小时前后做差值，正常情况下增量应该很小，而不是之前那种几 GB&#x2F;小时的量级。</p><blockquote><p>如果你用的是 <strong>macOS + Codex 桌面 App</strong>，升到 0.142.0 后 <code>max(id)</code> 很可能<strong>仍在涨</strong>——这是已知的部分修复缺口（<a href="https://github.com/openai/codex/issues/29532">#29532</a>）。这时别干等，直接上「进阶」里的 trigger 在线止血。</p></blockquote><h2 id="五、进阶：用-SQLite-Trigger-在线止血"><a href="#五、进阶：用-SQLite-Trigger-在线止血" class="headerlink" title="五、进阶：用 SQLite Trigger 在线止血"></a>五、进阶：用 SQLite Trigger 在线止血</h2><p>如果你不想删库、又想立刻止住写盘（尤其是升级到 0.142.0 后、在 macOS App 上仍没停的情况），可以用 SQLite trigger 拦截 <code>logs</code> 表的 insert。日志全落在一张名为 <code>logs</code> 的表里（主键 <code>id</code>），所以能精确处理。这也正是 0.142.0 之后社区在 macOS App 上仍在用的兜底方案（<a href="https://github.com/openai/codex/issues/29532">#29532</a> 评论里多人在用同名 <code>block_log_inserts</code> trigger）。</p><h3 id="1-确诊：TRACE-是不是元凶"><a href="#1-确诊：TRACE-是不是元凶" class="headerlink" title="1. 确诊：TRACE 是不是元凶"></a>1. 确诊：TRACE 是不是元凶</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sqlite3 ~/.codex/logs_2.sqlite <span class="string">&quot;SELECT level, COUNT(*) FROM logs GROUP BY level ORDER BY COUNT(*) DESC;&quot;</span></span><br></pre></td></tr></table></figure><p>实测里 TRACE 能占到五到七成行数，大头是 <code>target=log</code> 的 websocket &#x2F; tungstenite 噪声。再隔 30 秒采两次 <code>MAX(id)</code>，看还在不在涨：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sqlite3 ~/.codex/logs_2.sqlite <span class="string">&quot;SELECT MAX(id) FROM logs;&quot;</span></span><br><span class="line"><span class="built_in">ls</span> -l ~/.codex/logs_2.sqlite-wal</span><br><span class="line"><span class="comment"># 等 30 秒，重复上面两条</span></span><br></pre></td></tr></table></figure><p><code>id</code> 一次响应能跳几千、或每分钟稳定涨一两千，就是正在被持续写盘。</p><blockquote><p>别想着用 <code>RUST_LOG=info</code> 来压——实测设了它，<code>TRACE</code> &#x2F; <code>DEBUG</code> 照样落进 SQLite（<a href="https://github.com/openai/codex/issues/29532">#29532</a>）。这条持久化路径不吃环境变量的过滤。</p></blockquote><h3 id="2-先备份"><a href="#2-先备份" class="headerlink" title="2. 先备份"></a>2. 先备份</h3><p>即使 Codex 在跑也能做一致性备份：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sqlite3 ~/.codex/logs_2.sqlite <span class="string">&quot;.backup &#x27;<span class="variable">$HOME</span>/.codex/logs_2.sqlite.bak&#x27;&quot;</span></span><br></pre></td></tr></table></figure><h3 id="3-用-trigger-拦住-logs-表的-insert"><a href="#3-用-trigger-拦住-logs-表的-insert" class="headerlink" title="3. 用 trigger 拦住 logs 表的 insert"></a>3. 用 trigger 拦住 logs 表的 insert</h3><blockquote><p>拦普通表的 insert 要用 <code>BEFORE INSERT</code> + <code>RAISE(IGNORE)</code>，<strong>不是</strong> <code>INSTEAD OF</code>（那个只能建在视图上，照抄会报错）。</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sqlite3 ~/.codex/logs_2.sqlite <span class="string">&quot;CREATE TRIGGER IF NOT EXISTS block_log_inserts BEFORE INSERT ON logs BEGIN SELECT RAISE(IGNORE); END;&quot;</span></span><br></pre></td></tr></table></figure><p>之后所有往 <code>logs</code> 表的写入都会被静默忽略（不报错），从源头止住。trigger 是库级的，对 Codex 已经打开的连接也会即时生效。</p><h3 id="4-Checkpoint-并截断-WAL，把空间收回来"><a href="#4-Checkpoint-并截断-WAL，把空间收回来" class="headerlink" title="4. Checkpoint 并截断 WAL，把空间收回来"></a>4. Checkpoint 并截断 WAL，把空间收回来</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sqlite3 ~/.codex/logs_2.sqlite <span class="string">&quot;PRAGMA wal_checkpoint(TRUNCATE);&quot;</span></span><br></pre></td></tr></table></figure><p>返回第一列是 <code>0</code> 表示截断成功；如果是 <code>1</code>（busy），说明 Codex 还占着连接，彻底退出 Codex 再跑一次。</p><h3 id="5-采样确认彻底不涨了"><a href="#5-采样确认彻底不涨了" class="headerlink" title="5. 采样确认彻底不涨了"></a>5. 采样确认彻底不涨了</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sqlite3 ~/.codex/logs_2.sqlite <span class="string">&quot;SELECT MAX(id) FROM logs;&quot;</span>   <span class="comment"># 隔 30 秒两次，数值应不变</span></span><br><span class="line"><span class="built_in">ls</span> -l ~/.codex/logs_2.sqlite-wal                              <span class="comment"># 应停在很小，不再增长</span></span><br></pre></td></tr></table></figure><p><code>MAX(id)</code> 钉死不动、<code>-wal</code> 不再变大，就说明止血成功。</p><blockquote><p>这是侵入式的临时手段，<strong>升级到 0.142.0 仍是第一步</strong>（它确实砍掉了一大块）。等官方把 macOS App 的 <code>target=log</code> 缺口也补上后，记得把 trigger 删掉还原，否则诊断日志会一直写不进去：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sqlite3 ~/.codex/logs_2.sqlite <span class="string">&quot;DROP TRIGGER IF EXISTS block_log_inserts;&quot;</span></span><br></pre></td></tr></table></figure><p>另外 trigger 只对 <code>logs</code> 表下手，别对 <code>state_5.sqlite</code> 等状态库这么干。</p></blockquote><h3 id="让-AI-代跑整套流程"><a href="#让-AI-代跑整套流程" class="headerlink" title="让 AI 代跑整套流程"></a>让 AI 代跑整套流程</h3><p>如果你用的是 Claude Code 或 Codex 自己，把这段丢给它，让它代跑上面的步骤：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">排查 ~/.codex/logs_2.sqlite 是否因 TRACE 日志持续高频写盘：</span><br><span class="line">1) SELECT level, COUNT(*) FROM logs GROUP BY level ORDER BY COUNT(*) DESC; 看 TRACE 是否占大头</span><br><span class="line">2) 隔 30 秒采两次 SELECT MAX(id) FROM logs; 和 logs_2.sqlite-wal 文件大小，确认是否还在涨</span><br><span class="line">中招的话：先用 .backup 备份 → 建 BEFORE INSERT ... RAISE(IGNORE) trigger 拦 logs 表 insert →</span><br><span class="line">PRAGMA wal_checkpoint(TRUNCATE) 回收 WAL → 再采样确认 MAX(id) 和 WAL 都不再增长。</span><br></pre></td></tr></table></figure><h2 id="桌面-App-用户特别注意事项"><a href="#桌面-App-用户特别注意事项" class="headerlink" title="桌面 App 用户特别注意事项"></a>桌面 App 用户特别注意事项</h2><p>如果你用的是 Codex 桌面 App 而非命令行，注意以下几点：</p><ul><li>App 走<strong>独立版本线</strong>（最新 <code>26.616</code>，2026-06-18 发布，早于 CLI 的 0.142.0 修复），内嵌自己管理的引擎——<strong>所以 <code>npm</code> 升级 CLI 不一定能让 App 用上这个修复</strong>。</li><li><strong>用完彻底退出 App</strong>（不是最小化）。App 挂后台跑长任务会持续写盘，退出能立刻止血。</li><li>需要时<strong>改用已修复的 CLI（0.142.0）干活</strong>，功能和 App 高度重叠，是绕开「App 没修」最干净的办法。</li><li>清理时**只删 <code>logs_2.sqlite*</code>**，别碰 <code>state_5.sqlite</code> 等 state &#x2F; memories &#x2F; goals 库——那些含会话状态，删了 App 可能起不来（<a href="https://github.com/openai/codex/issues/24030">#24030</a>）。</li><li>盯官方更新，等版本号高于 <code>26.616</code>、且写明减少磁盘写入的 App 版本。</li></ul><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><p><strong>Q：升级完为什么磁盘空间没回来？</strong><br>升级只停掉<strong>未来</strong>的写入，已经占用的空间要手动清（见第三步）。</p><p><strong>Q：我已经升到 0.142.0，怎么还在写盘？</strong><br>因为 0.142.0 只是<strong>部分修复</strong>。实测在 macOS + Codex 桌面 App（app-server）路径下，<code>TRACE target=log</code> 的高频写入仍未根治（<a href="https://github.com/openai/codex/issues/29532">#29532</a>，OPEN，多人复现）。兜底两招：用「进阶」里的 trigger 在线止住 <code>logs</code> 表 insert；别指望 <code>RUST_LOG=info</code>，实测设了照样落库。剩下的盯后续版本。</p><p><strong>Q：删掉 <code>logs_2.sqlite</code> 会丢对话记录或配置吗？</strong><br>不会。它只是诊断日志，会话历史和配置都在别的文件里，不受影响。</p><p><strong>Q：暂时升不了级怎么办？</strong><br>如果暂时升不了级，可以把日志重定向到内存盘（tmpfs），让写入落在内存、重启即清，完全不碰 SSD。思路是把 <code>logs_2.sqlite*</code> 指到 <code>tmpfs</code>（如 Linux 的 <code>/dev/shm</code>、<code>/tmp</code>）。日志不含数据，重启丢失无所谓。Windows 没有等价的简单方案，建议直接走升级 + 清理。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/codex-cli-disk-write-fix/</id>
    <link href="https://www.tenxiaodao.top/codex-cli-disk-write-fix/"/>
    <published>2026-06-23T16:50:00.000Z</published>
    <summary>
      <![CDATA[<h1 id="Codex-CLI-疯狂写盘自救指南"><a href="#Codex-CLI-疯狂写盘自救指南" class="headerlink" title="Codex CLI 疯狂写盘自救指南"></a>Codex CLI 疯狂写盘自救指南</h1><p>最近 Ope]]>
    </summary>
    <title>Codex CLI 疯狂写盘自救指南</title>
    <updated>2026-06-23T16:50:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="AI 探索" scheme="https://www.tenxiaodao.top/categories/AI-%E6%8E%A2%E7%B4%A2/"/>
    <category term="AI" scheme="https://www.tenxiaodao.top/tags/AI/"/>
    <category term="微信" scheme="https://www.tenxiaodao.top/tags/%E5%BE%AE%E4%BF%A1/"/>
    <category term="OpenClaw" scheme="https://www.tenxiaodao.top/tags/OpenClaw/"/>
    <category term="OECT" scheme="https://www.tenxiaodao.top/tags/OECT/"/>
    <category term="部署" scheme="https://www.tenxiaodao.top/tags/%E9%83%A8%E7%BD%B2/"/>
    <content>
      <![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>之前折腾过 KouriChat 把 AI 接入微信，这次换了个方向——在 OECT 上部署 OpenClaw，把它变成一个全能助手，并通过微信直接跟它对话。效果出乎意料地好，遂整理一文分享完整流程。</p><p>OpenClaw 不是单纯的聊天机器人，它具备真实操作能力：能搜网页、读写文件、执行命令、设提醒、记偏好，还能扩展技能包。搭配微信使用后，相当于手机里多了一个随时在线的私人助理。</p><hr><h2 id="OpenClaw-是什么"><a href="#OpenClaw-是什么" class="headerlink" title="OpenClaw 是什么"></a>OpenClaw 是什么</h2><p>OpenClaw 是一个可自部署的 AI 助手框架，核心能力可以概括为三层：</p><table><thead><tr><th>层次</th><th>能力</th></tr></thead><tbody><tr><td>信息获取</td><td>搜索网页、读取文章&#x2F;文档内容</td></tr><tr><td>文件与命令</td><td>读取&#x2F;编辑&#x2F;创建文件、列出目录、执行 Shell 命令、交互式终端</td></tr><tr><td>智能与扩展</td><td>长期记忆、技能扩展、定时任务、消息推送、专项工具（天气&#x2F;GitHub&#x2F;浏览器自动化等）</td></tr></tbody></table><p>它的定位介于纯聊天 AI 和自动化运维工具之间——既理解自然语言，又能真去执行任务。</p><hr><h2 id="OECT-平台介绍"><a href="#OECT-平台介绍" class="headerlink" title="OECT 平台介绍"></a>OECT 平台介绍</h2><p>OECT 是一个轻量级云部署平台，适合运行个人服务和应用。相比直接买 VPS 从头搭环境，OECT 提供了更便利的部署体验：</p><ul><li>环境即开即用，无需手动配置系统</li><li>支持 Docker 化部署，隔离性好</li><li>提供公网访问能力，方便对接微信等服务</li><li>资源开销低，个人使用成本可控</li></ul><p>把 OpenClaw 部署在 OECT 上，既能保证服务稳定在线，又不需要维护物理设备。</p><hr><h2 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h2><p>开始前需要准备以下内容：</p><ol><li><strong>OECT 账号</strong> — 注册并登录 OECT 平台，建议先熟悉控制台的基本操作</li><li><strong>OpenClaw 部署包</strong> — 从官方渠道获取（GitHub Release 或镜像仓库）</li><li><strong>微信账号</strong> — 建议使用小号进行测试，避免对主号造成影响</li><li><strong>基础网络知识</strong> — 了解端口、域名、API 的基本概念即可</li></ol><hr><h2 id="部署步骤"><a href="#部署步骤" class="headerlink" title="部署步骤"></a>部署步骤</h2><h3 id="1-在-OECT-上创建服务"><a href="#1-在-OECT-上创建服务" class="headerlink" title="1. 在 OECT 上创建服务"></a>1. 在 OECT 上创建服务</h3><p>登录 OECT 控制台，创建一个新的服务实例：</p><ol><li>选择部署方式：推荐使用 Docker 镜像部署</li><li>配置资源规格：建议至少 1 核 2GB 内存（基础运行够用）</li><li>设置端口映射：将 OpenClaw 的服务端口（例如 8080）映射到公网</li><li>绑定域名（可选）：方便后续配置 Webhook 和 API 回调</li></ol><h3 id="2-启动-OpenClaw"><a href="#2-启动-OpenClaw" class="headerlink" title="2. 启动 OpenClaw"></a>2. 启动 OpenClaw</h3><p>服务实例就绪后，通过 SSH 或控制台终端进入环境，拉取并启动 OpenClaw：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 拉取 OpenClaw 镜像（以 Docker 方式为例）</span></span><br><span class="line">docker pull openclaw/server:latest</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动容器</span></span><br><span class="line">docker run -d \</span><br><span class="line">  --name openclaw \</span><br><span class="line">  -p 8080:8080 \</span><br><span class="line">  -v openclaw-data:/app/data \</span><br><span class="line">  -e OPENCLAW_API_KEY=your_api_key \</span><br><span class="line">  openclaw/server:latest</span><br></pre></td></tr></table></figure><p>启动后，访问 <code>http://你的地址:8080</code> 确认服务运行正常。看到 OpenClaw 的管理界面即表示部署成功。</p><h3 id="3-初始化配置"><a href="#3-初始化配置" class="headerlink" title="3. 初始化配置"></a>3. 初始化配置</h3><p>首次访问管理界面后，需要完成基础配置：</p><ol><li>设置管理员密码</li><li>配置 AI 模型接入（支持 OpenAI、DeepSeek 等主流 API）</li><li>开启需要的功能模块（搜索、文件操作、命令执行等）</li><li>配置长期记忆存储（默认使用本地文件，也可对接外部数据库）</li></ol><p>![OpenClaw 管理界面]</p><hr><h2 id="接入微信"><a href="#接入微信" class="headerlink" title="接入微信"></a>接入微信</h2><p>这是最核心的一步——让 OpenClaw 能通过微信接收和回复消息。</p><h3 id="方式一：微信公众号（推荐）"><a href="#方式一：微信公众号（推荐）" class="headerlink" title="方式一：微信公众号（推荐）"></a>方式一：微信公众号（推荐）</h3><p>如果你有自己的公众号（个人订阅号即可），可以通过公众号的开发者模式对接：</p><ol><li>在公众号后台开启开发者模式，配置服务器地址为 <code>https://你的域名/openclaw/wechat</code></li><li>在 OpenClaw 管理界面中启用「微信对接」功能</li><li>填入公众号的 AppID 和 AppSecret</li><li>设置消息加密方式（推荐安全模式）</li><li>保存并验证连通性</li></ol><p>配置完成后，粉丝在公众号发消息，OpenClaw 会自动处理并回复。</p><h3 id="方式二：个人微信（通过中间件）"><a href="#方式二：个人微信（通过中间件）" class="headerlink" title="方式二：个人微信（通过中间件）"></a>方式二：个人微信（通过中间件）</h3><p>如果想让 OpenClaw 直接跟你的个人微信对话，需要额外的中间件桥接：</p><ol><li>在 OECT 上部署微信桥接服务（如 wechat-bridge 或类似方案）</li><li>配置桥接服务指向 OpenClaw 的 API 地址</li><li>使用小号扫码登录</li><li>在 OpenClaw 中设置监听对象（按昵称或备注过滤）</li></ol><blockquote><p><strong>注意：</strong> 个人微信接入存在风控风险，请务必使用小号测试，且仅用于可控环境。</p></blockquote><hr><h2 id="核心能力详解"><a href="#核心能力详解" class="headerlink" title="核心能力详解"></a>核心能力详解</h2><p>部署 + 接入微信之后，OpenClaw 的能力基本全部可以通过微信对话调用了。说几个我日常用得最多的场景：</p><h3 id="信息搜索"><a href="#信息搜索" class="headerlink" title="信息搜索"></a>信息搜索</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">我：帮我搜一下这周上海的天气</span><br><span class="line">OpenClaw：本周上海天气如下……</span><br></pre></td></tr></table></figure><p>OpenClaw 会主动搜索网页、整合信息后返回，相当于微信里内置了一个搜索引擎。</p><h3 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">我：帮我创建一个备忘录文件，记录明天要买的东西</span><br><span class="line">OpenClaw：已创建文件 memo-0622.md，内容是待办清单……</span><br></pre></td></tr></table></figure><p>也可以让它读取已有文件、修改内容或列出目录。</p><h3 id="命令执行"><a href="#命令执行" class="headerlink" title="命令执行"></a>命令执行</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">我：看看服务器现在的负载情况</span><br><span class="line">OpenClaw：当前 CPU 占用 23%，内存占用 1.2GB/4GB……</span><br></pre></td></tr></table></figure><p>这对运维场景特别有用——人在外面，手机发条消息就能查服务器状态。</p><h3 id="定时任务"><a href="#定时任务" class="headerlink" title="定时任务"></a>定时任务</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">我：每天上午九点提醒我打卡</span><br><span class="line">OpenClaw：已设置定时提醒，每天 09:00 推送。</span><br></pre></td></tr></table></figure><p>提醒会通过微信直接发到你手机上。</p><h3 id="技能扩展"><a href="#技能扩展" class="headerlink" title="技能扩展"></a>技能扩展</h3><p>如果开启了 GitHub、浏览器自动化等技能，可以做到更复杂的事情：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">我：看看我的 GitHub 有没有新的 Issue</span><br><span class="line">我：帮我截图这个网页，存到服务器上</span><br></pre></td></tr></table></figure><hr><h2 id="安全与合规提醒"><a href="#安全与合规提醒" class="headerlink" title="安全与合规提醒"></a>安全与合规提醒</h2><p>把 AI 助手接入微信带来便利的同时，也有一些需要留意的地方：</p><ul><li><strong>API Key 安全</strong>：OpenClaw 使用的 AI 模型 API Key 请妥善保管，不要在对话中泄露</li><li><strong>账号风险</strong>：个人微信接入方案存在风控可能，始终用小号测试</li><li><strong>命令权限</strong>：建议限制 OpenClaw 可执行的命令范围，避免误操作</li><li><strong>数据隐私</strong>：对话内容可能会经过第三方 API（如 AI 模型服务），避免输入敏感信息</li><li><strong>公网暴露</strong>：如果 OpenClaw 直接暴露在公网上，建议配置 IP 白名单或额外鉴权</li></ul><hr><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><h3 id="服务启动后无法访问"><a href="#服务启动后无法访问" class="headerlink" title="服务启动后无法访问"></a>服务启动后无法访问</h3><ul><li>检查 OECT 上的端口映射是否配置正确</li><li>确认防火墙规则是否放行了对应端口</li><li>查看容器日志定位具体错误：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker logs openclaw --<span class="built_in">tail</span> 50</span><br></pre></td></tr></table></figure></li></ul><h3 id="微信消息收不到回复"><a href="#微信消息收不到回复" class="headerlink" title="微信消息收不到回复"></a>微信消息收不到回复</h3><ul><li>检查微信公众号的服务器配置是否保存成功</li><li>确认 OpenClaw 中微信对接功能已启用</li><li>查看 OpenClaw 的日志，看消息是否到达了服务端</li><li>如果是个人微信方案，检查桥接服务是否在线</li></ul><h3 id="AI-回复质量不理想"><a href="#AI-回复质量不理想" class="headerlink" title="AI 回复质量不理想"></a>AI 回复质量不理想</h3><ul><li>检查使用的模型 API 是否正确配置</li><li>尝试切换更强的模型（如 GPT-4 或 Claude 系列）</li><li>可以在 OpenClaw 中设置系统提示词来约束回复风格</li></ul><h3 id="定时任务不触发"><a href="#定时任务不触发" class="headerlink" title="定时任务不触发"></a>定时任务不触发</h3><ul><li>确认 OECT 实例的时区配置正确</li><li>检查定时任务的表达格式（cron 表达式）</li><li>查看任务执行日志，确认是否因错误中止</li></ul><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在 OECT 上部署 OpenClaw 并接入微信后，我的日常效率提升了不少。最常用的几个场景——搜信息、看服务器、设提醒——原本要在电脑上操作或者打开好几个 App，现在微信里一句话就搞定了。</p><p>从之前的 AI 女友到这次的 OpenClaw，本质上都是在探索”AI 如何真正介入日常生活”这个命题。KouriChat 偏向陪伴和对话，OpenClaw 偏向工具和操作，各有各的适用场景。如果你也在折腾 AI 落地的方向，不管是从对话切入还是从工具切入，都值得试一试。</p><p>后续可以研究的方向：</p><ul><li>把 OpenClaw 接入飞书&#x2F;Slack&#x2F;Telegram 等多平台</li><li>用技能扩展对接更多的外部服务</li><li>试试本地大模型替代云 API，实现完全本地化运行</li></ul><p>有什么问题欢迎留言交流。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/deploy-openclaw-on-oect-with-wechat/</id>
    <link href="https://www.tenxiaodao.top/deploy-openclaw-on-oect-with-wechat/"/>
    <published>2026-06-22T14:00:00.000Z</published>
    <summary>本文记录在 OECT 平台上部署 OpenClaw AI 助手，并通过微信公众号/个人微信与其交互的完整流程，含能力介绍、部署配置与使用技巧。</summary>
    <title>在 OECT 上部署 OpenClaw 并接入微信</title>
    <updated>2026-06-22T15:52:32.382Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="技术笔记" scheme="https://www.tenxiaodao.top/categories/%E6%8A%80%E6%9C%AF%E7%AC%94%E8%AE%B0/"/>
    <category term="Python" scheme="https://www.tenxiaodao.top/tags/Python/"/>
    <category term="前端性能" scheme="https://www.tenxiaodao.top/tags/%E5%89%8D%E7%AB%AF%E6%80%A7%E8%83%BD/"/>
    <category term="WebP" scheme="https://www.tenxiaodao.top/tags/WebP/"/>
    <category term="图片优化" scheme="https://www.tenxiaodao.top/tags/%E5%9B%BE%E7%89%87%E4%BC%98%E5%8C%96/"/>
    <category term="效率工具" scheme="https://www.tenxiaodao.top/tags/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    <content>
      <![CDATA[<blockquote><p>WebP 是 Google 推出的一种现代图片格式，在同等画质下体积比 JPEG 小 25%-35%，比 PNG 小得更多。对于博客网站和前端项目来说，全量切到 WebP 能显著减少页面加载速度。</p></blockquote><p>周末花了点时间写了一个 Python 脚本 <code>convert2webp</code>，解决一个一直懒得动手的需求：<strong>批量把图片文件夹里的各种格式拖进去，一键转成 WebP，顺便看看能省多少空间。</strong></p><hr><h2 id="为什么要用-WebP"><a href="#为什么要用-WebP" class="headerlink" title="为什么要用 WebP"></a>为什么要用 WebP</h2><p>在介绍脚本之前，先简单说说为什么值得花时间做这件事：</p><table><thead><tr><th>对比项</th><th>PNG</th><th>JPEG</th><th>WebP</th></tr></thead><tbody><tr><td>无损压缩</td><td>支持</td><td>不支持</td><td>支持，比 PNG 小 26%</td></tr><tr><td>有损压缩</td><td>—</td><td>基准</td><td>比 JPEG 小 25-35%</td></tr><tr><td>透明度</td><td>支持</td><td>不支持</td><td>支持</td></tr><tr><td>动图</td><td>—</td><td>—</td><td>支持（替代 GIF）</td></tr></tbody></table><p>根据 Google 的官方数据，WebP 无损图片比 PNG 体积小 26%，有损图片比同等 SSIM 质量的 JPEG 小 25-35%。对于流量来自移动端的场景，省下来的每一 KB 都在加快首屏速度。</p><p><strong>浏览器兼容性</strong>方面，主流现代浏览器（Chrome、Firefox、Edge、Safari 14+）都已原生支持 WebP。如果你需要兼容老旧浏览器，可以用 <code>&lt;picture&gt;</code> 标签做降级兜底（不过个人博客就不太需要操这个心了）：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">picture</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">source</span> <span class="attr">srcset</span>=<span class="string">&quot;image.webp&quot;</span> <span class="attr">type</span>=<span class="string">&quot;image/webp&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">&quot;image.png&quot;</span> <span class="attr">alt</span>=<span class="string">&quot;fallback&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">picture</span>&gt;</span></span><br></pre></td></tr></table></figure><hr><h2 id="convert2webp-脚本"><a href="#convert2webp-脚本" class="headerlink" title="convert2webp 脚本"></a>convert2webp 脚本</h2><p>这个脚本的设计目标很朴素：<strong>丢到图片文件夹里双击，完事。</strong></p><h3 id="使用方式"><a href="#使用方式" class="headerlink" title="使用方式"></a>使用方式</h3><p>脚本只有一个文件，依赖 Python 3，首次运行会自动安装 Pillow，不需要手动装任何东西。</p><p>最简单的用法——直接命令行跑：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">python3 convert2webp.py</span><br></pre></td></tr></table></figure><p>不过最舒服的用法还是把脚本丢到图片文件夹里双击：</p><p><strong>macOS</strong> —— 用附带的 <code>Convert2WebP.command</code>（chmod +x 后双击）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">cd</span> <span class="string">&quot;<span class="subst">$(dirname <span class="string">&quot;<span class="variable">$0</span>&quot;</span>)</span>&quot;</span></span><br><span class="line">python3 convert2webp.py</span><br></pre></td></tr></table></figure><p><strong>Windows</strong> —— 用附带的 <code>Convert2WebP.bat</code>（双击）：</p><figure class="highlight bat"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">@<span class="built_in">echo</span> off</span><br><span class="line"><span class="built_in">cd</span> /d &quot;%~dp0&quot;</span><br><span class="line">python convert2webp.py</span><br><span class="line"><span class="built_in">pause</span></span><br></pre></td></tr></table></figure><p>运行之后，脚本会扫描当前目录下所有常见格式的图片，在下方新建一个 <code>webp/</code> 文件夹存放转换结果，并在终端打印每张图的压缩率：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">==================================================</span><br><span class="line">  🖼️  批量图片转 WebP 工具</span><br><span class="line">==================================================</span><br><span class="line">📁 工作目录: /Users/yuhaozhe/Pictures/blog-assets</span><br><span class="line">⚙️  质量: 85 | 模式: 有损</span><br><span class="line"></span><br><span class="line">🔍 找到 12 张图片</span><br><span class="line"></span><br><span class="line">  [1/12] ✅ hero.png  120KB → 32KB (-73.3%)</span><br><span class="line">  [2/12] ✅ screenshot.jpg  340KB → 68KB (-80.0%)</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">==================================================</span><br><span class="line">  ✅ 成功: 12/12</span><br><span class="line">  💾 总大小: 2.3MB → 0.5MB (-78.3%)</span><br><span class="line">📂 输出目录: /Users/yuhaozhe/Pictures/blog-assets/webp</span><br><span class="line">==================================================</span><br></pre></td></tr></table></figure><h3 id="功能特性"><a href="#功能特性" class="headerlink" title="功能特性"></a>功能特性</h3><ul><li><strong>自动安装依赖</strong> —— 第一次运行自动 pip install Pillow，到手即用</li><li><strong>支持超多格式</strong> —— PNG、JPG、JPEG、BMP、TIFF、GIF、ICO 全都能吃</li><li><strong>保留原文件</strong> —— 输出全部丢到 <code>webp/</code> 子目录，不动原始图片</li><li><strong>文件名防冲突</strong> —— 如果 <code>photo.webp</code> 已存在，自动命名为 <code>photo_1.webp</code>、<code>photo_2.webp</code></li><li><strong>动图处理</strong> —— 遇到 GIF 只取第一帧，避免动图转成静态 WebP 后体积异常</li><li><strong>透明通道保留</strong> —— RGBA 图片正常处理，不会丢掉透明度</li><li><strong>详细日志</strong> —— 每张图显示压缩前后大小和压缩百分比，最后还有汇总</li></ul><h3 id="核心代码"><a href="#核心代码" class="headerlink" title="核心代码"></a>核心代码</h3><p>脚本不长，两百行不到，核心转换逻辑就这些：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> PIL <span class="keyword">import</span> Image</span><br><span class="line"></span><br><span class="line">SUPPORTED_EXT = &#123;<span class="string">&#x27;.png&#x27;</span>, <span class="string">&#x27;.jpg&#x27;</span>, <span class="string">&#x27;.jpeg&#x27;</span>, <span class="string">&#x27;.bmp&#x27;</span>, <span class="string">&#x27;.tiff&#x27;</span>, <span class="string">&#x27;.tif&#x27;</span>, <span class="string">&#x27;.gif&#x27;</span>, <span class="string">&#x27;.ico&#x27;</span>, <span class="string">&#x27;.webp&#x27;</span>&#125;</span><br><span class="line">OUTPUT_DIR = <span class="string">&#x27;webp&#x27;</span></span><br><span class="line">QUALITY = <span class="number">85</span></span><br><span class="line">LOSSLESS = <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">convert_to_webp</span>(<span class="params">src_path, dst_path</span>):</span><br><span class="line">    img = Image.<span class="built_in">open</span>(src_path)</span><br><span class="line">    <span class="comment"># 处理 GIF 动图只取第一帧</span></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">getattr</span>(img, <span class="string">&quot;is_animated&quot;</span>, <span class="literal">False</span>):</span><br><span class="line">        img = img.copy()</span><br><span class="line">    <span class="comment"># 统一颜色模式</span></span><br><span class="line">    <span class="keyword">if</span> img.mode <span class="keyword">in</span> (<span class="string">&#x27;RGBA&#x27;</span>, <span class="string">&#x27;LA&#x27;</span>, <span class="string">&#x27;PA&#x27;</span>):</span><br><span class="line">        img = img.convert(<span class="string">&#x27;RGBA&#x27;</span>)</span><br><span class="line">    <span class="keyword">elif</span> img.mode == <span class="string">&#x27;P&#x27;</span>:</span><br><span class="line">        img = img.convert(<span class="string">&#x27;RGBA&#x27;</span>)</span><br><span class="line">    <span class="keyword">elif</span> img.mode <span class="keyword">not</span> <span class="keyword">in</span> (<span class="string">&#x27;RGB&#x27;</span>, <span class="string">&#x27;L&#x27;</span>):</span><br><span class="line">        img = img.convert(<span class="string">&#x27;RGB&#x27;</span>)</span><br><span class="line">    <span class="comment"># 保存为 WebP</span></span><br><span class="line">    img.save(dst_path, <span class="string">&#x27;webp&#x27;</span>, quality=QUALITY, lossless=LOSSLESS)</span><br></pre></td></tr></table></figure><p>剩下就是扫描文件、建目录、算大小这些体力活，完整代码贴在文末。</p><h3 id="自定义配置"><a href="#自定义配置" class="headerlink" title="自定义配置"></a>自定义配置</h3><p>脚本顶部有三个配置项，打开 <code>.py</code> 文件就能改：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">QUALITY = <span class="number">85</span>       <span class="comment"># WebP 质量 (1-100)，越大越清晰</span></span><br><span class="line">LOSSLESS = <span class="literal">False</span>   <span class="comment"># True = 无损（文件大），False = 有损（文件小）</span></span><br></pre></td></tr></table></figure><p>不同场景的建议值：</p><table><thead><tr><th>场景</th><th>推荐质量</th></tr></thead><tbody><tr><td>产品图、照片</td><td>80-85</td></tr><tr><td>图标、UI 元素</td><td>90-95 或无损</td></tr><tr><td>缩略图、背景图</td><td>60-70</td></tr><tr><td>存档需求</td><td>无损模式</td></tr></tbody></table><hr><h2 id="备选方案：cwebp"><a href="#备选方案：cwebp" class="headerlink" title="备选方案：cwebp"></a>备选方案：cwebp</h2><p>如果你对压缩参数有极致控制欲（比如想调 <code>-m 6 -pass 10</code>），Google 官方的 <code>cwebp</code> 编码器是最强的。</p><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">brew install webp        <span class="comment"># macOS</span></span><br><span class="line"><span class="built_in">sudo</span> apt install webp    <span class="comment"># Ubuntu/Debian</span></span><br><span class="line">scoop install webp       <span class="comment"># Windows</span></span><br></pre></td></tr></table></figure><p>单文件转换：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cwebp -q 80 input.png -o output.webp</span><br></pre></td></tr></table></figure><p>批量处理整个目录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">find . -<span class="built_in">type</span> f \( -name <span class="string">&quot;*.png&quot;</span> -o -name <span class="string">&quot;*.jpg&quot;</span> -o -name <span class="string">&quot;*.jpeg&quot;</span> \) \</span><br><span class="line">  -<span class="built_in">exec</span> sh -c <span class="string">&#x27;cwebp -q 80 &quot;$1&quot; -o &quot;$&#123;1%.*&#125;.webp&quot;&#x27;</span> _ &#123;&#125; \;</span><br></pre></td></tr></table></figure><p>日常用的话 Pillow 脚本已经够用，<code>cwebp</code> 更适合对压缩质量有极致追求或者要集成到自动化流水线的场景。</p><hr><h2 id="避坑指南"><a href="#避坑指南" class="headerlink" title="避坑指南"></a>避坑指南</h2><h3 id="1-处理后的文件比原文件大？"><a href="#1-处理后的文件比原文件大？" class="headerlink" title="1. 处理后的文件比原文件大？"></a>1. 处理后的文件比原文件大？</h3><p>WebP 对<strong>照片类</strong>图片压缩效果最好，但对某些<strong>简单图形、截图、扁平色块的 PNG</strong>，WebP 无损模式的文件体积可能反而比原文件大。遇到这种情况，可以尝试有损模式或者保持原格式。</p><h3 id="2-不要删除原始文件"><a href="#2-不要删除原始文件" class="headerlink" title="2. 不要删除原始文件"></a>2. 不要删除原始文件</h3><p>建议转换完成后统一清理，而不是转换时直接覆盖。万一对质量不满意，还能用原图重新压。</p><h3 id="3-文件命名冲突"><a href="#3-文件命名冲突" class="headerlink" title="3. 文件命名冲突"></a>3. 文件命名冲突</h3><p>如果源文件有 <code>photo.jpg</code> 和 <code>photo.png</code>，转换后都会变成 <code>photo.webp</code>，两个文件会互相覆盖。脚本已经做了防冲突处理（自动加序号后缀），但如果在意输出文件名，建议转换前先检查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">find . -<span class="built_in">type</span> f \( -name <span class="string">&quot;*.png&quot;</span> -o -name <span class="string">&quot;*.jpg&quot;</span> -o -name <span class="string">&quot;*.jpeg&quot;</span> \) \</span><br><span class="line">  -<span class="built_in">exec</span> <span class="built_in">basename</span> &#123;&#125; \; | sed <span class="string">&#x27;s/\.[^.]*$//&#x27;</span> | <span class="built_in">sort</span> | <span class="built_in">uniq</span> -d</span><br></pre></td></tr></table></figure><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这次写的 <code>convert2webp</code> 主打一个开箱即用，不需要理解命令行参数，不需要手动装依赖，双击就能跑。代码放在 <a href="https://github.com/tenxiaodao/convert2webp.git">convert2webp</a> 这个 repo 里，包含了 Python 脚本和两个平台的启动器。</p><p>如果你有其他常用的 WebP 转换工具，下面做个简单对比：</p><table><thead><tr><th>方案</th><th>适合人群</th><th>特点</th></tr></thead><tbody><tr><td>convert2webp.py</td><td>懒人</td><td>丢文件夹双击，自动装依赖</td></tr><tr><td><code>cwebp</code></td><td>命令行发烧友</td><td>Google 官方，压缩参数最细</td></tr><tr><td>ImageMagick</td><td>已有该工具链</td><td>可同时做缩放&#x2F;裁剪</td></tr><tr><td>Sharp 脚本</td><td>前端项目</td><td>可集成 CI&#x2F;CD</td></tr></tbody></table><p>不管用哪种方案，WebP 迁移都是一次性的工作，带来的加载速度提升却是长久的。如果你还在用原始图片格式往外丢，花半小时做一次全量迁移吧，值得。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/batch-convert-images-to-webp/</id>
    <link href="https://www.tenxiaodao.top/batch-convert-images-to-webp/"/>
    <published>2026-06-19T02:00:00.000Z</published>
    <summary>
      <![CDATA[<blockquote>
<p>WebP 是 Google 推出的一种现代图片格式，在同等画质下体积比 JPEG 小 25%-35%，比 PNG 小得更多。对于博客网站和前端项目来说，全量切到 WebP 能显著减少页面加载速度。</p>
</blockquote>
<p>周末花了]]>
    </summary>
    <title>写了一个开箱即用的批量图片转 WebP 脚本</title>
    <updated>2026-06-24T18:04:46.199Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="苹果生态" scheme="https://www.tenxiaodao.top/categories/%E8%8B%B9%E6%9E%9C%E7%94%9F%E6%80%81/"/>
    <category term="MacBook Air" scheme="https://www.tenxiaodao.top/tags/MacBook-Air/"/>
    <category term="M4" scheme="https://www.tenxiaodao.top/tags/M4/"/>
    <category term="Apple" scheme="https://www.tenxiaodao.top/tags/Apple/"/>
    <category term="开箱" scheme="https://www.tenxiaodao.top/tags/%E5%BC%80%E7%AE%B1/"/>
    <content>
      <![CDATA[<blockquote><p>本文于 2026 年 6 月 18 日持续更新中。</p></blockquote><hr><h2 id="为什么换这台机器"><a href="#为什么换这台机器" class="headerlink" title="为什么换这台机器"></a>为什么换这台机器</h2><p>之前的机器已经不太够用了——编译慢、电池崩、风扇起飞是常态。正好 M4 Air 发布之后口碑一直不错，忍了一段时间终于下单了。</p><p>选的是 10 核 CPU + 10 核 GPU 的满血版，16G 内存配 512GB 存储。对我目前的开发和工作场景来说，这个配置应该够用很久了。</p><p>下单之后那几天，物流信息一天刷八遍。</p><hr><h2 id="开箱"><a href="#开箱" class="headerlink" title="开箱"></a>开箱</h2><p>包裹到的第一时间拆箱。MacBook Air 的包装还是一贯的简洁风格——白色盒子，正面就是机器的俯视图。</p><p>打开之后机器本体在最上面，下面是电源线和充电头。这次选了 <strong>天蓝色</strong> 的新配色。拿到手第一感觉比官网渲染图更耐看——光线好的时候偏淡蓝，暗处接近银灰，很低调又有点不一样。</p><hr><h2 id="外观与手感"><a href="#外观与手感" class="headerlink" title="外观与手感"></a>外观与手感</h2><p>先说重量。Air 系列一直以轻薄著称，这台也不例外。从盒子里拿出来第一感觉就是轻——比想象中还轻，单手拿着完全没有负担。</p><p>机身很薄，最薄的地方大概跟 USB-C 接口差不多厚。天晚上放进包里，第二天背去上班基本感觉不到多了什么东西。</p><p>做工方面 Apple 一直在线，机身一体成型，手感温润，没什么好挑剔的。</p><hr><h2 id="屏幕"><a href="#屏幕" class="headerlink" title="屏幕"></a>屏幕</h2><p>Liquid Retina 显示屏，13.6 英寸，亮度够高，色彩表现好。日常写代码、看文档、看视频都完全够用。</p><p>刘海屏确实存在，但用起来很快就忽略了——至少对我而言，它没有影响任何实际使用。</p><hr><h2 id="性能"><a href="#性能" class="headerlink" title="性能"></a>性能</h2><h3 id="编译体验"><a href="#编译体验" class="headerlink" title="编译体验"></a>编译体验</h3><p>第一次跑项目的编译时感受最明显，比我旧机器快了一大截。</p><p>根据公开的跑分数据，M4（10 核）的 Geekbench 6 单核成绩在 <strong>3700 左右</strong>，多核约 <strong>14500</strong>——比 M3 提升了大概 20%，比我的旧机器翻了一倍还不止。日常的 Xcode 编译、前端构建这些任务，体感速度提升非常明显。</p><h3 id="日常使用"><a href="#日常使用" class="headerlink" title="日常使用"></a>日常使用</h3><p>开了几十个 Chrome 标签页 + VS Code + 几个终端窗口 + 微信 + 邮件，完全没有卡顿。16G 内存对于我目前的开发场景来说绰绰有余，完全没有感受到内存压力。</p><h3 id="风扇噪音"><a href="#风扇噪音" class="headerlink" title="风扇噪音"></a>风扇噪音</h3><p>零。这台机器没有风扇。</p><p>这是我最满意的地方之一。之前的机器编译时风扇声音大得离谱，开会的时候很尴尬。M4 Air 无论怎么跑都是安静的。满载时机身会热，但没有风扇声。</p><hr><h2 id="续航"><a href="#续航" class="headerlink" title="续航"></a>续航</h2><p>满电状态下：</p><ul><li>日常开发（VS Code + 浏览器 + 终端）：大概能撑 <strong>12-14 小时</strong></li><li>视频播放：<strong>18 小时</strong>（Apple 官方标称数据）</li><li>轻度使用（写文档 + 网页浏览）：<strong>15-18 小时</strong></li></ul><blockquote><p>续航数据来自 Apple 官方标称（视频播放 18 小时 &#x2F; 无线网页 15 小时），日常综合使用预计在 14-16 小时左右。实际续航等用一段时间后回来更新。</p></blockquote><p>充电用的是 MagSafe，很喜欢这个设计——有人绊到线的时候会安全脱落，不会把电脑带下去。</p><hr><h2 id="键盘与触控板"><a href="#键盘与触控板" class="headerlink" title="键盘与触控板"></a>键盘与触控板</h2><p>键盘是妙控键盘，键程比旧款 MacBook 好一些，打字手感扎实。触控板还是那么大，手势操作一如既往地好用。</p><p>Touch ID 很方便，解锁和支付按一下就行，省了输密码。</p><hr><h2 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h2><p>两个雷雳 4（USB-C）接口 + 一个 MagSafe 充电口 + 一个 3.5mm 耳机孔。</p><p>接口不算多，但对我够了。外接显示器和扩展坞的时候走一个 USB-C 就行。MagSafe 充电时两个 USB-C 都能空出来用。</p><hr><h2 id="适合谁"><a href="#适合谁" class="headerlink" title="适合谁"></a>适合谁</h2><p><strong>适合：</strong></p><ul><li>日常开发、写代码、写文档为主的人</li><li>需要一台轻薄、安静、续航好的笔记本</li><li>对学生和刚工作的开发者很友好</li></ul><p><strong>不太适合：</strong></p><ul><li>需要长时间跑 GPU 渲染、大规模机器学习的</li><li>需要外接多个高分辨率显示器的</li><li>手头有大量 x86 专用软件还没适配 ARM 的</li></ul><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>MacBook Air M4 是一台目标明确的产品——它不要做性能猛兽，而是做一台你愿意每天都带出门的电脑。</p><p>对我而言，它做到了。轻、安静、续航好、性能够用。这就够了。</p><p><em>等用一段时间之后再回来更新长期使用感受。</em></p>]]>
    </content>
    <id>https://www.tenxiaodao.top/macbook-air-m4-unboxing/</id>
    <link href="https://www.tenxiaodao.top/macbook-air-m4-unboxing/"/>
    <published>2026-06-18T02:00:00.000Z</published>
    <summary>
      <![CDATA[<blockquote>
<p>本文于 2026 年 6 月 18 日持续更新中。</p>
</blockquote>
<hr>
<h2 id="为什么换这台机器"><a href="#为什么换这台机器" class="headerlink" title="为什么换这台机器"><]]>
    </summary>
    <title>MacBook Air M4（10+10 核）开箱与初体验</title>
    <updated>2026-06-18T05:00:22.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="网络运维" scheme="https://www.tenxiaodao.top/categories/%E7%BD%91%E7%BB%9C%E8%BF%90%E7%BB%B4/"/>
    <category term="NAS" scheme="https://www.tenxiaodao.top/tags/NAS/"/>
    <category term="Homelab" scheme="https://www.tenxiaodao.top/tags/Homelab/"/>
    <category term="虚拟化" scheme="https://www.tenxiaodao.top/tags/%E8%99%9A%E6%8B%9F%E5%8C%96/"/>
    <category term="设备" scheme="https://www.tenxiaodao.top/tags/%E8%AE%BE%E5%A4%87/"/>
    <content>
      <![CDATA[<p>从大一开始折腾，东西不算多，但每一件都是慢慢添起来的。写一篇总结，记录一下目前手上在用的设备。</p><hr><h2 id="MacBook-Air-M4"><a href="#MacBook-Air-M4" class="headerlink" title="MacBook Air M4"></a>MacBook Air M4</h2><p>主力机，MacBook Air M4 16G 内存 + 512GB 硬盘。</p><p>这台机器是我目前最核心的生产力工具。开发、写博客、跑虚拟机、剪视频——大部分时间都在这上面。M4 的性能对我目前的需求来说完全溢出，好处是不管怎么折腾都几乎没有风扇声。</p><p>我之前折腾黑苹果的时候，想在非苹果硬件上体验 macOS；现在换了白苹果之后，回头看那段时间的经历还挺感慨的——轻是真的轻——出门塞包里几乎感觉不到重量，续航也够撑一整天。之前折腾黑苹果的时候总想体验白苹果是什么感觉，现在用上了，确实不一样。</p><hr><h2 id="玩客云-NAS"><a href="#玩客云-NAS" class="headerlink" title="玩客云 NAS"></a>玩客云 NAS</h2><p>这台玩客云是我 Homelab 的起点。</p><p>几十块在闲鱼收的，刷了 Armbian，挂了块闲置硬盘。主要用来做下载机和轻量文件存储。功耗低、发热小、24 小时开着不用管。部署了 Samba 共享和 Transmission 下载，平时追的番剧都是它下的。</p><p>关于玩客云的具体折腾过程，我之前写过一篇《玩客云做轻NAS低成本看番》，记录了刷机配置的完整步骤。</p><hr><h2 id="Surface-Pro-4"><a href="#Surface-Pro-4" class="headerlink" title="Surface Pro 4"></a>Surface Pro 4</h2><p>这台 Surface Pro 4 目前的主力用途——装 macOS。</p><p>是的，我在上面跑过 macOS，过程记录了一篇文章《在 Surface Pro 4 里安装 macOS》。虽然平时不用它做主力，但用来做硬件兼容性测试和系统实验非常方便。偶尔也带着出门当平板用。</p><hr><h2 id="网络设备"><a href="#网络设备" class="headerlink" title="网络设备"></a>网络设备</h2><p>家里有一条千兆宽带，配合一台支持端口聚合的交换机使用。我写过《如何配置端口聚合》来记录链路的配置过程。交换机之前也没少折腾——从 VLAN 划分到链路聚合，早期 Homelab 的网络知识基本都是靠这玩意儿练出来的。</p><hr><h2 id="虚拟化与开发环境"><a href="#虚拟化与开发环境" class="headerlink" title="虚拟化与开发环境"></a>虚拟化与开发环境</h2><p>主力虚拟化平台用的 VMware，在上面跑过 Ubuntu Server 来部署各种服务。WSL2 也一直在用，适合在 Windows 上快速搭 Linux 环境。</p><p>开发这块涉及的比较多——Java、Python、Vue、Flutter 都写过。Docker 用来容器化部署，Git 用来管理代码。从爬虫到 API 开发到前端页面，我的 Homelab 不只是硬件折腾，也是我练技术的一块试验田。</p><hr><h2 id="为什么折腾"><a href="#为什么折腾" class="headerlink" title="为什么折腾"></a>为什么折腾</h2><p>说到底，Homelab 对我来说不是目的，是过程。从一台几十块的玩客云开始，到学会 Linux、网络协议、虚拟化、CI&#x2F;CD——这些东西在学校学的是理论，在家搭一遍才是自己的。</p><p>而且说真的，就是感觉好玩。</p><p>未来打算上一台真正的多盘位 NAS，给核心设备配个 UPS，再搭一套监控面板。慢慢来，不着急。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/my-homelab-setup/</id>
    <link href="https://www.tenxiaodao.top/my-homelab-setup/"/>
    <published>2026-06-17T08:00:00.000Z</published>
    <summary>
      <![CDATA[<p>从大一开始折腾，东西不算多，但每一件都是慢慢添起来的。写一篇总结，记录一下目前手上在用的设备。</p>
<hr>
<h2 id="MacBook-Air-M4"><a href="#MacBook-Air-M4" class="headerlink" title="MacB]]>
    </summary>
    <title>我的 Homelab 全家桶</title>
    <updated>2026-06-17T06:43:30.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="生活随笔" scheme="https://www.tenxiaodao.top/categories/%E7%94%9F%E6%B4%BB%E9%9A%8F%E7%AC%94/"/>
    <category term="大同" scheme="https://www.tenxiaodao.top/tags/%E5%A4%A7%E5%90%8C/"/>
    <category term="工作" scheme="https://www.tenxiaodao.top/tags/%E5%B7%A5%E4%BD%9C/"/>
    <category term="漂泊" scheme="https://www.tenxiaodao.top/tags/%E6%BC%82%E6%B3%8A/"/>
    <content>
      <![CDATA[<p>去年十二月开始在大同实习，到现在也半年了。谈不上多适应，但也慢慢习惯了这座城市。</p><hr><h2 id="为什么来大同"><a href="#为什么来大同" class="headerlink" title="为什么来大同"></a>为什么来大同</h2><p>很简单，数据中心在这里。</p><p>拿到中联数据集团的 offer 之后，同学问我去哪，我说大同。大部分人的反应是——啊？大同？不是只有煤矿和刀削面吗？</p><p>我一开始也是这么想的。但来了之后发现，大同跟我印象里的那个大同不太一样。</p><hr><h2 id="初印象"><a href="#初印象" class="headerlink" title="初印象"></a>初印象</h2><p>12 月的大同，是真的冷。出火车站那一刻，风刮过来像是有人在往你脸上撒沙子。我从太原过来，按理说也是山西人，但大同比太原还要冷一个级别。</p><p>实习第一个星期住在单位安排的宿舍。房间不大，暖气倒是很足。每天走着去上班，路过古城墙——对，大同有一个完整的古城墙，晚上灯一亮挺好看的。之前完全不知道，来了之后才发现的。</p><hr><h2 id="日常"><a href="#日常" class="headerlink" title="日常"></a>日常</h2><p>工作日的节奏很简单：起床、上班、吃饭、下班、回宿舍躺着。</p><p>数据中心在 1 号楼。机房里的日常前面那篇实习笔记已经写过了，这里就不重复了。说说下班之后的生活吧。</p><p>大同的吃食跟太原有点像，但又有自己的特色。刀削面两边都有，但大同的刀削面面片更厚更劲道。还有浑源凉粉，用土豆淀粉做的，跟南方的凉粉完全不一样，浇上醋和辣椒油，夏天来一碗很爽。</p><p>周末偶尔去古城里转转。华严寺去过一次，里面的壁画和塑像保存得很好，是整个寺里最值得看的地方。善化寺也去了，比华严寺小一些，但更安静，适合发呆。</p><hr><h2 id="一个人的时候"><a href="#一个人的时候" class="headerlink" title="一个人的时候"></a>一个人的时候</h2><p>说实话，刚来的时候挺不适应的。太原离大同不远，但毕竟不是家。下了班回到宿舍，有时候会觉得这座城市的夜晚跟自己没什么关系。</p><p>后来慢慢找到了一些自己的节奏。周末去网吧打打游戏，或者去超市买点东西回来自己做饭。偶尔跟同事出去吃顿烧烤，喝点啤酒，聊聊天。</p><p>其实在哪都一样，关键是身边有没有能说话的人。</p><hr><h2 id="大同这个城市"><a href="#大同这个城市" class="headerlink" title="大同这个城市"></a>大同这个城市</h2><p>半年下来，对大同的印象改变了不少。没有以前想象的那么灰暗。古城修得很好，护城河也注了水，天气好的时候天很蓝。</p><p>但对我来说，它还不是”家”。家是太原，大同是工作的城市。我不知道会在这里待多久，但既然来了，就好好待着吧。</p><blockquote><p>抛弃幻想，准备斗争。</p></blockquote>]]>
    </content>
    <id>https://www.tenxiaodao.top/living-in-datong/</id>
    <link href="https://www.tenxiaodao.top/living-in-datong/"/>
    <published>2026-06-17T07:00:00.000Z</published>
    <summary>
      <![CDATA[<p>去年十二月开始在大同实习，到现在也半年了。谈不上多适应，但也慢慢习惯了这座城市。</p>
<hr>
<h2 id="为什么来大同"><a href="#为什么来大同" class="headerlink" title="为什么来大同"></a>为什么来大同</h2><p>很]]>
    </summary>
    <title>我在大同的日子</title>
    <updated>2026-06-17T06:10:14.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="生活随笔" scheme="https://www.tenxiaodao.top/categories/%E7%94%9F%E6%B4%BB%E9%9A%8F%E7%AC%94/"/>
    <category term="年度回顾" scheme="https://www.tenxiaodao.top/tags/%E5%B9%B4%E5%BA%A6%E5%9B%9E%E9%A1%BE/"/>
    <category term="旅行" scheme="https://www.tenxiaodao.top/tags/%E6%97%85%E8%A1%8C/"/>
    <category term="成长" scheme="https://www.tenxiaodao.top/tags/%E6%88%90%E9%95%BF/"/>
    <content>
      <![CDATA[<p>今天翻了一下云盘里的照片，发现 2025 年实在是过得太快了——从年初的海南到年末的数据中心，像是被人按了快进键。趁还记得清楚，记一笔。</p><hr><h2 id="一月-·-海南与直升机"><a href="#一月-·-海南与直升机" class="headerlink" title="一月 · 海南与直升机"></a>一月 · 海南与直升机</h2><p>年初是在海南过的，带着家人。第一次在海边过年，暖和的、湿润的，跟大同的冬天完全是两个世界。</p><p>最难忘的是坐了直升机。直升机和民航客机完全不是一个东西——起飞的时候那种震动从座椅直接传到骨头里，视野一下子拉高，海岸线尽收眼底。落地之后腿有点软，但值。</p><p>从海南的照片来看，那时候还胖一点。</p><hr><h2 id="三月-·-宿舍日常"><a href="#三月-·-宿舍日常" class="headerlink" title="三月 · 宿舍日常"></a>三月 · 宿舍日常</h2><p>回到学校。三月拍了一张宿舍大合照，现在已经想不起来那天是什么由头拍的了，但看到那几个人的脸还是觉得挺开心的。</p><p>现在大家各奔东西了，有人在备考，有人在上班，还有人在不知道哪里的地方躺着。</p><hr><h2 id="四月-·-挣扎"><a href="#四月-·-挣扎" class="headerlink" title="四月 · 挣扎"></a>四月 · 挣扎</h2><p>四月的短文写得不多，有一句话我记得很清楚：”抛弃幻想，准备斗争。”</p><p>那时候在学一些东西，但进度很慢，焦虑和丧气交替出现。现在回头看，其实那段时间也没浪费，只是当时的自己不知道。</p><hr><h2 id="九月-·-山东之旅"><a href="#九月-·-山东之旅" class="headerlink" title="九月 · 山东之旅"></a>九月 · 山东之旅</h2><p>九月是我这一整年最开心的几天。</p><p>泰山是第一站。凌晨开始爬，到山顶正好赶上日出。泰山顶上那个风大得夸张，但日出那几分钟，所有人都安静了。</p><p>然后一路走下去：济南的泉水、淄博的烧烤——淄博烧烤是真的好吃，小饼一卷，肉串一撸，放上葱和酱，那个味道我现在写文章还在咽口水。</p><p>威海的海很蓝。那几天天气好，海边能见度很高，随便拍一张都能当壁纸。</p><p>最后一站是青岛。红瓦绿树，碧海蓝天，名不虚传。走了很多路，吃了很多海鲜。</p><p>回程的时候心里空落落的，因为知道这样的日子不会常有。</p><hr><h2 id="十一月-·-低谷与转机"><a href="#十一月-·-低谷与转机" class="headerlink" title="十一月 · 低谷与转机"></a>十一月 · 低谷与转机</h2><p>十一月状态不好。短文里写了很多情绪化的东西——关于失去、关于后悔、关于一些说不清道不明的事情。</p><p>但就在这个月，”龙族”里的这句话一直跟着我：</p><blockquote><p>“我是不是就这样参加高考，上一所不好不坏的大学，我也找不到我喜欢的女孩。我将来会找一份我不太喜欢的工作，找一个房子，然后就跟一个可能愿意和我结婚的女人过一辈子。”</p></blockquote><p>像在照镜子。</p><hr><h2 id="十二月-·-拿到-offer"><a href="#十二月-·-拿到-offer" class="headerlink" title="十二月 · 拿到 offer"></a>十二月 · 拿到 offer</h2><p>12 月 7 日，拿到了中联数据集团的实习 offer。</p><p>那天我没发朋友圈，就在随笔里写了一句话：今天拿到中联数据集团的实习offer了,加油吧！！！</p><p>十天后入职培训结束，被分到了 1 号楼。从此开始了在数据中心搬砖的日子。</p><hr><h2 id="一年下来"><a href="#一年下来" class="headerlink" title="一年下来"></a>一年下来</h2><p>读了一遍自己写的，发现 2025 是这样的：上半年在走，下半年在熬。</p><p>走过了泰山、济南、淄博、威海、青岛、海南。熬过了焦虑、失眠、自我怀疑。</p><p>最后拿到 offer 的那一刻，谈不上多激动，但觉得没白过这一年。</p><p>2026 年，继续走吧。</p><blockquote><p>君看今日树头花，不是去年枝上朵。</p></blockquote>]]>
    </content>
    <id>https://www.tenxiaodao.top/my-2025-review/</id>
    <link href="https://www.tenxiaodao.top/my-2025-review/"/>
    <published>2026-06-17T05:00:00.000Z</published>
    <summary>
      <![CDATA[<p>今天翻了一下云盘里的照片，发现 2025 年实在是过得太快了——从年初的海南到年末的数据中心，像是被人按了快进键。趁还记得清楚，记一笔。</p>
<hr>
<h2 id="一月-·-海南与直升机"><a href="#一月-·-海南与直升机" class="headerli]]>
    </summary>
    <title>我的 2025 年度回顾</title>
    <updated>2026-06-17T06:07:24.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="生活随笔" scheme="https://www.tenxiaodao.top/categories/%E7%94%9F%E6%B4%BB%E9%9A%8F%E7%AC%94/"/>
    <category term="运维" scheme="https://www.tenxiaodao.top/tags/%E8%BF%90%E7%BB%B4/"/>
    <category term="数据中心" scheme="https://www.tenxiaodao.top/tags/%E6%95%B0%E6%8D%AE%E4%B8%AD%E5%BF%83/"/>
    <category term="实习" scheme="https://www.tenxiaodao.top/tags/%E5%AE%9E%E4%B9%A0/"/>
    <category term="基础设施运维" scheme="https://www.tenxiaodao.top/tags/%E5%9F%BA%E7%A1%80%E8%AE%BE%E6%96%BD%E8%BF%90%E7%BB%B4/"/>
    <content>
      <![CDATA[<p>2025 年 12 月 7 日，我拿到了中联数据集团的实习 offer。说实话，当时的心情比想象中平静——大概是准备材料、面试、等通知这一套流程走下来，人已经有些麻木了。</p><p>但入职那天走进机房的那一刻，我才真正意识到：我真的要开始在这地方上班了。</p><hr><h2 id="进机房的第一印象"><a href="#进机房的第一印象" class="headerlink" title="进机房的第一印象"></a>进机房的第一印象</h2><p>之前在学校学网络、学服务器，全是纸上谈兵。第一次进数据中心机房的感觉，很难形容。</p><p>首先是声音。你想象一下几百台服务器同时转风扇的声音——不是巨响，是一种持续的、低沉的嗡嗡声，像置身在一台巨大的机器内部。待久了其实会习惯，但头几天下班回家耳朵里还是那个声音。</p><p>然后是温度。制冷通道和热通道是分开的，冷通道待久了甚至觉得有点冷。巡检的时候从冷区走到热区，温差特别明显。</p><p>还有那种整齐的压迫感——一排又一排的机柜，闪着绿灯，标签打得整整齐齐。每一台机器都能在网上找到它的踪迹，但现实中它们就安安静静地待在这里。</p><p>我的岗位是基础设施运维，被分到了 1 号楼。</p><p>说白了就是维护数据中心的基础设施——电力、制冷、网络链路、服务器硬件。服务器出问题了要去换硬盘，网络不通了要跟着排查链路，空调报警了也要去看。跟学校里学的”运维”概念不太一样，这里的活更偏硬件和底层。</p><hr><h2 id="日常做什么"><a href="#日常做什么" class="headerlink" title="日常做什么"></a>日常做什么</h2><p>实习生的日常没有那么酷。巡检、跳线、换硬件、配合工单——说白了就是脏活累活。</p><p>但对我来说一切都是新鲜的。第一次拿着螺丝刀开机柜门，第一次看运维同事排查网络故障时在命令行里的操作速度——真的快，不带犹豫的。我在旁边看着，心里默默记。</p><p>有时候碰上割接或者变更，得留在现场配合。这种时候虽然累，但能学到最多的东西——原来生产环境里一个操作要经过这么多审批和复核。</p><hr><h2 id="最有收获的事"><a href="#最有收获的事" class="headerlink" title="最有收获的事"></a>最有收获的事</h2><p>有次网络波动，需要排查一个端口的问题。同事带着我从物理链路开始看，一步一步顺着走——从跳线面板到交换机端口，再看配置、看日志。最后发现是一个配置跟预期不一致。</p><p>排查过程本身并不复杂，但那种”按顺序来，不要跳步”的思路，对我影响挺大的。以前我自己折腾网络，总喜欢跳着试，试对了就行。但在数据中心里，每一步都要有据可查。</p><hr><h2 id="一些感受"><a href="#一些感受" class="headerlink" title="一些感受"></a>一些感受</h2><p>实习之前我对数据中心的想象全是错的——不是那种电影里蓝色灯光的高科技场景，也不是科幻片里那样空旷无人。真实的机房是务实的地方：亮就行，冷就行，不出故障就行。</p><p>同事们都挺有意思，虽然大家平时话不多，但遇到问题的时候配合很默契。吃饭的时候偶尔聊聊设备配置、聊聊最近的故障处理，我大部分时间在听，也挺满足。</p><p>说到底，这份实习让我从一个在学校里”学过网络”的状态，慢慢变成了一个真的在处理网络问题的人。</p><p>还差得远，但至少上路了。</p><hr><blockquote><p>“当下的所有问题都是上一个问题的答案，但当下的答案又会成为下一个问题。”<br>— 2025 年 11 月 18 日的随笔</p></blockquote>]]>
    </content>
    <id>https://www.tenxiaodao.top/data-center-internship/</id>
    <link href="https://www.tenxiaodao.top/data-center-internship/"/>
    <published>2026-06-17T04:00:00.000Z</published>
    <summary>
      <![CDATA[<p>2025 年 12 月 7 日，我拿到了中联数据集团的实习 offer。说实话，当时的心情比想象中平静——大概是准备材料、面试、等通知这一套流程走下来，人已经有些麻木了。</p>
<p>但入职那天走进机房的那一刻，我才真正意识到：我真的要开始在这地方上班了。</p>
<hr]]>
    </summary>
    <title>数据中心实习笔记</title>
    <updated>2026-06-17T06:07:10.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="生活随笔" scheme="https://www.tenxiaodao.top/categories/%E7%94%9F%E6%B4%BB%E9%9A%8F%E7%AC%94/"/>
    <category term="二次元" scheme="https://www.tenxiaodao.top/tags/%E4%BA%8C%E6%AC%A1%E5%85%83/"/>
    <category term="动漫" scheme="https://www.tenxiaodao.top/tags/%E5%8A%A8%E6%BC%AB/"/>
    <category term="EVA" scheme="https://www.tenxiaodao.top/tags/EVA/"/>
    <category term="紫罗兰永恒花园" scheme="https://www.tenxiaodao.top/tags/%E7%B4%AB%E7%BD%97%E5%85%B0%E6%B0%B8%E6%81%92%E8%8A%B1%E5%9B%AD/"/>
    <category term="五等分的新娘" scheme="https://www.tenxiaodao.top/tags/%E4%BA%94%E7%AD%89%E5%88%86%E7%9A%84%E6%96%B0%E5%A8%98/"/>
    <content>
      <![CDATA[<p>前几天整理博客关于页的时候，写到了自己看过的番。列完才发现，原来不知不觉已经追了这么多。</p><p>想想还是写一篇吧，当个记录。</p><hr><h2 id="EVA-—-最痛的成长"><a href="#EVA-—-最痛的成长" class="headerlink" title="EVA — 最痛的成长"></a>EVA — 最痛的成长</h2><p>第一次看 EVA 是在一个暑假的深夜。说真的，看完之后好几天没缓过来。</p><p>真嗣的性格让很多人反感，可我觉得正因为不完美，这个角色才是真实的。一个十四岁的少年被推上驾驶舱，没人问过他愿不愿意。明日香用嚣张来掩饰脆弱，绫波丽用沉默来掩盖空洞。</p><p>“梦是现实的延续，现实是梦的终结。”</p><p>长大之后重看，才明白这部作品讲的不是机甲，是人跟人之间永远无法彻底相互理解的孤独。每次看都有不同的感受，这就是经典该有的样子。</p><hr><h2 id="紫罗兰永恒花园-—-每集都在哭"><a href="#紫罗兰永恒花园-—-每集都在哭" class="headerlink" title="紫罗兰永恒花园 — 每集都在哭"></a>紫罗兰永恒花园 — 每集都在哭</h2><p>这部是朋友强推的，第一集我还不以为然，觉得画面好归好，也不至于那么夸张。结果从第三集开始，每集结尾准时掉眼泪。</p><p>薇尔莉特从不懂感情的人偶，到慢慢理解”我爱你”这三个字的分量，这个过程本身就很动人。印象最深的是第十集——母亲给女儿写的五十封信，每年一封，直到女儿成年。看到那里我直接破防了。</p><blockquote><p>“我爱你，就是你给予我的，让我成为人。”</p></blockquote><p>画面很美，配乐也美，但最打动人的还是那种克制的温柔。如果你想找一部能让你静下心来的番，选它没错。</p><hr><h2 id="国家队（Darling-in-the-Franxx）—-遗憾也是一种美"><a href="#国家队（Darling-in-the-Franxx）—-遗憾也是一种美" class="headerlink" title="国家队（Darling in the Franxx）— 遗憾也是一种美"></a>国家队（Darling in the Franxx）— 遗憾也是一种美</h2><p>这部是我追得最投入的番之一。前期的世界观、男女主的互动、战斗场面，真的让人上头。002 和 广那种”只有你能和我一起飞”的羁绊，中二但是动人。</p><p>可惜后期的剧情走向争议挺大，结尾更是让很多人意难平。但我不后悔追过它——有些作品的魅力就在于它的不完美，遗憾本身就是它的一部分。</p><p>毕竟，哪段青春没有遗憾呢。</p><hr><h2 id="五等分的新娘-—-纯粹的快乐"><a href="#五等分的新娘-—-纯粹的快乐" class="headerlink" title="五等分的新娘 — 纯粹的快乐"></a>五等分的新娘 — 纯粹的快乐</h2><p>前面说的几部都比较沉重，来点轻松的。</p><p>五等分就是那种不需要带脑子看的番。五个性格迥异的姐妹同一个男主，悬疑式的”最后娶了谁”的设定，追番过程充满乐趣。</p><p>一花的帅气、二乃的傲娇、三玖的安静、四叶的元气、五月的认真——总有一款你会喜欢。虽然炒股番容易让人站错队急得跺脚（没错我就是三玖党），但这种轻松的恋爱喜剧，在工作学习之余看一看，真的很解压。</p><hr><h2 id="尾声"><a href="#尾声" class="headerlink" title="尾声"></a>尾声</h2><p>写这篇不是为了安利谁入坑，只是想记录下来——在某个年纪，这些作品实实在在地陪过我。</p><p>技术教程写多了，偶尔也写点这种不费脑子的东西。等再过几年回头看，也许还能想起当时追番的心情。</p><blockquote><p>君看今日树头花，不是去年枝上朵。</p></blockquote><p>那就写到这里吧。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/anime-that-accompanied-me/</id>
    <link href="https://www.tenxiaodao.top/anime-that-accompanied-me/"/>
    <published>2026-06-15T13:30:00.000Z</published>
    <summary>
      <![CDATA[<p>前几天整理博客关于页的时候，写到了自己看过的番。列完才发现，原来不知不觉已经追了这么多。</p>
<p>想想还是写一篇吧，当个记录。</p>
<hr>
<h2 id="EVA-—-最痛的成长"><a href="#EVA-—-最痛的成长" class="headerlink]]>
    </summary>
    <title>那些年陪我的二次元</title>
    <updated>2026-06-15T09:07:43.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="DevOps" scheme="https://www.tenxiaodao.top/categories/DevOps/"/>
    <category term="Hexo" scheme="https://www.tenxiaodao.top/tags/Hexo/"/>
    <category term="CI/CD" scheme="https://www.tenxiaodao.top/tags/CI-CD/"/>
    <category term="GitHub Actions" scheme="https://www.tenxiaodao.top/tags/GitHub-Actions/"/>
    <category term="自动化部署" scheme="https://www.tenxiaodao.top/tags/%E8%87%AA%E5%8A%A8%E5%8C%96%E9%83%A8%E7%BD%B2/"/>
    <content>
      <![CDATA[<h1 id="GitHub-Actions-使用指南：从入门到实战"><a href="#GitHub-Actions-使用指南：从入门到实战" class="headerlink" title="GitHub Actions 使用指南：从入门到实战"></a>GitHub Actions 使用指南：从入门到实战</h1><h2 id="什么是-GitHub-Actions"><a href="#什么是-GitHub-Actions" class="headerlink" title="什么是 GitHub Actions"></a>什么是 GitHub Actions</h2><p>GitHub Actions 是 GitHub 自带的自动化工作流平台。你可以把它理解成 <strong>GitHub 仓库自带的免费服务器</strong>，每次有人推送代码、提 Issue、创建 PR……都可以触发它执行一系列任务。</p><p>对于个人开发者来说，最常见的用法就是 <strong>自动化部署</strong>，也就是 CI&#x2F;CD。但它的能力远不止于此——编译代码、运行测试、发送通知、定时任务，只要是能写脚本完成的事，它都能干。</p><h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><p>一个 GitHub Actions 工作流由几个层级组成：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Workflow（工作流）          ← 一个 .yml 文件</span><br><span class="line">  └─ Jobs（作业）           ← 可以并行或串行</span><br><span class="line">       └─ Steps（步骤）     ← 按顺序执行</span><br><span class="line">            └─ Actions（动作） ← 可复用的公共模块</span><br></pre></td></tr></table></figure><h3 id="Workflow（工作流）"><a href="#Workflow（工作流）" class="headerlink" title="Workflow（工作流）"></a>Workflow（工作流）</h3><p>工作流就是一个 YAML 文件，放在仓库的 <code>.github/workflows/</code> 目录下。一个仓库可以有多个工作流文件，分别处理不同的事情。</p><h3 id="Events（触发器）"><a href="#Events（触发器）" class="headerlink" title="Events（触发器）"></a>Events（触发器）</h3><p>工作流靠事件来触发。最常用的是 <code>push</code>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">on:</span></span><br><span class="line">  <span class="attr">push:</span></span><br><span class="line">    <span class="attr">branches:</span> [<span class="string">master</span>]</span><br></pre></td></tr></table></figure><p>其他常见事件：</p><ul><li><code>pull_request</code> — 有人提交 PR 时触发</li><li><code>schedule</code> — 定时触发（cron 表达式）</li><li><code>workflow_dispatch</code> — 手动在 GitHub 页面上点击运行</li><li><code>issue_comment</code> — 有人评论 Issue 时触发</li><li><code>release</code> — 发布 Release 时触发</li></ul><h3 id="Jobs（作业）"><a href="#Jobs（作业）" class="headerlink" title="Jobs（作业）"></a>Jobs（作业）</h3><p>一个工作流可以包含多个 job，默认是并行执行的。如果 job B 依赖 job A 的结果，可以用 <code>needs</code> 来控制顺序：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">jobs:</span></span><br><span class="line">  <span class="attr">test:</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br><span class="line">    <span class="attr">steps:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">run:</span> <span class="string">npm</span> <span class="string">test</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">deploy:</span></span><br><span class="line">    <span class="attr">needs:</span> <span class="string">test</span>          <span class="comment"># 等 test 跑完才执行</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br><span class="line">    <span class="attr">steps:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">run:</span> <span class="string">npm</span> <span class="string">run</span> <span class="string">build</span></span><br></pre></td></tr></table></figure><h3 id="Steps（步骤）"><a href="#Steps（步骤）" class="headerlink" title="Steps（步骤）"></a>Steps（步骤）</h3><p>每个 job 由多个 step 组成，按顺序执行。每个 step 可以是一条命令，或者引用一个 Action：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">steps:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">拉取代码</span></span><br><span class="line">    <span class="attr">uses:</span> <span class="string">actions/checkout@v4</span></span><br><span class="line"></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">安装依赖</span></span><br><span class="line">    <span class="attr">run:</span> <span class="string">npm</span> <span class="string">install</span></span><br><span class="line"></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">运行测试</span></span><br><span class="line">    <span class="attr">run:</span> <span class="string">npm</span> <span class="string">test</span></span><br></pre></td></tr></table></figure><h3 id="Actions（可复用模块）"><a href="#Actions（可复用模块）" class="headerlink" title="Actions（可复用模块）"></a>Actions（可复用模块）</h3><p>Actions 是 GitHub Marketplace 上的公共模块，像搭积木一样拼到工作流里。最常用的几个：</p><table><thead><tr><th>Action</th><th>用途</th></tr></thead><tbody><tr><td><code>actions/checkout@v4</code></td><td>拉取仓库代码</td></tr><tr><td><code>actions/setup-node@v4</code></td><td>安装指定版本 Node.js</td></tr><tr><td><code>actions/setup-python@v5</code></td><td>安装指定版本 Python</td></tr><tr><td><code>actions/cache@v4</code></td><td>缓存依赖加速构建</td></tr><tr><td><code>actions/upload-artifact@v4</code></td><td>上传构建产物</td></tr><tr><td><code>peaceiris/actions-gh-pages@v4</code></td><td>部署到 GitHub Pages</td></tr></tbody></table><h2 id="我的博客工作流拆解"><a href="#我的博客工作流拆解" class="headerlink" title="我的博客工作流拆解"></a>我的博客工作流拆解</h2><p>下面是我博客的 GitHub Actions 配置，一步步拆开看：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">name:</span> <span class="string">Deploy</span> <span class="string">Hexo</span> <span class="string">to</span> <span class="string">GitHub</span> <span class="string">Pages</span></span><br></pre></td></tr></table></figure><p>这是工作流的名字，会显示在 Actions 页面上。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">on:</span></span><br><span class="line">  <span class="attr">push:</span></span><br><span class="line">    <span class="attr">branches:</span> [<span class="string">master</span>]</span><br><span class="line">  <span class="attr">workflow_dispatch:</span></span><br></pre></td></tr></table></figure><p>触发条件：当 <code>master</code> 分支有推送时自动运行，同时支持在页面手动点击触发。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">permissions:</span></span><br><span class="line">  <span class="attr">contents:</span> <span class="string">write</span></span><br></pre></td></tr></table></figure><p>给工作流分配权限，<code>contents: write</code> 允许它向仓库推送代码。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">jobs:</span></span><br><span class="line">  <span class="attr">deploy:</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br></pre></td></tr></table></figure><p>定义了一个叫 <code>deploy</code> 的作业，运行在最新版 Ubuntu 上。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">steps:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">uses:</span> <span class="string">actions/checkout@v4</span></span><br><span class="line">    <span class="attr">with:</span></span><br><span class="line">      <span class="attr">fetch-depth:</span> <span class="number">0</span></span><br></pre></td></tr></table></figure><p>第一步：把仓库源码拉到虚拟机上。<code>fetch-depth: 0</code> 表示拉取完整 Git 历史，某些插件需要用到。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="bullet">-</span> <span class="attr">uses:</span> <span class="string">actions/setup-node@v4</span></span><br><span class="line">  <span class="attr">with:</span></span><br><span class="line">    <span class="attr">node-version:</span> <span class="number">20</span></span><br></pre></td></tr></table></figure><p>第二步：安装 Node.js 20。<code>setup-node</code> 会自动处理下载和环境变量。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Install</span> <span class="string">dependencies</span></span><br><span class="line">  <span class="attr">run:</span> <span class="string">npm</span> <span class="string">install</span></span><br><span class="line"></span><br><span class="line"><span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Generate</span> <span class="string">site</span></span><br><span class="line">  <span class="attr">run:</span> <span class="string">npx</span> <span class="string">hexo</span> <span class="string">generate</span></span><br></pre></td></tr></table></figure><p>第三步和第四步：安装依赖、生成静态页面。和你在本地执行的 <code>npm install &amp;&amp; hexo g</code> 完全一样。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Deploy</span> <span class="string">to</span> <span class="string">GitHub</span> <span class="string">Pages</span></span><br><span class="line">  <span class="attr">run:</span> <span class="string">|</span></span><br><span class="line"><span class="string">    git clone --depth 1 --branch main https://x-access-token:$&#123;&#123; secrets.GITHUB_TOKEN &#125;&#125;@github.com/tenxiaodao/tenxiaodao.github.io.git deploy</span></span><br><span class="line"><span class="string">    rm -rf deploy/*</span></span><br><span class="line"><span class="string">    cp -r public/* deploy/</span></span><br><span class="line"><span class="string">    cd deploy</span></span><br><span class="line"><span class="string">    git config user.name &#x27;github-actions[bot]&#x27;</span></span><br><span class="line"><span class="string">    git config user.email &#x27;github-actions[bot]@users.noreply.github.com&#x27;</span></span><br><span class="line"><span class="string">    git add -A</span></span><br><span class="line"><span class="string">    git commit -m &quot;deploy: $&#123;&#123; github.sha &#125;&#125;&quot; || exit 0</span></span><br><span class="line"><span class="string">    git push</span></span><br></pre></td></tr></table></figure><p>第五步：部署。先 clone 当前的 <code>main</code> 分支（Pages 用的分支），清空后用生成的静态文件替换，提交并推送。</p><p>这里的 <code>$&#123;&#123; secrets.GITHUB_TOKEN &#125;&#125;</code> 是 GitHub 自动提供的认证令牌，每个工作流运行都会生成一个临时有效的 token，不需要你自己配置。</p><h2 id="怎么看工作流跑没跑通"><a href="#怎么看工作流跑没跑通" class="headerlink" title="怎么看工作流跑没跑通"></a>怎么看工作流跑没跑通</h2><p>推送代码后，打开 GitHub 仓库，点顶部的 <strong>Actions</strong> 标签：</p><ul><li>✅ 绿色表示成功</li><li>❌ 红色表示失败，点进去可以看到每一步的日志</li><li>🟡 黄色表示正在运行</li></ul><p>每步的日志会显示命令的输出，如果构建报错，日志里会明确告诉你在哪行出了问题。</p><h2 id="进阶技巧"><a href="#进阶技巧" class="headerlink" title="进阶技巧"></a>进阶技巧</h2><h3 id="缓存依赖加速构建"><a href="#缓存依赖加速构建" class="headerlink" title="缓存依赖加速构建"></a>缓存依赖加速构建</h3><p>如果你的项目有 <code>package-lock.json</code>（或 <code>yarn.lock</code>），可以用缓存功能：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="bullet">-</span> <span class="attr">uses:</span> <span class="string">actions/setup-node@v4</span></span><br><span class="line">  <span class="attr">with:</span></span><br><span class="line">    <span class="attr">node-version:</span> <span class="number">20</span></span><br><span class="line">    <span class="attr">cache:</span> <span class="string">npm</span></span><br></pre></td></tr></table></figure><p>这样 <code>node_modules</code> 会被缓存，后续构建就直接恢复缓存，跳过 <code>npm install</code>。<strong>但如果你的项目没有 lockfile，不要加这个选项，会报错。</strong></p><h3 id="并行-Job"><a href="#并行-Job" class="headerlink" title="并行 Job"></a>并行 Job</h3><p>如果你想在部署前先跑测试，可以拆成两个并行 job：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">jobs:</span></span><br><span class="line">  <span class="attr">lint:</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br><span class="line">    <span class="attr">steps:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">run:</span> <span class="string">npm</span> <span class="string">run</span> <span class="string">lint</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">deploy:</span></span><br><span class="line">    <span class="attr">needs:</span> <span class="string">lint</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br><span class="line">    <span class="attr">steps:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">run:</span> <span class="string">npm</span> <span class="string">run</span> <span class="string">build</span></span><br></pre></td></tr></table></figure><p><code>needs: lint</code> 确保 lint 通过了才部署。</p><h3 id="手动触发"><a href="#手动触发" class="headerlink" title="手动触发"></a>手动触发</h3><p>在 workflow 文件里加上 <code>workflow_dispatch</code>，就可以在 GitHub 页面手动点按钮触发，适合调试。</p><h3 id="定时触发"><a href="#定时触发" class="headerlink" title="定时触发"></a>定时触发</h3><p>如果需要定时任务（比如每天自动更新某个数据），用 cron 表达式：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">on:</span></span><br><span class="line">  <span class="attr">schedule:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">cron:</span> <span class="string">&#x27;0 0 * * *&#x27;</span>   <span class="comment"># 每天 UTC 0 点运行</span></span><br></pre></td></tr></table></figure><h2 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h2><p>GitHub Actions 是我用过最顺手的自动化工具之一。它对公开仓库完全免费，语法清晰，生态成熟，而且和 GitHub 深度集成，不需要额外配置任何东西。</p><p>从一个最简单的自动部署开始，你可以慢慢扩展它的用途——自动检查文章链接是否失效、自动压缩图片、自动同步到其他平台……只要你能想到的自动化任务，它几乎都能做。</p><p>你的第一个工作流跑通了吗？</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/github-actions-guide/</id>
    <link href="https://www.tenxiaodao.top/github-actions-guide/"/>
    <published>2026-06-13T08:30:00.000Z</published>
    <summary>
      <![CDATA[<h1 id="GitHub-Actions-使用指南：从入门到实战"><a href="#GitHub-Actions-使用指南：从入门到实战" class="headerlink" title="GitHub Actions 使用指南：从入门到实战"></a>GitHub A]]>
    </summary>
    <title>GitHub Actions 使用指南：从入门到实战</title>
    <updated>2026-06-13T08:30:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="DevOps" scheme="https://www.tenxiaodao.top/categories/DevOps/"/>
    <category term="Hexo" scheme="https://www.tenxiaodao.top/tags/Hexo/"/>
    <category term="CI/CD" scheme="https://www.tenxiaodao.top/tags/CI-CD/"/>
    <category term="GitHub Actions" scheme="https://www.tenxiaodao.top/tags/GitHub-Actions/"/>
    <category term="自动化部署" scheme="https://www.tenxiaodao.top/tags/%E8%87%AA%E5%8A%A8%E5%8C%96%E9%83%A8%E7%BD%B2/"/>
    <content>
      <![CDATA[<blockquote><p><strong>更新说明</strong>：本文原本配了 GitHub Actions 自动部署流程，后来还是觉得手动部署更顺手，所以已移除工作流文件。不过 CI&#x2F;CD 的思路和配置方法本身没问题，如果你感兴趣依然可以参考。</p></blockquote><h1 id="CI-CD-配置指南：让你的-Hexo-博客自动部署"><a href="#CI-CD-配置指南：让你的-Hexo-博客自动部署" class="headerlink" title="CI&#x2F;CD 配置指南：让你的 Hexo 博客自动部署"></a>CI&#x2F;CD 配置指南：让你的 Hexo 博客自动部署</h1><h2 id="什么是-CI-CD？"><a href="#什么是-CI-CD？" class="headerlink" title="什么是 CI&#x2F;CD？"></a>什么是 CI&#x2F;CD？</h2><p>CI&#x2F;CD 是 <strong>持续集成（Continuous Integration）</strong> 和 <strong>持续部署（Continuous Deployment）</strong> 的缩写。说白了就是：<strong>你只管写代码推上去，剩下的构建、测试、部署全都自动完成。</strong></p><p>以前更新博客的流程是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">本地写文章 → hexo generate → hexo deploy → 等待上传完成</span><br></pre></td></tr></table></figure><p>有了 CI&#x2F;CD 之后变成：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">本地写文章 → git push → 剩下全自动</span><br></pre></td></tr></table></figure><p>GitHub 收到你的推送后，会自动拉取代码、安装依赖、生成静态页面、部署到 Pages。整个过程在 GitHub 的服务器上完成，你本地连 Node.js 都不用装。</p><h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><h3 id="持续集成（CI）"><a href="#持续集成（CI）" class="headerlink" title="持续集成（CI）"></a>持续集成（CI）</h3><p>每次你推送代码到仓库，CI 会自动执行一系列任务：</p><ul><li>拉取最新代码</li><li>安装项目依赖（npm install）</li><li>运行构建命令（hexo generate）</li><li>验证构建是否成功</li></ul><p>如果构建失败（比如依赖出问题、模板语法错误），你会立刻收到通知，不会把坏代码部署上线。</p><h3 id="持续部署（CD）"><a href="#持续部署（CD）" class="headerlink" title="持续部署（CD）"></a>持续部署（CD）</h3><p>CI 验证通过后，CD 会自动把构建产物发布到生产环境。对于 Hexo 博客来说，就是把生成的 <code>public/</code> 目录推送到 GitHub Pages 的分支上，网站就自动更新了。</p><h2 id="我的-Hexo-博客是怎么配置的？"><a href="#我的-Hexo-博客是怎么配置的？" class="headerlink" title="我的 Hexo 博客是怎么配置的？"></a>我的 Hexo 博客是怎么配置的？</h2><p>我的博客源码托管在 GitHub，使用 GitHub Actions 作为 CI&#x2F;CD 平台。GitHub Actions 是 GitHub 自带的免费自动化服务，对于公开仓库完全没有使用限制。</p><h3 id="工作流程文件"><a href="#工作流程文件" class="headerlink" title="工作流程文件"></a>工作流程文件</h3><p>在项目根目录创建 <code>.github/workflows/deploy.yml</code>，内容如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">name:</span> <span class="string">Deploy</span> <span class="string">Hexo</span> <span class="string">to</span> <span class="string">GitHub</span> <span class="string">Pages</span></span><br><span class="line"></span><br><span class="line"><span class="attr">on:</span></span><br><span class="line">  <span class="attr">push:</span></span><br><span class="line">    <span class="attr">branches:</span> [<span class="string">master</span>]</span><br><span class="line">  <span class="attr">workflow_dispatch:</span></span><br><span class="line"></span><br><span class="line"><span class="attr">permissions:</span></span><br><span class="line">  <span class="attr">contents:</span> <span class="string">write</span></span><br><span class="line"></span><br><span class="line"><span class="attr">jobs:</span></span><br><span class="line">  <span class="attr">deploy:</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br><span class="line">    <span class="attr">steps:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Checkout</span> <span class="string">source</span></span><br><span class="line">        <span class="attr">uses:</span> <span class="string">actions/checkout@v4</span></span><br><span class="line">        <span class="attr">with:</span></span><br><span class="line">          <span class="attr">fetch-depth:</span> <span class="number">0</span></span><br><span class="line"></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Setup</span> <span class="string">Node.js</span></span><br><span class="line">        <span class="attr">uses:</span> <span class="string">actions/setup-node@v4</span></span><br><span class="line">        <span class="attr">with:</span></span><br><span class="line">          <span class="attr">node-version:</span> <span class="number">20</span></span><br><span class="line">          <span class="attr">cache:</span> <span class="string">npm</span></span><br><span class="line"></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Install</span> <span class="string">dependencies</span></span><br><span class="line">        <span class="attr">run:</span> <span class="string">npm</span> <span class="string">ci</span></span><br><span class="line"></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Generate</span> <span class="string">site</span></span><br><span class="line">        <span class="attr">run:</span> <span class="string">npx</span> <span class="string">hexo</span> <span class="string">generate</span></span><br><span class="line"></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Deploy</span> <span class="string">to</span> <span class="string">GitHub</span> <span class="string">Pages</span></span><br><span class="line">        <span class="attr">env:</span></span><br><span class="line">          <span class="attr">GIT_USER:</span> <span class="string">github-actions[bot]</span></span><br><span class="line">          <span class="attr">GIT_PASS:</span> <span class="string">$&#123;&#123;</span> <span class="string">secrets.GITHUB_TOKEN</span> <span class="string">&#125;&#125;</span></span><br><span class="line">        <span class="attr">run:</span> <span class="string">|</span></span><br><span class="line"><span class="string">          git config --global user.name &#x27;github-actions[bot]&#x27;</span></span><br><span class="line"><span class="string">          git config --global user.email &#x27;github-actions[bot]@users.noreply.github.com&#x27;</span></span><br><span class="line"><span class="string">          npx hexo deploy</span></span><br></pre></td></tr></table></figure><h3 id="流程拆解"><a href="#流程拆解" class="headerlink" title="流程拆解"></a>流程拆解</h3><p>这个工作流定义了 5 个步骤：</p><ol><li><strong>Checkout</strong> — 把源码拉到 GitHub 的虚拟服务器上</li><li><strong>Setup Node.js</strong> — 安装 Node.js 20 并启用 npm 缓存加速后续构建</li><li><strong>Install dependencies</strong> — 执行 <code>npm ci</code>，根据 <code>package-lock.json</code> 精确安装依赖（比 <code>npm install</code> 更快更稳定）</li><li><strong>Generate site</strong> — 运行 <code>hexo generate</code> 生成静态页面</li><li><strong>Deploy to Pages</strong> — 通过 <code>hexo deploy</code> 将生成的页面推送到 GitHub Pages</li></ol><p>触发条件写的是 <code>push: branches: [master]</code>，也就是只要往 master 分支推送代码，流水线就会自动启动。</p><h2 id="分支策略"><a href="#分支策略" class="headerlink" title="分支策略"></a>分支策略</h2><p>为了配合这个 CI&#x2F;CD 流程，我的博客采用了两个分支：</p><table><thead><tr><th>分支</th><th>内容</th><th>用途</th></tr></thead><tbody><tr><td><code>master</code></td><td>Hexo 源码（Markdown、主题、配置）</td><td>日常写文章、改配置</td></tr><tr><td><code>main</code></td><td>Hexo 生成的静态页面</td><td>GitHub Pages 读取的分支</td></tr></tbody></table><p>以前我需要在本地跑 <code>hexo g &amp;&amp; hexo d</code>，现在只要：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git add -A</span><br><span class="line">git commit -m <span class="string">&quot;新文章：xxx&quot;</span></span><br><span class="line">git push origin master</span><br></pre></td></tr></table></figure><p>然后去 GitHub 仓库的 <strong>Actions</strong> 标签页就能看到流水线正在运行。等跑完（一般 1-2 分钟），网站就自动更新了。</p><h2 id="CI-CD-的好处"><a href="#CI-CD-的好处" class="headerlink" title="CI&#x2F;CD 的好处"></a>CI&#x2F;CD 的好处</h2><h3 id="1-告别环境依赖"><a href="#1-告别环境依赖" class="headerlink" title="1. 告别环境依赖"></a>1. 告别环境依赖</h3><p>本地换电脑了？重装系统了？只要 GitHub 上有源码，随时可以写文章推上去，构建环境是云端统一管理的，不会出现”在我电脑上是好的啊”这种尴尬。</p><h3 id="2-构建过程可追溯"><a href="#2-构建过程可追溯" class="headerlink" title="2. 构建过程可追溯"></a>2. 构建过程可追溯</h3><p>每次部署的记录都保存在 GitHub Actions 的日志里，构建成功还是失败、用了多少时间、输出了什么内容，一目了然。</p><h3 id="3-多人协作友好"><a href="#3-多人协作友好" class="headerlink" title="3. 多人协作友好"></a>3. 多人协作友好</h3><p>如果你的博客有多位作者，每个人只需要往 master 推送文章，GitHub Actions 会自动处理后续的构建和部署，不需要每个人都配置部署密钥。</p><h3 id="4-免费且稳定"><a href="#4-免费且稳定" class="headerlink" title="4. 免费且稳定"></a>4. 免费且稳定</h3><p>GitHub Actions 对公开仓库完全免费，每月有 2000 分钟的免费运行时长。对于个人博客的更新频率来说，根本用不完。</p><h2 id="如何验证-CI-CD-是否运行正常？"><a href="#如何验证-CI-CD-是否运行正常？" class="headerlink" title="如何验证 CI&#x2F;CD 是否运行正常？"></a>如何验证 CI&#x2F;CD 是否运行正常？</h2><p>代码推送后，打开 GitHub 仓库页面，点顶部的 <strong>Actions</strong> 标签：</p><ol><li>如果流水线显示绿色 ✅，说明构建和部署成功</li><li>如果显示红色 ❌，点进去查看具体的报错日志，通常 <code>npm ci</code> 或构建步骤出问题会有明确提示</li></ol><p>你也可以手动触发：在 Actions 页面找到你的工作流，点 <strong>Run workflow</strong> → 选择分支 → 绿色按钮，不需要推送代码也能触发。</p><h2 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h2><p>配置 CI&#x2F;CD 是我博客搭建以来最值得的投资之一。每次更新博客从原来的多步操作变成一行 <code>git push</code>，这种体验的提升是实实在在的。</p><p>如果你的 Hexo 博客还没配 CI&#x2F;CD，花 10 分钟搞定它，往后每次更新都能省下一两分钟。积少成多，省下的时间够多写好几篇文章了。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/ci-cd-auto-deploy-guide/</id>
    <link href="https://www.tenxiaodao.top/ci-cd-auto-deploy-guide/"/>
    <published>2026-06-13T07:00:00.000Z</published>
    <summary>
      <![CDATA[<blockquote>
<p><strong>更新说明</strong>：本文原本配了 GitHub Actions 自动部署流程，后来还是觉得手动部署更顺手，所以已移除工作流文件。不过 CI&#x2F;CD 的思路和配置方法本身没问题，如果你感兴趣依然可以参考。</p>
</]]>
    </summary>
    <title>CI/CD 配置指南：让你的 Hexo 博客自动部署</title>
    <updated>2026-06-13T07:30:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>周言志</name>
    </author>
    <category term="DevOps" scheme="https://www.tenxiaodao.top/categories/DevOps/"/>
    <category term="AI" scheme="https://www.tenxiaodao.top/tags/AI/"/>
    <category term="Codex" scheme="https://www.tenxiaodao.top/tags/Codex/"/>
    <category term="OpenAI" scheme="https://www.tenxiaodao.top/tags/OpenAI/"/>
    <content>
      <![CDATA[<h1 id="Codex-使用指南"><a href="#Codex-使用指南" class="headerlink" title="Codex 使用指南"></a>Codex 使用指南</h1><p>OpenAI Codex 是一种专注于代码理解与生成的模型，适合用于编程助手、自动补全、脚本生成、代码审查等场景。本文从基础概念、使用方法、提示词设计和常见实践入手，帮助你快速掌握 Codex 的核心用法。</p><h2 id="什么是-Codex"><a href="#什么是-Codex" class="headerlink" title="什么是 Codex"></a>什么是 Codex</h2><p>Codex 是 OpenAI 系列模型中的一个分支，专门用于处理编程语言。</p><ul><li>支持多种编程语言，包括 Python、JavaScript、TypeScript、Go、Java 等。</li><li>能理解自然语言指令并生成对应代码。</li><li>适合作为开发辅助工具、代码搜索、自动化脚本生成的底层模型。</li></ul><p>Codex 与通用语言模型的主要区别在于，它更加关注“代码语义”和“代码结构”，因此在编程任务上表现更稳定。</p><h2 id="Codex-可以做什么"><a href="#Codex-可以做什么" class="headerlink" title="Codex 可以做什么"></a>Codex 可以做什么</h2><p>常见使用场景包括：</p><ul><li>代码片段自动生成</li><li>代码补全与重构建议</li><li>单元测试、文档注释生成</li><li>数据处理脚本、API 调用示例</li><li>代码审查与错误定位辅助</li></ul><p>例如，你可以让 Codex 根据需求生成一个函数实现，或者把伪代码翻译成目标语言。</p><h2 id="如何调用-Codex"><a href="#如何调用-Codex" class="headerlink" title="如何调用 Codex"></a>如何调用 Codex</h2><p>如果你使用 OpenAI API，基本流程如下：</p><ol><li>创建 API 密钥</li><li>选择 Codex 模型，例如 <code>code-davinci-002</code> &#x2F; <code>code-cushman-001</code></li><li>传入提示词（prompt）并接收生成结果</li></ol><p>示例请求：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> openai</span><br><span class="line"></span><br><span class="line">openai.api_key = <span class="string">&quot;YOUR_API_KEY&quot;</span></span><br><span class="line"></span><br><span class="line">response = openai.Completion.create(</span><br><span class="line">    model=<span class="string">&quot;code-davinci-002&quot;</span>,</span><br><span class="line">    prompt=<span class="string">&quot;Write a Python function that reverses a string.&quot;</span>,</span><br><span class="line">    temperature=<span class="number">0.2</span>,</span><br><span class="line">    max_tokens=<span class="number">120</span>,</span><br><span class="line">    top_p=<span class="number">1.0</span>,</span><br><span class="line">    frequency_penalty=<span class="number">0</span>,</span><br><span class="line">    presence_penalty=<span class="number">0</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(response.choices[<span class="number">0</span>].text.strip())</span><br></pre></td></tr></table></figure><p>这个示例演示了最基础的调用方式，真实项目中可以根据需求调整参数。</p><h2 id="提示词设计技巧"><a href="#提示词设计技巧" class="headerlink" title="提示词设计技巧"></a>提示词设计技巧</h2><p>Codex 的生成效果很大程度上依赖于提示词的质量。以下是几个常见技巧：</p><ul><li>说明语言和输出格式：<code>用 Python 写一个函数 ...</code></li><li>提供上下文和约束条件：<code>函数输入为字符串，输出为翻转后的字符串</code></li><li>先给出示例，再让模型补全：</li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 输入：hello</span></span><br><span class="line"><span class="comment"># 输出：olleh</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 输入：world</span></span><br><span class="line"><span class="comment"># 输出：</span></span><br></pre></td></tr></table></figure><ul><li>使用注释引导模型理解意图</li><li>对复杂需求拆分成多个小任务</li></ul><h2 id="实用示例"><a href="#实用示例" class="headerlink" title="实用示例"></a>实用示例</h2><h3 id="示例-1：生成函数实现"><a href="#示例-1：生成函数实现" class="headerlink" title="示例 1：生成函数实现"></a>示例 1：生成函数实现</h3><p>提示词：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">请帮我生成一个 Python 函数，接受一个字符串列表，返回按长度从大到小排序后的列表。</span><br></pre></td></tr></table></figure><h3 id="示例-2：生成-SQL-查询"><a href="#示例-2：生成-SQL-查询" class="headerlink" title="示例 2：生成 SQL 查询"></a>示例 2：生成 SQL 查询</h3><p>提示词：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">请生成一条 SQL 查询，查询用户表中最后登录时间在最近 30 天内的用户，并按登录时间降序排列。</span><br></pre></td></tr></table></figure><h3 id="示例-3：生成文档注释"><a href="#示例-3：生成文档注释" class="headerlink" title="示例 3：生成文档注释"></a>示例 3：生成文档注释</h3><p>提示词：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">为下面这个 JavaScript 函数添加 JSDoc 注释：</span><br><span class="line">function add(a, b) &#123;</span><br><span class="line">  return a + b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="常见问题与注意点"><a href="#常见问题与注意点" class="headerlink" title="常见问题与注意点"></a>常见问题与注意点</h2><ul><li>模型有时会生成语法正确但逻辑不符合预期的代码，生产环境前请务必测试。</li><li>Codex 更适合生成“确定性代码”，对于创意性或开放性任务，效果可能没有 GPT-4 那么好。</li><li>避免把敏感信息直接写入提示词或代码中。</li><li>对于大型项目，可以把 Codex 用于生成单元测试、文档、模板代码，而不是完整替代人工开发。</li></ul><h2 id="什么时候选择-Codex"><a href="#什么时候选择-Codex" class="headerlink" title="什么时候选择 Codex"></a>什么时候选择 Codex</h2><p>Codex 适合以下情况：</p><ul><li>你需要将自然语言需求转换为代码实现</li><li>你希望快速生成样板代码或 API 调用示例</li><li>你想在 IDE 中实现智能代码补全</li></ul><p>如果你的任务更偏向于对话、写作或内容创作，建议优先考虑 GPT 系列模型；如果你需要编程辅助，Codex 是更合适的选择。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Codex 是一个强大的编程助手，但它不是“万能钥匙”。正确的使用方式是：把它当成能够大幅提高效率的辅助工具，同时保持人工审核和测试。这样才能让 AI 生成的代码更稳健、更可靠。</p>]]>
    </content>
    <id>https://www.tenxiaodao.top/codex-shiyong-zhinan/</id>
    <link href="https://www.tenxiaodao.top/codex-shiyong-zhinan/"/>
    <published>2026-06-12T15:42:47.000Z</published>
    <summary>
      <![CDATA[<h1 id="Codex-使用指南"><a href="#Codex-使用指南" class="headerlink" title="Codex 使用指南"></a>Codex 使用指南</h1><p>OpenAI Codex 是一种专注于代码理解与生成的模型，适合用于编程助手]]>
    </summary>
    <title>Codex 使用指南</title>
    <updated>2026-06-12T15:42:47.000Z</updated>
  </entry>
</feed>
